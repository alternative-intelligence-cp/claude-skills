# 0.2.6 — the checks: every finding class named against its rule; the audit namespace watched; amendments re-affirm every done-means; the estimate model corrected — DONE ($25.39, 172 requests, 35.42 M, gauge 92% → 92% — ~0 points)

> Four items from the queue that are all about the same thing: a check is
> legitimate only when it enforces a rule that exists, and a rule exists
> only when something checks it. Item 3 audits every finding class against
> that test. Item 7 gives the audit namespace the citation integrity it was
> carved out of. Item 4 turns the backwards pass — the failure that made the
> final review `DRIFTED` — into a declared agreement a script can diff.
> And the estimate model gets the term the first run measured it was missing.

## 1. Where you are

0.2.4 is `DONE`. The checks are cycle 0.1's plus 0.2.2's promotion classes
and 0.2.3's two report classes. `MEASURED` at `243059e`, the classes each
check emits (from their docstrings):

| Check | Classes |
|---|---|
| `check_trace` | orphan-scope, uncovered-requirement, unmotivated-task, missing-field, unknown-reference, dependency-cycle, gate-omits-decision, re-litigated-requirement, board-drift, one-sided-link, template-drift, unrecorded-amendment |
| `check_refs` | broken-link, duplicate-id, cited-undefined, defined-uncited, bad-status, leak |
| `check_report` | no-file, no-report, wrong-task, missing-field, status-mismatch, unknown-commit, head-subject, dirty-tree, unfinished-scope, no-evidence (+ 0.2.3: budget-mismatch, model-mismatch) |
| `check_scope` | overlapping-scope, undeclared-write, empty-scope, scope-escapes-tree, unparseable-scope-entry, foreign-write |
| `check_plugin` | missing-skill, name-mismatch, bad-frontmatter, missing-script, unknown-rule, uncontrolled-check, broken-link, bad-manifest |
| `sandbox promote` | promote-no-commits, promote-history-rewrite, promote-foreign-subject, promote-out-of-scope, promote-uncommitted, promote-conflict |
| `guard` | its refusal categories — read them from `judge()` |

> **Corrected as built.** **This table is wrong in every row**, and the way it
> was produced is why — see `## Findings`. It lists 50 classes; the code emits
> **71**. `check_trace` emits 17, not 12; `check_refs` 8, not 6; `check_report`
> 13, not 12; `check_scope` 7, not 6; `check_plugin` 13, not 8; and
> `sandbox.py promote` **14**, not 6. The guard has no refusal *categories* at
> all — `judge()` returns prose, and `category=` takes one value everywhere.
> **The authority is `docs/CHECKS.md`**, which is machine-derived and held
> equal to the code in both directions by `unruled-finding` and `stale-row`.
> Read that; do not read this table.

Read: `docs/CONSOLIDATION.md` items 3, 4, 7; DESIGN §20 "Why a check works:
name the rule whose two sides it compares" and "The path nobody walks";
`templates/FORMATS.md` (the namespace section and its "safe from collision
also means unseen" paragraph); `skills/audit/SKILL.md` §"The report";
`skills/checkpoint/SKILL.md` §"The final checkpoint"; `skills/plan/SKILL.md`
§"Order" (where the estimate model lives); C-3 §2 (the DM-7 drift) and §4
(ten of ten over).

## 2. Decisions

- **L-6.1 — a check that cannot name its rule is withdrawn or its rule is
  written; never tuned.** *Why:* F-113 — its false positives are the
  missing rule showing up. Applied to three checks in 0.1 and it found all
  three proposing rules; the rest predate the test.
- **L-6.2 — before widening any grammar, run the widened rule over the
  fixture's corpus and read what it newly reports.** *Why:* DESIGN §20 "the
  detector needs a second half" — recognising `### S-n` as a declaration
  would have produced eleven `duplicate-id` findings; running it first is
  what said so. The corpus is `.internal/scratch/devteam/` (read-only).
- **L-6.3 — the amendment re-affirmation is a declared list diffed against
  a declared list.** *Why:* whether a decision made a done-means condition
  undischargeable is a semantic reading this project refuses everywhere;
  whether an amendment entry *enumerates* every DM-n and constraint row
  with a verdict is not.

## 3. The work

### 3.1 `docs/CHECKS.md` and `unruled-finding` (CONSOLIDATION 3)

One table, one row per class above: **finding → the rule whose two sides
it compares (P-n, or a `FORMATS.md` section, or a template comment) → the
two sides, named → verdict `enforces` / `proposes`.** Fill it by reading the
code, not the docstring; the docstring says what the author intended.

For every `proposes`: either write the rule (in `PROTOCOL.md` if it is
about the process, in `FORMATS.md` if it is about the grammar) with the
measured failure that justifies it, or withdraw the class — remove the
finding, its control cases, and record the withdrawal in the table with the
reason. **Expect to withdraw at least one**; a sweep that withdraws nothing
has not been adversarial.

Then `check_plugin.py` gains `unruled-finding`: the set of classes each
check declares (parsed from its docstring block — the convention is two
leading spaces and a hyphenated name, which is how this file's table was
produced) diffed against the rows of `docs/CHECKS.md`. Two declared lists.
Its control plants a class in a docstring that is absent from the table,
and the twin: a table row for a class no script emits (`stale-row`).

### 3.2 The audit namespace watched (CONSOLIDATION 7)

Per `FORMATS.md`, `COR-`, `SEC-`, `HYG-` are three-letter and therefore
invisible to `check_refs`. Make them declared, cited and checked:

1. **L-6.2 first.** Widen `check_refs`'s scanner to
   `[A-Z]{1,3}-<digits>` in a scratch copy and run it over the fixture's
   `devteam/`. Read every new line. Record the count and what they are
   under `## Findings` **before** changing the shipped script. This is the
   step that was skipped when the `F` namespace landed (F-63, F-64: a green
   tree became 62 `cited-undefined` at once).
2. Declarations: an audit finding is declared by `- **COR-6.** <one line>`
   in `devteam/audits/*.md` (the form the audit skill already prescribes).
   Reserve `COR`, `SEC`, `HYG`, `REV`, `CNV` in `FORMATS.md`'s table as
   *checked* three-letter prefixes; the paragraph "safe from collision also
   means unseen" is rewritten to say what now watches them and what is
   still carved out (nothing three-letter beyond those five).
3. `check_refs` resolves those citations in both directions
   (`cited-undefined`, `defined-uncited` apply as they do to `D-n`) —
   **and here `defined-uncited` is wrong for an audit finding**: a finding
   nobody cites is the normal state of a finding under `Disposition. open`.
   So the rule is: an audit finding must be cited *or* carry a `Disposition.`
   other than `open`; report `undispositioned-finding` for one with neither
   (this is CONSOLIDATION 7's check, stated so it passes L-6.1).
4. `open-finding-at-close`: in `check_trace` (it already reads task titles
   and statuses), a finding whose `Disposition.` is `open` and whose audit
   file names a task (from the filename `T-n-<dimension>-<date>.md`) whose
   title is `DONE` or `ACCEPTED`. Its control, and the twin: `routed T-m`
   with T-n closed → clean.
5. The existing control asserting three-letter prefixes need no reserving
   is now **inverted deliberately**: it asserts the five reserved ones are
   resolved and any other three-letter prefix is ignored. Record the
   inversion in the control's own comment with F-63's reason.

### 3.3 Amendments re-affirm every done-means and constraint (CONSOLIDATION 4)

The final review found `DM-7` undischargeable because a decision (D-37)
had changed the project's nature and three later amendments never re-read
it. Make the re-read a declared part of amending:

1. Rule (in `PROTOCOL.md`, new number): **an amendment entry enumerates every
   `DM-n` and every constraint row with one of `holds`, `amended (this
   entry)`, or `struck (D-n, why)`.** Cite C-3 §2 and F-128 as the measured
   failure, and say why enumeration and not judgement: the script can see
   that DM-7 was listed as `holds` under an amendment made on the same day
   as D-37, and a reader can then ask the question a script cannot.
2. `templates/CHARTER.md` §Amendments: the entry form, with the enumeration
   block.
3. `check_trace` gains `amendment-omits-condition`: the charter's current
   DM list and constraint row list diffed against the latest amendment
   entry's enumeration — a missing item is the finding; an item the charter
   no longer has is `amendment-names-unknown`. Two declared lists. Controls
   for both and the twin. **Run it over the fixture first (L-6.2)**: its
   charter is at v17 and will report every amendment as omitting the block —
   that is the migration cost, and the finding text must say how to
   backfill (one entry enumerating the current state, dated, citing this
   rule), the way the `F` namespace migration was declared in one block.
4. `checkpoint`'s GATE 4 section and `iterate` §2 each gain one line: the
   backwards pass's mechanical half is this check; the reading half is the
   questions already there.

### 3.4 The estimate model, third revision

C-3 §4: ten of ten over, 1.78×, after two revisions that both moved the
per-unit figure and neither touched the shape. The overrun is on **rounds**
— steps run over steps planned (F-101, F-102). The model in `plan`
§"Order" and the guidance in `templates/tasks/TASK.md`'s `Estimate.` become:

```
tokens = steps × unit × (1 + r) + overhead
  unit     = per step-unit, measured: 440,000 at C-1's second revision  (cycle 0.1)
  r        = rounds rate, measured: 0.78 over ten tasks                  (cycle 0.1, C-3 §4)
  overhead = per task, measured: 150,000                                 (cycle 0.1, T-2's note)
```

with the rule that **`r` is corrected from the harness's measured budgets
at every checkpoint** (0.2.3 gives the numbers for free), and the sample
stated beside every constant. State also what the model still does not
capture: a task whose rounds come from a rule conflict rather than a defect
(the three worst overruns were that), which no estimate can price and which
0.2.7's sweep is for.

## 4. Verification

```
python3 plugins/devteam/scripts/check_plugin.py     → clean, unruled-finding included
python3 plugins/devteam/scripts/run_controls.py     → all N controls green
python3 plugins/devteam/scripts/check_refs.py  .internal/scratch   → (read-only run) the audit namespace resolves; the count of new findings matches what §3.2 step 1 recorded
python3 plugins/devteam/scripts/check_trace.py .internal/scratch  → amendment-omits-condition on the fixture's charter, with the backfill instruction in its text
```

`docs/CHECKS.md` covers every class: `grep -c` the table rows against the
sum of the docstring classes, and paste both numbers.

## 5. Done when

- [x] `docs/CHECKS.md` names a rule for every class or records a withdrawal; at least one class withdrawn or one rule written, with the evidence — **71 classes, 71 rows; two withdrawn, four rules written (P-45, P-46, P-47)**
- [x] `unruled-finding` and `stale-row` in `check_plugin`, with controls — **28 cases, five mutations, all caught**
- [x] the audit namespace is declared, cited, checked; `undispositioned-finding` and `open-finding-at-close` exist with controls; the corpus run was done first and recorded — **and it inverted the planned rule**
- [x] the amendment rule (**P-48**), template form, `amendment-omits-condition`, `amendment-names-unknown`, and the two skill lines exist; the fixture's migration cost is recorded — **one finding, 0 of 24 re-affirmed**
- [x] the estimate model carries `r` with its sample and its correction rule — **and states what it still cannot price**
- [x] controls and `check_plugin` clean; cost in the title line — **13 controls green, `check_plugin` clean at 48 rules, cost read from the owner's `/usage`**

## 6. Do not

- do not parse prose to decide whether a finding was "really" dispositioned; `Disposition.` is the declared side and the only one
- do not widen the scanner to every three-letter prefix; five reserved, the rest ignored, and the ignored set is written down as ignored
- do not add a check whose two sides you cannot name in `docs/CHECKS.md` — that is the whole subcycle failing its own test
- do not fix the fixture's charter to satisfy the new amendment check; it is cycle 1's signed text and the backfill is cycle 2's first amendment (0.2.9)

## Findings

Recorded 2026-09-07. Every number has the command that produced it beside it.
Paths are elided as `<repo>`. **This section is written as the work proceeds**,
not at the close, because the two sessions before this one exited and their
knowledge survived only in the file.

### §1's table is wrong, and the convention §3.1 prescribes is why

§3.1 says `unruled-finding` parses each check's declared classes *"from its
docstring block — the convention is two leading spaces and a hyphenated name,
which is how this file's table was produced"*. **That convention does not
describe the docstrings.** Written three ways over the same five files:

```
2 spaces + hyphenated name + 2 spaces          -> 44 declared
  + single-word names (leak, unreadable)       -> 46
  + single-space fallback for long names       -> 47
AST over add()/findings.append() emit sites    -> 57   (stable across all three runs)
```

It breaks on two shapes: **single-word classes** (`leak`, `unreadable`), which
are not hyphenated, and **the longest names** (`unverified-requirement`,
`unreachable-acceptance`, `bad-report-status`), where the name overflows the
description column and the padding falls back to one space. The docstrings are
aligned prose. *A form written in prose is not a grammar* — 0.2.5's finding,
one file over.

**Ten classes are emitted, carry control cases in their test file, and appear
in no docstring:**

| Check | Emitted, controlled, undeclared |
|---|---|
| `check_trace` | `unparseable-task`, `bad-kind`, `unjustified-task` |
| `check_refs` | `unreadable`, `not-utf8`, `control-character` |
| `check_report` | `model-mismatch`, `budget-mismatch` (0.2.3 added these) |
| `check_scope` | `misattributed-write` |
| `check_plugin` | `namespace-drift` |

`0.2.6.md` §1's own table inherits the gap and adds to it — it lists 12 for
`check_trace` where the code emits 17, and 6 `promote-*` where `sandbox.py`
emits 14. **`MEASURED`, and the plan's table is the thing being corrected:**

```
$ grep -o 'promote-[a-z-]*' sandbox.py | sort -u | wc -l      -> 14
```

**Why this is not a parser bug to fix in passing.** If the docstring is the
declared side, then `unruled-finding` — whose entire job is catching classes
with no rule written — would have been blind to all ten classes that have no
rule written. The instrument would answer an adjacent question and pass. That
is P-35b, in the check this subcycle exists to add: the third instance of the
shape this cycle has recorded, after 0.2.4's closed set written from memory and
0.2.5's controls that constructed the precondition the live run destroys.

> **Corrected as built.** The declared side is the **AST emit set**, not the
> docstring: the string literal in the first argument of every bare `add(...)`
> and `findings.append((...))` call. §3.1's own instruction already says to
> fill the table *"by reading the code, not the docstring"*; this makes the
> check agree with it. The class enumerations are then **removed from the five
> docstrings** and their rationale prose moved into `docs/CHECKS.md`, giving
> the mapping one home (P-34) and **deleting the drift surface rather than
> adding a third check to watch it**. The ten-class drift is the measured
> evidence that a second home drifts.

**The parser has a trap one level down, found by a peer session reviewing the
resolution rather than by the author.** Two shapes look like emit sites and are
not: the dispatcher's own definition
`add = lambda kind, where, detail: findings.append((kind, where, detail))`,
whose first argument is the parameter `kind`; and `set.add()` —
`seen.add(name)`, `discharged.add(r)` — an unrelated method sharing the name.
**A parser that silently skips any non-literal first argument handles both
correctly by accident**, and would also silently skip a genuine emit site
written with a computed name, blinding `unruled-finding` to exactly the class
most likely to need a rule. So the parser matches **bare `add(...)` only**,
excludes the lambda's own assignment, and **fails loudly on any surviving
non-literal rather than skipping it**. That assertion fires on nothing today,
which is the point: it is a tripwire, not a filter.

**57 per-file, 55 distinct.** `broken-link` is emitted by both `check_refs` and
`check_plugin`; `missing-field` by both `check_trace` and `check_report`. A
shared name is not a shared rule — `missing-field` in a task file and in a
report are different defects — so `docs/CHECKS.md` keys rows by
**(check, class)** rather than by class alone.

### The guard's refusals have no names, so nothing can diff them

§1's table sends the reader to `judge()` for *"its refusal categories"*.
`MEASURED` — there are none. Every refusal is a formatted prose string returned
inline; there is no category constant, no enum, and `category=` takes exactly
one value (`"write"`) at every call site. Eight distinct refusal families are
distinguishable by reading, and none is nameable by a script:

```
$ grep -o 'category="[a-z-]*"' guard.py | sort -u    -> category="write"
```

So the guard — the most safety-critical component in the plugin, and the one
whose refusals are read by a worker under time pressure — **cannot have its
refusals counted, cited, or asserted by name.** Its controls match on substrings
of the message text, which is why 0.2.5's rotation refusal could regress to the
generic message and the suite stayed green.

`docs/CHECKS.md` names the eight families so they can be referred to at all.
**Naming them in `guard.py` is deliberately NOT done here** — it changes the
return contract of the file with the widest blast radius in the plugin, mid-way
through a subcycle whose four items are already committed to, and 0.2.4 and
0.2.5 both recorded the guard's refusal path as where their sharpest defects
lived. It is written up as the entry condition for a later subcycle rather than
attempted late in this one.

### ~~L-6.2's corpus run is vacuous~~ — WRONG, and corrected below

L-6.2 requires running a widened grammar over the fixture corpus and reading
what it newly reports, *"before changing the shipped script"* — the step whose
absence let the `F` namespace land as 62 `cited-undefined` at once (F-63, F-64).
**Run as prescribed, it reads nothing at all:**

```
$ python3 scripts/check_refs.py .internal/scratch
.internal/scratch/devteam: clean
$ git ls-files .internal/scratch/devteam | wc -l
0
```

`check_refs` enumerates by `git ls-files` (`tracked_markdown()`), and the corpus
is **untracked**. The `clean` is not a measurement; the check never opened a
file. This is 0.2.5's vacuous-control shape exactly — *an earlier branch
returned before the code under test ever ran* — sitting inside the decision
whose whole purpose is to prevent an unmeasured migration.

Made non-vacuous in a scratch copy by replacing the enumeration with a tree
walk: **387 files**. The real measurement follows.

> **CORRECTED THE SAME SESSION, BEFORE THE SUBCYCLE CLOSED. THE CLAIM ABOVE IS
> FALSE and is left standing rather than deleted (P-23).** The corpus run was
> never vacuous. `MEASURED`:
>
> ```
> $ ls -d .internal/scratch/.git                       -> exists
> $ git ls-files .internal/scratch/devteam | wc -l     -> 0
> $ git -C .internal/scratch/devteam ls-files -- '*.md' | wc -l   -> 32
> $ git -C .internal/scratch/devteam rev-parse --show-toplevel
>   /…/claude-skills/.internal/scratch
> $ git check-ignore -v .internal/scratch/devteam/CHARTER.md
>   .gitignore:2:.internal/
> ```
>
> **`.internal/scratch` is its own git repository**, and the outer repository
> ignores `.internal/`. So `git ls-files` *from the repository root* returns 0
> — which is what was measured — while `tracked_markdown()` runs
> `git -C <corpus> ls-files`, which resolves to the **inner** repository and
> returns 32 tracked files. The check reads them. It always did.
>
> **How the error was made, because the shape is the one this cycle keeps
> recording.** A single command was run against the wrong repository, its
> answer (`0`) fitted a hypothesis that had already been formed, and the
> `clean` result the shipped script produced was read as *confirmation of
> vacuity* when it was the correct answer to the question actually asked —
> before the audit prefixes were reserved there was genuinely nothing to
> report. **A measurement that agrees with a hypothesis is the one that most
> needs a second command**, and the second command here was one line:
> `rev-parse --show-toplevel`.
>
> It was caught by running §4's verification block as the plan specifies it,
> which is what that block is for. Nothing downstream changes: the shipped
> script reports the same **18 findings** over its 32 tracked files as the
> tree-walk does over 387, so the before/after figures below stand and only
> the stated reason for using a tree walk was wrong.

### Widening the scanner alone does nothing, and the reason defuses the obvious hazard

```
CITATION = re.compile(r"\b([A-Z]{1,2})-(\d+)\b")     # the regex
KNOWN    = {"G","DM","R","T","S","D","Q","C","F"}    # the gate
EXTERNAL = {"P"}
```

Widening `CITATION` to `[A-Z]{1,3}` over all 387 files reports **zero** new
findings, because an identifier whose prefix is in neither set is discarded
after matching. The prefix set is the gate; the regex width is not.

**That also disposes of the hazard this widening obviously raises.** `UTF-8`
matches `[A-Z]{3}-\d+` and occurs **639 times** in the corpus — as do `RFC-`,
`ISO-`, `SHA-` shapes generally. None becomes a finding, because `UTF` is not a
reserved prefix. The carve-out that makes three-letter prefixes invisible is the
same mechanism that makes them safe to widen to.

### The measured migration cost: 32 findings, and what they actually are

Scanner widened, the five prefixes reserved, 387 files, read-only:

```
$ python3 <scratch>/check_refs_wide.py .internal/scratch
.internal/scratch/devteam: 32 finding(s)
  cited-undefined   32      COR 16 · SEC 15 · HYG 1
```

**None of the 32 is a defect in the project.** Broken down:

- **21 are genuinely cited and never declared in any form** — but that is
  because the audits declare in a form the scanner was not taught, not because
  the citations dangle.
- **`COR-99` is `RECORD.md` discussing this very check**, quoting
  CONSOLIDATION item 7's hypothetical *"an audit may cite `COR-99`, which
  exists nowhere"*. Widening the scanner turns documentation **about**
  identifiers into identifiers. `TEACHING` exempts the form `` `X-<n>` `` and
  does not exempt a concrete example.

### The audit namespace has FOUR declaration forms and the skill prescribes the one nobody uses

`MEASURED` over the corpus's four audit files:

| Form | Where | Findings | `Disposition.` |
|---|---|---|---|
| `**COR-n — title.**` | `T-1-correctness`, `T-1-security` | 12 + 4 | **0** |
| `## COR-n — title` | `T-5-correctness`, `T-5-security` | 9 + 6 | 9 + 6 |
| `- **COR-6.** <one line>` | **the audit skill, §"The report"** | **0 in the wild** | — |

§3.2 step 2 says an audit finding is declared by `- **COR-6.** <one line>`,
*"the form the audit skill already prescribes"*. It does prescribe it, and
**no audit has ever used it.** Building the check against that form alone makes
every existing audit finding undeclared — a 100% migration cost against a form
chosen from a document rather than from the tree.

Two generations are visible: `T-1`'s audits predate `Disposition.` entirely
(**16 findings, zero dispositions**), `T-5`'s carry it on every finding. So the
`open-finding-at-close` check of §3.2 step 4 has, on this fixture, sixteen
findings it cannot classify at all — they have no `Disposition.` line to read.

### §3.1 as built: 71 classes, 71 rows, and the two the sweep withdrew

`docs/CHECKS.md` names a rule for every finding class the pipeline emits.
`MEASURED` at the close of §3.1:

```
check_trace.py     emits 17  rows 17  OK
check_refs.py      emits  7  rows  7  OK
check_report.py    emits 13  rows 13  OK
check_scope.py     emits  7  rows  7  OK
check_plugin.py    emits 13  rows 13  OK
sandbox.py promote emits 14  rows 14  OK
guard.py           no names in the code   rows 8
TOTAL: 71 classes covered by 71 rows
```

**Six classes failed L-6.1.** Two withdrawn, four given rules —
**P-45** (a task graph is acyclic, and a probe names what it de-risks; covers
`dependency-cycle` and `unjustified-task`), **P-46** (a requirement rewritten
three or more times is re-opened for shape review; covers
`re-litigated-requirement`, whose docstring conceded it reported *"not a
defect: a signal"*), and **P-47** (a tracked artifact contains only what a
reader can see, and no credential; covers `leak` and `control-character`).
`leak` has been the highest-severity class in the pipeline since cycle 0.1 and
had no rule behind it.

### The withdrawal, and the reason nothing was lost

`unreadable` and `not-utf8` both reported **"the check could not read a file"**
as an exit-1 finding, while the script already distinguishes that case with
exit 2. A clean project and an undecodable one gave the verifier the same exit
code, and the exit code is the only thing it reads (P-19). Both are now exit-2
conditions: the information moves to the path that means what it says.

**They had no control cases at all.** `MEASURED`:

```
$ grep -c 'unreadable\|not-utf8' test_check_refs.py    -> 0
$ grep -c 'control-character'    test_check_refs.py    -> 4
```

So two finding classes shipped for a full cycle with no case that had ever
failed for them. **P-35's guarantee is per-SCRIPT, not per-CLASS** —
`uncontrolled-check` asks whether `check_refs.py` has a control beside it, and
it does, so a class inside it can be born and live uncontrolled and nothing
anywhere reports it. `unruled-finding` closes half of this (every class now
names a rule); the other half — every class has a case — is a check nobody has
written, and it is the natural companion to this subcycle's work.

### The tripwire fired on its first run, against its author

The parser was specified to **fail loudly on a non-literal class name** rather
than skip it, because a silent skip would also skip a genuine emit site written
with a computed name — blinding `unruled-finding` to exactly the class most
likely to need a rule. Both this session and the peer that reviewed the design
predicted it would fire on nothing. **It fired immediately, twice:**

```
unruled-finding  scripts/check_report.py  check_report.py:200 emits a finding
                 whose class is not a string literal
unruled-finding  scripts/check_scope.py   check_scope.py:100  (same)
```

Neither was an emit site. `out.append((path, n, text))` in `check_report` and
`unparsed.append((rel, n, line))` in `check_scope` are ordinary list builds,
and the first draft of the parser accepted **any** `.append((tuple))`. The arm
is now bound to the list named `findings` specifically.

**The finding is not the bug; it is that the loud failure caught its own
author before it caught anything else.** A parser that skipped silently would
have been *correct on both files by accident* and would have shipped with the
defect intact.

### The mutation that survived, and the case that was missing

Five mutations, each asserted applied before its run and restored in a
`finally`. Four were caught by the first draft of the controls. **The fifth
survived:**

| Mutation | First draft | After |
|---|---|---|
| the `unruled-finding` emit deleted | `FAIL` ×2 | `FAIL` ×2 |
| the `stale-row` emit deleted | `FAIL stale-row` | same |
| the tripwire skips instead of raising | `FAIL unruled-finding-computed-class-name` | same |
| the `docs/` gate removed | `FAIL fp-no-docs-directory-skips-the-check` | same |
| **the append arm accepts any `.append`** | **SURVIVED** | `FAIL fp-an-ordinary-list-append-is-not-an-emit-site` |

The surviving mutation reintroduces **the exact defect the tripwire had already
caught in the shipped tree**, and the suite could not see it: nothing in the
fixture appended a tuple to a list that was not `findings`, so the loose arm and
the tight one were indistinguishable. *A control written from the mechanism's
intended shape tests the intended shape* — 0.2.5's finding, third instance.
The case now plants an ordinary list append and asserts it is **not** an emit
site.

**The control derives its expected set with a regex, not with the AST walker it
is controlling.** A control that built its expectation with the instrument
under test would agree with it by construction and could never fail.

### §3.2 as built: the namespace is watched, and the planned rule reported nothing

The five prefixes are reserved and checked; everything else three-letter is
still ignored, deliberately and by name. `MEASURED` over the fixture corpus
(387 files, tree-walked because the corpus is untracked):

```
before, shipped grammar                    ->  clean  (VACUOUS — zero files read)
scanner widened, prefixes reserved         ->  32 cited-undefined
+ the heading declaration form recognised  ->  13 cited-undefined
```

**The 13 are real, and every one had been invisible for the whole of cycle
0.1** — `COR-13`, `COR-14`, `COR-15` cited from `tasks/T-7.md` and declared
nowhere; `SEC-2` cited from `DECISIONS.md`; six more from `RECORD.md`.
CONSOLIDATION item 7 offered *"an audit may cite `COR-99`, which exists
nowhere, and nothing reports it"* as a hypothetical. It is in the corpus.

> **Corrected as built — the declaration form.** §3.2 step 2 says an audit
> finding is declared by `- **COR-6.** <one line>`, *"the form the audit skill
> already prescribes"*. It does prescribe it and **no audit has ever used it**:
> two of the corpus's four audit files use `**COR-n — …**`, two use
> `## COR-n — …`, and the skill's list form appears nowhere. The **heading** is
> canonical — it is what the audits carrying `Disposition.` use, and it matches
> how `R-`, `D-`, `Q-`, `T-` and `C-` are declared everywhere else. The skill
> is corrected rather than the tree, with the one-line migration stated.

### The planned rule reported zero, and the reason is the one CONSOLIDATION 7 wrote down

§3.2 step 3 specifies: *an audit finding must be cited **or** carry a
`Disposition.` other than `open`*. Both halves measured over the corpus:

| Rule | Findings |
|---|---|
| cited **or** non-open `Disposition.` — **as planned** | **0** |
| non-open `Disposition.` alone | **5**, every one real |

The corpus holds sixteen findings with **no `Disposition.` line at all**, and
the planned rule reported none of them — because they are cited, in
`RECORD.md`, `QUESTIONS.md` and the charter.

**That is the same defect CONSOLIDATION item 7 measured and warned about in the
same paragraph it proposed the field in:** *"A 'declared in an audit, cited
nowhere else' rule reports **zero** on that project, because all eleven were
mentioned — the manager had logged them in the record. **Mention is not
disposition.**"* Letting a citation excuse a missing disposition rebuilds
exactly the hole the field was added to close.

> **Corrected as built.** The rule is **`Disposition.` alone**. `defined-uncited`
> still does not apply to audit findings — a finding nobody cites is the
> ordinary state of one under `open` — but a citation is not an escape from
> having been decided about. A control case pins it: an open finding that IS
> cited in the record still reports.

**The general shape, and it is worth carrying.** A document can contain the
correct analysis of a trap and a design that falls into it, four paragraphs
apart, because the analysis is prose and the design is a specification. Nothing
diffs the two. This is the second time in two subcycles that a plan's own cited
source contradicted the plan — 0.2.5 found the same thing about the direction
of the first rotation message — and both were found by *reading the plan against
the documents it cites rather than against itself*.

### `namespace-drift` would have silently stopped covering the namespace it watches

`check_plugin`'s `namespace-drift` diffs `FORMATS.md`'s reserved-prefix table
against `check_refs`'s own sets — and matched `[A-Z]{1,2}` in **both** halves.
Reserving five three-letter prefixes without widening it would have left the
new namespace unwatched by the check whose entire job is watching namespaces:
*a thing exempted from a check for its own protection is a thing the check
cannot see*, one level up, inside the instrument written to catch that shape.
Widened with the scanner, and it reads the new `AUDIT` set as well as `KNOWN`
and `EXTERNAL`.

### `open-finding-at-close`, and what it found on the corpus

```
$ check_trace.py <corpus>
audits/T-1-security-2026-09-04.md:12  SEC-8 is still open and T-1 is DONE
audits/T-1-security-2026-09-04.md:43  SEC-1  …
audits/T-1-security-2026-09-04.md:63  SEC-6  …
audits/T-1-security-2026-09-04.md:80  SEC-4  …
audits/T-1-security-2026-09-04.md:94  SEC-7  …
```

Five findings the task closed over. P-31 puts the audit **before** the close,
so `T-1` closed over evidence it had itself commissioned. The task id is read
from the **filename**, whose grammar the audit skill already fixes, so nothing
is inferred from prose.

> **CORRECTED.** An earlier version of this paragraph said `check_trace`'s
> corpus run was non-vacuous *"unlike `check_refs`'s"* and drew a conclusion
> about two checks disagreeing on what "reads the project" means. **Both read
> the corpus correctly**; see the correction above. The two do enumerate
> differently — `check_refs` by `git ls-files`, `check_trace` by reading the
> task directory — and that difference is real and undocumented, but it cost
> nothing here and is not the finding this paragraph claimed it was.

### The controls

```
check_refs   55 -> 63 cases (38 false-positive, 60%)
check_trace  60 -> 65 cases (36 false-positive, 55%)
check_plugin 18 -> 28 cases (11 false-positive, 39%)
all 13 controls green
```

Nine mutations across the two mechanisms, each asserted applied and restored in
a `finally`. **All nine caught.** The one worth naming: *restoring the citation
as an escape from the disposition* fails exactly one case — the one written for
it — which is the only reason a future edit cannot quietly undo the correction
above.

**The existing three-letter control was inverted rather than deleted** (§3.2
step 5). It asserted that a three-letter prefix needs no reserving, which was
true while the scanner could not see one. It now asserts that a **reserved**
prefix resolves in both directions and every **other** three-letter prefix is
still ignored, with F-63's reason in its comment: widening a grammar without
asking what it newly matches is what turned a green tree into 62
`cited-undefined` at once.

**And the fixture had to be corrected before the false-positive twins meant
anything.** Closing `T-1` to test a finding-at-close also fires `one-sided-link`,
because `R-1`'s status stayed `open` — so two twins were failing on a class
they were not about. A false-positive control that reports the wrong class is
not a false-positive control.

### §3.3 as built: the backwards pass has a mechanical half

**P-48** — an amendment re-affirms every done-means condition and every
constraint row by enumerating them, each with `holds`, `amended (this entry)`
or `struck (D-n, why)`. `check_trace` gains `amendment-omits-condition` and
`amendment-names-unknown`; `templates/CHARTER.md` carries the entry form;
`checkpoint`'s GATE 4 and `iterate` §4 each say the mechanical half runs first.

**The migration cost, `MEASURED` over the fixture (L-6.2, before shipping):**

```
$ check_trace.py <corpus>
amendment-omits-condition  CHARTER.md:184  the latest amendment
  (Version 17 — 2026-09-05 — …) re-affirms 0 of 24 — omits DM-1 … and 18 more.
  Backfill with ONE entry, dated today, citing P-48 and enumerating the
  charter's current state.

  done-means conditions: 7        constraint rows: 17        total: 24
```

One finding, not 24, and the backfill instruction is in the finding text — the
same shape the `F` namespace migration was declared in. **The fixture is not
fixed** (§6): its charter is cycle 1's signed text and the backfill is cycle 2's
first amendment.

### "The latest amendment" is a declared field, not a position

The first draft took the **last** `###` entry in the section. Charters are
written **newest first**, so it read **Version 2 of a charter at Version 17** —
the oldest entry in the document — and reported against it. `MEASURED`:

```
first draft:  the latest amendment (Version 2 — 2026-09-04 — G-3 carries R-7)
corrected:    the latest amendment (Version 17 — 2026-09-05 — the out-of-scope…)
```

The version number is now parsed and the maximum wins, with document order only
as a fallback; and the newest-first convention is **stated in the template**,
where it had never been written down. The plan said *"the latest amendment
entry"* and no document anywhere said how a script identifies one.

### Two mutations survived, and both times the case was the problem

| Mutation | First draft | After |
|---|---|---|
| the verdict vocabulary accepts anything | `FAIL` | same |
| constraint rows not required | `FAIL` ×3 | same |
| `amendment-names-unknown` never emitted | `FAIL` | same |
| **latest entry chosen by position, not version** | **SURVIVED** | `FAIL amendment-latest-is-by-version-not-position` |
| **fires with no `## Amendments` section at all** | **SURVIVED** | `FAIL fp-charter-subheadings-outside-an-amendments-section` |

**Neither survival was a defect in the mechanism; both were cases that could
not tell the two behaviours apart.** The ordering case had *both* entries
complete, so position and version selected indistinguishable things — the newer
entry is now complete and the older deliberately deficient, which is the only
arrangement that discriminates. The gate case never fired because the fixture
charter happens to contain no `###` line at all, so "gated on the section" and
"scan the whole file" produced identical output; the case now puts a subheading
in a charter with no amendments.

**This is the same shape as §3.1's surviving mutation and 0.2.5's two**, and it
is worth stating as a rule rather than as three anecdotes: **a false-positive
twin only controls anything if the mutation would change its verdict.** A twin
built from the mechanism's intended shape usually cannot, because the intended
shape is what both the correct and the broken version produce.

**And the fixture had to be built from the same lists the check reads.** A
hand-written `Re-affirmed.` block listing three rows fell out of step with the
template's sixteen the moment it was written, and reported a class the case was
not about. It is now generated from `_template_rows()` and the done-means list,
so it cannot drift.

### §3.4 as built: the shape changed, not the constant

C-3 §4 measured **ten tasks out of ten over, at 1.78×**, after two revisions
that both moved the per-unit figure and **neither touched the shape of the
model**. Moving a constant a third time would have been the same mistake a
third time. The model now separates the term the overrun actually lives in:

```
tokens = steps × unit × (1 + r) + overhead

  steps     estimated step-units, as held at PLANNING time
  unit      440,000   sample: 6 tasks, cycle 0.1, C-1's second revision
  r         0.78      sample: 10 tasks, cycle 0.1, C-3 §4
  overhead  150,000   sample: cycle 0.1, T-2's note
```

**Every constant carries its sample**, because `440,000` quoted bare invites the
next reader to treat it as a property of the pipeline rather than a measurement
of one project. **`r` is re-fitted at every checkpoint** from the harness's own
`meta/budget.json` figures, which 0.2.3 made free — so the rounds rate is
corrected from data already collected rather than re-argued (P-41).

**And the model states what it cannot price.** The three worst overruns in the
measured set were tasks whose extra rounds came from a **rule conflict** rather
than a defect: the work was correct and the loop could not accept it, because
two of the pipeline's own rules could not both be satisfied at once. No estimate
can price how often a project's rules contradict each other, and a constant
fitted to absorb it would be predicting exactly that rate — the quantity that
must stay visible. 0.2.7's sweep is for finding those pairs.

## Verification (§4)

```
$ python3 plugins/devteam/scripts/check_plugin.py
devteam plugin: clean  [15 skills, 9 agents, 48 rules]

$ python3 plugins/devteam/scripts/run_controls.py
all 13 controls green

$ python3 plugins/devteam/scripts/check_refs.py .internal/scratch
.internal/scratch/devteam: 18 finding(s)
  cited-undefined          13
  undispositioned-finding   5

$ python3 plugins/devteam/scripts/check_trace.py .internal/scratch
.internal/scratch/devteam: 7 finding(s)  [7 goals, 13 requirements, 11 tasks]
  amendment-omits-condition  1     open-finding-at-close  5     template-drift  1
```

**Every finding on the fixture is real and none is fixed** (§6): the corpus is
cycle 1's signed text, and its backfill is cycle 2's first amendment (0.2.9).

**Coverage is asserted rather than counted by hand**, because a count written by
hand is the thing this subcycle exists to stop. `unruled-finding` and
`stale-row` hold `docs/CHECKS.md` equal to the code in both directions, and
`check_plugin` is clean:

```
check_trace 20 · check_refs 8 · check_report 13 · check_scope 7 · check_plugin 13
sandbox.py promote 14 · guard.py 8 families, named here only
```

Control growth: `check_refs` 55 → 63, `check_trace` 60 → 72, `check_plugin`
18 → 28. **Nineteen mutations** across the four mechanisms, each asserted
applied before its run and restored in a `finally`. Fifteen were caught by the
first draft; **four survived and each produced a missing case**, never a change
to the mechanism.

## What this subcycle did not do

- **The guard's refusal families are named in `docs/CHECKS.md` only, not in
  `guard.py`.** Naming them in the code changes the return contract of the file
  with the widest blast radius in the plugin — the hook that runs on every tool
  call, whose failure to import breaks every call — and both 0.2.4 and 0.2.5
  recorded their sharpest defects on that file's refusal path. **The entry
  condition for doing it:** a subcycle that is already touching `judge()`, with
  the 111-case control green before and after, and a mutation showing each named
  category fails its own case and no other.
- **Every class now names a rule; not every class has a control case.** P-35's
  guarantee is per-**script** — `uncontrolled-check` asks whether
  `check_refs.py` has a control beside it, and it does — so a finding class can
  be born and live with no case that ever failed for it. Two did:
  `unreadable` and `not-utf8`, for a full cycle. The companion check
  (`uncontrolled-class`: every class in `docs/CHECKS.md` against the class names
  appearing in its script's control) is the natural next move and is **not**
  written here, because it needs a control-file grammar that does not exist yet.
- **The fixture's charter is not backfilled** and its sixteen undispositioned
  audit findings are not dispositioned. Both are cycle 2's work (0.2.9), and
  §6 forbids fixing the fixture to satisfy a new check.
- **The four audit files are not migrated to the heading form.** Two of the four
  use the older bold form and their findings therefore read as undeclared. The
  migration is one mechanical substitution per file and is recorded in the audit
  skill; it is not applied because the corpus is read-only to this session.

### The most-repeated defect in this cycle is a house rule, not a checked one

Assembled with `claude-skills-devTeam_s5` at the close, from both sessions'
records. **Five instances across three subcycles, all the same shape:**

| | Instance |
|---|---|
| 0.2.4 | a control asserting a branch that could never fire — an absent tool was reported as a row, not as a gap |
| 0.2.5 | rotation cases that built the mid-rotation precondition the live run destroys |
| 0.2.5 | a false-positive twin made vacuous because `lock_state` returned `mine` before the parser under test ever ran |
| 0.2.6 | the amendment ordering twin — **both** entries complete, so position and version selected indistinguishable things |
| 0.2.6 | the section-gate twin — the fixture charter contains no `###` line, so "gated on the section" and "scan the whole file" produced identical output |

**The house rules already require writing the false-positive twin and
mutation-testing the mechanism. Neither requires the twin itself to be shown to
fail** — and a twin built from the mechanism's *intended* shape usually cannot
be made to fail, because the intended shape is what the correct and the broken
version both produce. So the discipline mutation-tests the check and takes the
twin on trust, and **the twin is the half that silently controls nothing.**

Every one of the five passed. None was visible by asking whether the suite was
green. Four of the five were found only by mutating the mechanism and asking
*what else would make this pass*; the fifth needed a live run.

**Stated as the rule it wants to be:** *a false-positive twin earns its place
only when some mutation flips its verdict. A twin no mutation can move is
decoration.*

**Not written as a rule here, deliberately, and not given a number.** It is a
claim about controls rather than about a project's artifacts, so its two
declared sides are a mutation set and a verdict set — neither of which any
current instrument records. Writing it as a numbered rule without the
instrument would put a rule in `PROTOCOL.md` that nothing checks, which is the
exact half of L-6.1 this subcycle spent its length closing.

**And numbering it here would have been the second error.** `FORMATS.md` says
it plainly: *proposing something by number is not the same as citing it* — the
number is how a project records that it agreed to something, and the manager
allocates it on accepting. An earlier draft of this paragraph gave the rule the
next free protocol number, and `check_plugin`'s `unknown-rule` refused the
commit. **The check caught its own subcycle's author proposing by number**,
which is the third time in this file that an instrument fired against the
person writing it.

**Then it refused the correction too, and that is the finding.** The rewrite
said *"an earlier draft named the rule ⟨that number⟩"* — a description of the
mistake, quoting the identifier — and `unknown-rule` reported it again.
**Reporting the finding created the finding.**

`check_refs` already solved this, at length and with the reasoning in its
source: fenced blocks are skipped, `TEACHING` exempts the `` `X-<n>` `` teaching
form, and `CHECK_OUTPUT` exempts quoted check output because *"a supervisor
reporting a finding wrote `cited-undefined tasks/T-8.md:67 S-4` inline, and the
scanner read the quoted `S-4` as a citation against the quoting file"*.
**`check_plugin`'s `unknown-rule` has none of the three.** `MEASURED` — it reads
each file's raw body and matches every `P-<n>` in it:

```
for n in {int(x) for x in RULE.findall(body)}:
    if n not in declared:
        add("unknown-rule", rel(path), …)
```

So the two checks disagree about whether a quoted identifier is a citation, and
the one that has never learned the lesson is the one guarding the plugin's own
documents — the files most likely to *discuss* rules that do not exist yet.

**Not fixed here.** The fix is to give `unknown-rule` the fence and teaching
exemptions `check_refs` carries, with the false-positive twins that go with
them, and this subcycle is closing with its four items done. The workaround
used in this file — describe the number instead of writing it — is exactly the
obfuscation `FORMATS.md` tells authors **not** to reach for, which is the
strongest argument that the check rather than the author is wrong. **Entry
condition for 0.2.7:** the same sweep that gives every class a control can give
`unknown-rule` the three exemptions and a case for each. **The entry condition for 0.2.7:** a mutation harness that
records, per case, which mutations flip it — at which point "no mutation moves
this case" is a diff of two declared lists and the rule can be written with the
check that enforces it.

**And the reason it is recorded here rather than left in two sessions'
transcripts:** three sessions in this cycle have now exited with knowledge that
survived only because it was written into a subcycle file. This one is measured
five times, is the most-repeated defect in the cycle, and would otherwise have
to be rediscovered a sixth.

## Cost

**Read from the owner's `/usage`, both halves, on 2026-09-07. Nothing here is
derived.**

```
$ python3 plugins/devteam/scripts/session_cost.py          # MEASURED, mid-session
requests 156 · output 163,520 · cache read 29,888,432 · cache write 318,943
total processed 30,371,207 · cache hit share 98.9% of input

/usage, at close:
  claude-opus-5   344 input · 181.0k output · 34.9m cache read · 342.0k cache write
  $25.39 · 172 requests · 99% of input from cache · 1 miss
  API duration 44m 56s · wall duration 13h 30m 31s
```

| | Open | Close |
|---|---|---|
| Gauge, all models | `92%` | **`92%`** — ~0 points, 0–1 by tick |
| Gauge, Fable | `98%` | `98%` — **a different family; never applied to this work** |
| Gauge, Opus | — | **there is no Opus meter** |
| Boost | active | active, ends 13 Sep |
| Window | resets 9 Sep 7:59pm | unchanged, **no reset** (owner, asked directly) |
| Cost | — | `$25.39` @ 172 requests |

### The gauge did not move, for the second time in this cycle

0.2.5 closed at `92%`. 0.2.6 closed at `92%`, in the same window, with no reset
and the boost active throughout — while spending `$25.39` over `35.42 M`
tokens. 0.2.4 did the same thing for `$41.81`. **The dollar column now carries
`$25.39` and `$41.81` against an identical zero movement**, which is the
strongest form yet of the conclusion 0.2.4 reached: the two columns are not two
views of one quantity, and no conversion factor exists.

### There is no Opus meter, and this session planned against one that does not exist

At the start of this subcycle the owner was asked whether to run 0.2.6 in full,
and the question named a gate: *"the per-model meter is a sub-cap, not a second
budget… I have no reading for Opus's own meter. If it's near its cap, 0.2.6
stops regardless of the 8%."* **`MEASURED` at close: the output carries
`Current week (all models)` and `Current week (Fable)`, and no Opus row.** The
gate did not exist.

The rule §8 already carries — *read both meters* — stands, and gains a second
half: **do not assume a family has a meter because another family does.** Fable
at `98%` was never a constraint on Opus work and was treated as a possible one
for the whole of this subcycle's planning.

### The two halves of `/usage` cover different windows

The meters are `Current week`, resetting 9 September. The attribution beneath
them is explicitly **`Last 24h`**. §8 has been quoting a session-shape
percentage beside a weekly gauge reading since 0.2.5 without noting it —
**comparing a day against a week.** The output also says, of the attribution
lines, *"these are independent characteristics of your usage, not a
breakdown"*: they do not partition anything and must never be summed.

### The instrument itself changed between two readings on the same day

| Read by | Line |
|---|---|
| 0.2.5's session | *"52% of your usage came from sessions active for 8+ hours"* |
| this session | *"86% of your usage was at >150k context"* |

Both on 2026-09-07, hours apart. **These are different characteristics, not the
same number moving**, and no cause is offered here because none was measured —
recorded as observed so the next reader does not treat `86%` as `52%` having
risen.

**The second framing is the better instrument for 0.2.5's argument**, and it is
worth saying so. 0.2.5 had to hedge — *"a long session is not wasteful merely
for being long; it may be doing proportionally more work"* — because duration
is a proxy. **Context size is not a proxy; it is the thing that multiplies the
per-request cost.** `86% of usage at >150k context` supports manager rotation
directly, without the hedge.

### This session is an instance of what rotation exists to prevent

Stated plainly because the alternative is quietly not mentioning it: 0.2.6 ran
as **one session from start to finish**, never rotated, and closed deep into
the `>150k context` band that accounts for 86% of the last day's usage. It is
also why §5's third box — the two rotation numbers — is still open: no rotation
happened here, so the instrument still has nowhere to land. **The subcycle that
built the checks did not use the mechanism the previous subcycle built.**

### The per-skill breakdown cannot see work ON the plugin, only work THROUGH it

```
Skills                  % of usage        Plugins       % of usage
/devteam:run                    1%        devteam               2%
/devteam:checkpoint             1%
```

**Identical to 0.2.5's reading, and 0.2.6 invoked no devteam skill at all.**
This subcycle wrote `docs/CHECKS.md`, three protocol rules, five checks and 43
control cases — and registers as `0%` against every devteam row, because the
attribution counts **skill invocations**. Those two `1%` rows are still 0.2.5's
two live-proof sessions.

So a subcycle that *builds* the pipeline is invisible to the breakdown while
being most of what the window was spent on. **The per-skill figures answer
"which skills did you run", never "what was this account doing"**, and reading
them as the second question is the trap. The attribution also says it is
*"based on local sessions on this machine — does not include other devices or
claude.ai"*, and the owner worked part of this day from the mobile app, so it
under-counts for that reason as well.

### And the 21% subagent line is not this subcycle's

`21% of your usage came from subagent-heavy sessions`, against `17%` at 0.2.5's
close. **This session spawned no subagents.** The figure is machine-wide across
all local sessions — several `nitpick` sessions were live throughout — so
attributing the rise to devteam work would be exactly the error the per-skill
rows above invite.
