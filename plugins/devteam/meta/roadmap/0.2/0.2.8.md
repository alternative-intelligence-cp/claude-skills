# 0.2.8 — release 0.2.0: the plugin checks itself, the record is rewritten, a fresh setup proves the whole — PLANNED

> A release here is not a tag. It is the moment the plugin's own account of
> itself is diffed against what it contains, the queue is rewritten to say
> what this cycle took and what it left, and a project the pipeline did not
> write is scaffolded from nothing and run one task through — because the
> artifact being verified and the deployment being reported are two
> different things (DESIGN §15, the guard that was believed live for a
> whole rehearsal).

## 1. Where you are

Every core subcycle this cycle shipped is `DONE` and in `../done/`; the
deferred ones (0.2.6, 0.2.7 if skipped) are `STOPPED` with a reason in their
title line. `run_controls.py` is green and `check_plugin.py` clean at the
last subcycle's close.

Read: `plugins/devteam/.claude-plugin/plugin.json`; `.claude-plugin/` at the
repository root (the marketplace manifest); `HANDOFF.md`;
`docs/CONSOLIDATION.md`; DESIGN §14; the root `README.md`; `.gitignore`'s
"NOT ignored, deliberately" block (the new script and its control must
ship — a control that is not shipped cannot be run by whoever doubts the
check).

## 2. Decisions

L-9, L-10 apply. Local:

- **L-8.1 — the version is `0.2.0` in `plugin.json` and the marketplace
  manifest, and nowhere else.** One home.
- **L-8.2 — the handoff is rewritten, not appended.** *Why:* `HANDOFF.md`
  is for a reader with none of the conversation; a file that carries two
  cycles' states is a file that makes that reader reconcile them. The 0.1
  handoff's content that is still true moves to DESIGN or CONSOLIDATION;
  the rest is history in git.

## 3. The work

### 3.1 The plugin's account of itself

- `plugin.json`: version `0.2.0`; description gains "structurally contained
  workers" in one clause.
- `README.md` (plugin): the roster and the checks tables reflect
  `sandbox.py`; the install section names the Linux-with-user-namespaces
  requirement and what happens without it.
- `DESIGN.md` §14: phase 7 (containment, 0.2.0–0.2.4), phase 8 (rotation),
  phase 9 (the checks and sweeps, if shipped), each with its done-when and
  the measured control count; phase 6 ("in flight") closed with a pointer
  to C-3.
- `DESIGN.md` §2's roster table: the three worker roles' `Model` column is
  unchanged, but a column or note says which roles run headless.
- `docs/CONSOLIDATION.md`: rewritten whole — the header names the cycle;
  each 0.1 item either *taken (0.2.n)* with one line on what landed, or
  *remains* with its trigger unchanged; then the new queue: what 0.2's
  subcycles recorded under `## Findings` as deferred, each with the reason.
  Nothing on this list is a bug report.
- `HANDOFF.md`: rewritten for whoever picks up after 0.2 — the state, where
  things are, what is not in any file (the credential-refresh risk from
  0.2.3 §3.2 and what was observed; the `guard-only` platforms; the network
  risk accepted), and where to start. Every number read from the tree, with
  its command, as the 0.1 handoff did.

### 3.2 The self-check and the controls

```
python3 plugins/devteam/scripts/check_plugin.py     → clean
python3 plugins/devteam/scripts/run_controls.py     → all N controls green
```

Record N and the case count beside it (`run_controls.py` prints per-control
last lines; sum the case counts and paste) in DESIGN §14's phase 7 row.
`MEASURED` at `243059e`: 328 cases; the new figure should be well above it,
and the false-positive share of `test_sandbox.py` above a third.

### 3.3 The end-to-end proof on a project the pipeline did not write

In the scratchpad, never inside this repository: `git init` a throwaway
project with one source file and one test; run `/devteam:setup`, then an
`onboard` of two questions, a one-task `plan`, and `run width=1`. Assert,
and paste under `## Findings`:

- `setup` wrote `Containment: structural` from the probe, proved the guard
  with a literal-path refusal, and proved the sandbox with an `exec` that
  read `No such file or directory`
- the task dispatched through `sandbox.py dispatch`, its `.sandbox` file
  was written and rewritten, its commits were promoted with rewritten hashes
  and intact subjects, a fresh verifier passed against the promoted tree,
  the board closed the task
- `check_report` on the task is clean including `budget-mismatch` /
  `model-mismatch`
- the host tree's `sha256sum` over every file **outside** the task's scope
  is identical before and after the run (the property the whole cycle
  bought, measured once end to end)

If 0.2.5 shipped: run to a checkpoint, rotate into a second session, paste
the handoff lines and the refusal from the old session.

### 3.4 Tag

`git tag -a v0.2.0 -m "devteam 0.2.0: structural containment"` on the
commit that closes this subcycle. **Do not push.** Publishing is the owner's
(P-26: outward-facing is irreversible; the pipeline's own rule applies to
its own release).

## 4. Verification

The three commands above green; the end-to-end proof pasted; `git status
--porcelain` empty; `git log --oneline -1` names the release commit and
`git tag --points-at HEAD` prints `v0.2.0`.

## 5. Done when

- [ ] version, README, DESIGN §14, CONSOLIDATION, HANDOFF as §3.1
- [ ] self-check and controls green with the numbers recorded
- [ ] the end-to-end proof pasted, including the host-tree hash equality
- [ ] tagged, not pushed
- [ ] this cycle's `../README.md` §8 table has the measured cost of every subcycle that ran, and the total, so 0.3's model starts from data

## 6. Do not

- do not push; do not publish the marketplace entry; do not touch `.internal/scratch/`
- do not close a subcycle that is `STOPPED` as if it were `DONE` in DESIGN §14 — the phase row says `deferred`, with the trigger
- do not write a release note that claims a property no control proves — every sentence in `HANDOFF.md` about containment cites `test_sandbox.py` or the pasted proof
