# 0.2.9 — the second run: cycle 2 on the fixture, with the mechanisms that have never fired — PLANNED

> This is what the cycle is for. Three things in the pipeline have never
> produced output: `/devteam:iterate`, the unreviewed-decision path (P-27),
> and now everything 0.2 built. A mechanism that has never fired has not
> been shown to work, and the fixture's record already contains a second
> cycle's plan — nine open audit findings, four unowned product findings,
> three counted claims — so the target is cheap and the reasoning is on
> disk. This file is the run's protocol: what it exercises, what it
> measures, where its ceiling is, and when to stop.

## 1. Where you are

0.2.8 is `DONE`: plugin 0.2.0 tagged. `.internal/scratch/` is the fixture
at the end of cycle 1 — every task closed, C-3 `DRIFTED` and accepted, the
private remote current (`git -C .internal/scratch status` clean and
`git -C .internal/scratch log origin/main..HEAD` empty; if not, push first —
the handoff says push after any change to that tree).

Read: `.internal/scratch/devteam/checkpoints/C-3-2026-09-05.md` §5–§6
(what is left, and the recommendation that it is a plan already written);
`.internal/scratch/devteam/audits/*.md` (the `Disposition.` lines);
`.internal/scratch/devteam/DECISIONS.md` D-37, D-41, D-42 (the three
declines on one thread — HANDOFF §4 says a successor reversing any should
reverse the reasoning explicitly); `skills/iterate/SKILL.md` in full;
`HANDOFF.md` §4 (the client was an AI session; eighteen blocking stops in
fifteen hours); the owner's memory on experiments competing with the main
budget, if it loads — the run must surface its bill unprompted.

## 2. Decisions — the ones the plan can make, and the ones it cannot

- **L-9.1 — the target is the fixture, through `iterate`.** *Why:* the
  mechanism has never run; the state exists; a "small real target" (HANDOFF
  §4's preference) would spend the interview and the plan again before any
  0.2 mechanism is reached, and the sibling repositories have their own lock
  regime and declined at the door. *Declined:* a new fixture; a sibling
  library. **Reversible by the owner** — recorded here so a different
  choice is made knowingly.
- **L-9.2 — the client channel is `session <name>`, and the client session
  is briefed to answer `REVERSIBLE` questions slowly.** *Why:* P-27 has
  never fired because the client answered everything within the hour;
  a client that is deliberately late on reversible questions — and prompt
  on `IRREVERSIBLE` and `CHARTER` ones — exercises the window without
  risking anything the class says must block. The escalation window is set
  short (the charter row; one hour) so the run does not idle for it.
- **L-9.3 — a budget ceiling is set, in the charter, and it is a stop.**
  *Why:* D-8 set `none` and C-3 §4 records that this is why the 1.78×
  overrun "cost nothing and also why it was never forced into the open".
  The ceiling is the owner's number; the plan recommends the estimate ×
  1.5 (the model now carries `r`), and a `BLOCKED` at the ceiling, not a
  quiet continuation.
- **L-9.4 — width 2, then 3 only if the first two tasks promote cleanly.**
  *Why:* the promotion lock and the base-moved path (0.2.2) are exercised
  at width 2; width 3 adds nothing new and costs a third of the budget.
- **L-9.5 — fixture defects are fixed only where fixing them exercises a
  pipeline mechanism**, and the run says which mechanism each task is for.
  *Why:* HANDOFF §2 — the fixture has no users (D-37) and a defect fixed for
  its own sake burns budget without buying data.

**What is the owner's to decide, and the recommendation for each** (these
go to the owner *before* the run, as one batch, because they are `CHARTER`
class for the experiment itself):

| Decision | Recommendation | Why |
|---|---|---|
| the target | the fixture (L-9.1) | above |
| the tasks | see §3.2 — four tasks, each named for the mechanism it exercises | each buys data; none is a fixture fix for its own sake |
| the ceiling | estimate × 1.5, `BLOCKED` at the ceiling | L-9.3 |
| reversing D-37 / D-41 / D-42 | **do not** — leave the memory bound declined, consistent with the thread; the task that touches memory (below) exercises `promote` on a scope pairing two files, not the bound | HANDOFF §4: reverse the reasoning explicitly or not at all |
| who plays the client | an AI session, briefed per L-9.2, with the owner's positions written down before the interview as 0.1 did | budget; and the pre-written positions are what make the run scoreable |

## 3. The work

### 3.1 Opening cycle 2

`/devteam:iterate` on `.internal/scratch/`, exactly as the skill says: read
the last cycle first; the short interview (three areas); amend, never
rewrite. Expected outputs, and each is a measurement of the skill:

- the charter goes to v18 with an amendment entry that **carries the
  enumeration block from 0.2.6 §3.3** if that shipped (this is the fixture's
  backfill, and the first place `amendment-omits-condition` goes clean on a
  real charter) and the `Containment: structural` row from the probe
- `DM-7`'s amended text from C-3's recommendation stands; the interview does
  not re-ask it
- the nine open audit findings are shown to the client with their `Needs.`,
  and each gets a disposition — routed to one of §3.2's tasks, or declined
  with a decision; `undispositioned-finding` (0.2.6) is then clean
- `RECORD.md` gains `cycle 2 opened`; task numbers continue from T-12
- **every question the skill had to ask that the record could have
  answered is logged as a defect in the skill**, the way `resume` §0 logs
  handoff questions

### 3.2 The tasks — four, each for a mechanism

| Task | What | Exercises |
|---|---|---|
| T-12 | `COR-6` — a blank first line accepted as a zero-column header; the finding the auditor least wanted lost | the ordinary sandboxed path: dispatch, promote, verify, close, at width 2 with T-13 |
| T-13 | the three counted claims (`F-137`, `F-147`, `F-148`): rewrite each so it cannot become false (D-41's standard), across three files in three former tasks' scopes | a scope spanning files three closed tasks owned — the plan must declare it fresh; `promote-out-of-scope` gets its first live chance to fire, and the guard's inside warning its first live reader |
| T-14 | `SEC-12` — unroutable in cycle 1 because no task's scope paired the two files it needs | a two-file scope that C-3 said could not exist; and the rotation, because the checkpoint after T-12/T-13 closes falls before it |
| T-15 | the four unowned product findings from T-7 (docstring/README disagreement, the FIFO block, the phantom command name, the `_one_line` docstring) — a documenter task | the documenter headless; a `REVERSIBLE` question about wording that the slow client leaves to the window — **P-27's first firing** |

Estimates from the v3 model with cycle 1's constants; the client sees them
with the ceiling.

### 3.3 What to measure, and where it is written

At every checkpoint (cadence: every two closed tasks, and the mid-task rule),
under a `## Cycle-2 measurements` heading, each from a command:

| Measurement | Command | What it tells |
|---|---|---|
| host-tree integrity | `sha256sum` over every file outside every live scope, before and after each step's promotion | zero mutations outside promotion — the property bought |
| promotion findings | `grep -c '^promote-' devteam/RECORD.md` by class | whether the gate fires, and on what |
| guard warnings inside | the harness's `meta/stderr.txt` refusal lines, counted | whether the early warning is reached and by which form (heredoc, `Write`, redirect) |
| sandbox overhead | per step: harness wall-clock minus the worker's own; and `budget.json` tokens against the worker's self-report | the cost of containment, and P-17c's measured-versus-remembered gap |
| estimate error | measured ÷ estimate per task, and `r` recomputed | whether the v3 model is less biased than 1.78× |
| rotation | the two numbers from 0.2.5 §3.3 | the cadence question |
| P-27 | rows in the board's "Decided without the client" table; each reviewed at the next checkpoint | the first unreviewed decision the design has ever had to show |
| client stops | count of `IRREVERSIBLE`/`CHARTER` stops and their wall-clock cost | against 0.1's eighteen in fifteen hours |
| the bill | the harness's measured tokens, summed, against the ceiling, **in every checkpoint's first line** | the owner should never discover the number by running short |

### 3.4 Stop rules

The run stops, with `BLOCKED` and a checkpoint, when any of these is true;
the plan says so in advance so that stopping is a decision already made and
not a judgement under sunk cost:

- the ceiling is reached (L-9.3)
- a host mutation outside promotion is measured — the property failed; stop
  and file it, nothing else matters more
- a `promote-conflict` at width 2 with disjoint scopes — P-12's guarantee
  failed somewhere; stop and file it
- the second `BLOCKED` on the same class in one task (P-20c's spirit,
  applied to the experiment: the third attempt belongs to the owner)
- the credential-refresh risk from 0.2.3 §3.2 materialises (a worker
  cannot authenticate mid-run) — stop, record exactly what was observed;
  it is the cycle's most important `REASONED` line becoming measured

### 3.5 Closing

GATE 4 as the checkpoint skill says, with its five rules. Then this file's
`## Findings` gets the measured cost, and `../README.md` §8's last row
gets the number. Push `.internal/scratch/` (the handoff rule). Open 0.3's
planning from what the run's `findings-for-protocol` lines and the
checkpoints' measurements say — not before.

## 4. Verification

The run's own: every task closed on `VERIFY PASS` or `ACCEPTED (D-n)`;
`check_trace`, `check_scope`, `check_refs`, `check_report` clean over the
fixture (the commands and their output pasted in C-4, the way C-3 did); the
host-integrity hashes equal at every step; the bill under the ceiling or a
`BLOCKED` explaining why not.

## 5. Done when

- [ ] the owner's five decisions in §2 are answered and recorded in the fixture's `DECISIONS.md` (they are cycle 2's first decisions)
- [ ] `iterate` ran and its questions-the-record-could-have-answered are logged
- [ ] the four tasks ran to a verdict each; P-27 has at least one row; rotation happened at least once; every measurement in §3.3 has a number at every checkpoint
- [ ] GATE 4 filed; the fixture pushed; the cost in this file's title line and in `../README.md` §8

## 6. Do not

- do not fix a fixture defect that no task in §3.2 names — it buys no data (L-9.5)
- do not reverse D-37, D-41 or D-42 quietly; if the client reverses one, the decision reverses the reasoning by name
- do not run past the ceiling "to finish the task"; the ceiling is the experiment's `IRREVERSIBLE` question and it always blocks
- do not let the client session answer a `REVERSIBLE` question promptly — it is playing a client who is busy, and that is the mechanism under test
- do not start 0.3's plan until the run has stopped and been read whole; CONSOLIDATION exists because that reading is different from the running
