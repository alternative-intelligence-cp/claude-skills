# 0.2.0 — probes: the riskiest unknowns first, and small — PLANNED

> The spec's build order puts the worker-inside-the-sandbox step sixth and
> calls it "the one that matters". This cycle's own rule is that the riskiest
> unknown goes first, as a probe that is allowed to fail and change the design.
> So the headless-worker question is asked here, before any harness exists,
> along with four other facts the whole cycle rests on. **Nothing in this
> subcycle is product**; its output is a script, a set of measured lines in
> this file, and a decision.

## 1. Where you are

`plugins/devteam/` is cycle 0.1 as shipped: 15 skills, 9 agents, 9 scripts
with controls (`python3 plugins/devteam/scripts/run_controls.py` → `all N
controls green`; `MEASURED` at `243059e`: 328 cases). No sandbox code exists.
`.internal/scratch/` holds the fixture and its `devteam/` record — read it,
never write it.

Read before starting: `../README.md` §4 (what was measured), the spec §2, §3,
§5, §6, and `plugins/devteam/skills/setup/SKILL.md` §3 (how the guard is
proved to fire — the same discipline applies to proving a sandbox contains).

Scratch space for this subcycle: the session's scratchpad directory, never
`/tmp` directly and never inside the repository.

## 2. Decisions (settled — see `../README.md` §6)

L-1, L-2, L-3, L-4 apply. Local:

- **L-0.1 — a probe records its result in this file before the next probe
  starts.** *Why:* the probes are cheap and the lesson of P-35b is that an
  instrument built in a hurry answers a question it was never wired to ask;
  writing the observed output down, next to the expected output, is what
  makes a wrong instrument visible. Each probe below has an `Observed.` line
  to fill.
- **L-0.2 — every probe carries one case whose answer is already known**
  (P-35b): one known-contained write and one known-allowed write, before any
  interesting case.

## 3. The work

### 3.1 The baseline, as a script

Write `plugins/devteam/scripts/sandbox_probe.py`. It prints spec §2's table
and exits `0` if this machine can run the two-stage composition, `1` if not,
`2` if it could not determine. It becomes `setup`'s detector in 0.2.4, so
it is a shipped check and needs a control (`test_sandbox_probe.py`) that
plants each failure by monkeypatching the command runner — bwrap missing,
bwrap present but userns clone disabled, `unshare` failing — and demands the
matching line.

Rows, each with the command that produced it:

| Row | Command | Expected here (`MEASURED` 2026-09-05) |
|---|---|---|
| kernel | `uname -r` | `7.0.0-30-generic` |
| bwrap | `command -v bwrap; bwrap --version` | `/usr/bin/bwrap`, `bubblewrap 0.9.0` |
| bwrap has `--overlay` | `bwrap --help 2>&1 \| grep -c -- --overlay` | `0` — if non-zero, **stop**: spec §2 says native overlay collapses the two-stage design into one |
| fuse-overlayfs | `command -v fuse-overlayfs` | absent |
| unshare | `command -v unshare` | `/usr/bin/unshare` |
| userns clone | `sysctl -n kernel.unprivileged_userns_clone` | `1` |
| apparmor restriction | `cat /proc/sys/kernel/apparmor_restrict_unprivileged_userns` | `0` |
| userns works | `unshare --user --map-root-user true; echo $?` | `0` |
| overlay in userns works | 3.2 below, scripted | see 3.2 |
| **already inside a sandbox?** | `cat /proc/self/uid_map` | `0 0 4294967295` on the host. Anything else means this process is already in a user namespace — the harness's own sandbox (`../README.md` §4.2) or a container — and nesting applies (`enableWeakerNestedSandbox` is the harness's own name for that case). Print it and exit `2`: nested is a different design, not a degraded one |
| headless CLI | `command -v claude; claude --version` | `~/.local/bin/claude`, `2.1.261 (Claude Code)` |
| disk under the sandbox root | `df -h "${DEVTEAM_SANDBOX_ROOT:-/tmp}"` | `5.5T` free — the spec's 30.9 GiB compile is not a constraint here |

`Observed.` <fill in when run>

### 3.2 Overlay in a user namespace (spec S-2)

A script, not typed by hand, so it can be re-run: create `lower/` with
`tracked.txt` = `v1` and `gone.txt`; `upper/`, `work/`, `merged/` empty; then

```bash
unshare --user --map-root-user --mount sh -c '
  mount -t overlay overlay -o lowerdir=lower,upperdir=upper,workdir=work merged &&
  printf v2 > merged/tracked.txt && printf new > merged/added.txt && rm merged/gone.txt &&
  ls -la upper'
```

Expected: exit 0; host `lower/tracked.txt` still `v1` and `lower/gone.txt`
still present (byte-identical: `sha256sum` before and after); `upper/`
holds `tracked.txt` (`v2`), `added.txt`, and `gone.txt` as a character device
`c--------- … 0, 0` — the whiteout, spec §6.1. **Never walk `upper/` to
compute a diff**; this probe exists so you have seen why.

`Observed.` <fill in>

### 3.3 The headless worker inside the composition (spec S-6, brought forward)

The probe that matters. Compose spec §5 by hand for one throwaway git
repository (`git init`, one commit), and run a real headless `claude -p`
inside it with a one-line task. Bind the merged view **at the same absolute
path as the throwaway repo on the host** (L-3). Bind the plugin directory
read-only at its host path. `HOME` is a tmpfs.

Three variants, run in this order, each with the cases below:

| Variant | Credentials | Expected |
|---|---|---|
| A | `~/.claude/.credentials.json` copied into the tmpfs `HOME/.claude/` (a *copy*, so a token refresh inside cannot touch the host's file) | the CLI authenticates and runs |
| B | an environment variable carrying an OAuth or API token, name taken from the CLI's own documentation (`claude --help`, the docs) — `REASONED`: the variable's name is not known to this plan | same |
| C | nothing | **must fail** to authenticate — this is the known-deny case (L-0.2) |

Run inside, with `--output-format json --plugin-dir <plugin> --model <cheapest>`
and a prompt of the form *"Invoke the devteam:work skill's inputs section,
then create the file `<repo>/hello.txt` containing `hello` using the Write
tool, then run `cat /proc/self/uid_map` with Bash and report both outputs."*

Record, for the variant that works:

1. `hello.txt` appears in `upper/`, **not** on the host (`ls` both).
2. the JSON result's `result` field carries the two outputs; the `uid_map`
   line is not the host's identity map (the Bash tool ran inside).
3. the JSON's usage / cost fields — note their exact names; 0.2.3 reads them.
4. what the CLI wrote under the tmpfs `HOME` (`ls -R "$HOME/.claude"` as a
   trailing command in the same `bwrap` invocation): session files, caches,
   a rewritten credentials file. This is the list of things a worker's
   `HOME` must be allowed to hold.
5. wall-clock and whether the `work` skill was actually loaded (the result
   should quote its inputs section; if it does not, `--plugin-dir` did not
   take effect headless — record that, it changes 0.2.3).
6. whether the CLI attempted to sandbox its own Bash (the harness's built-in
   sandbox, `../README.md` §4.2): pass `--settings '{"sandbox":{"enabled":false}}'`
   on one run and omit it on another, and compare the `uid_map` output and
   any `[Sandbox Linux]` lines. Nested `bwrap` under our `bwrap` is the case
   to know about, not to prevent.

`Observed.` <fill in, per variant>

### 3.4 Background lifetime (`../README.md` §4.5)

From a session with the `Bash` tool: run the 3.3 composition with a command
of `sleep 720; echo survived` using `run_in_background`, and separately with
`--die-with-parent` removed. Expected, `REASONED`: the backgrounded process
survives the ten-minute foreground limit and the session is re-invoked when
it exits with `survived` in its output. If the `--die-with-parent` variant
survives and the other does not, the background shell is not a stable
parent, and 0.2.3 §3.4 changes to a detached process with its own reaper.

`Observed.` <fill in>

### 3.5 `SendMessage` to a session whose turn has ended

Needed by 0.2.5 (rotation). From a second session on this machine, send a
message to this one after this one has ended its turn. Expected, `REASONED`:
`ListAgents` lists it as a local session and the message wakes it. If it
does not, rotation's live exchange (`resume` §0) only works while the outgoing
session is mid-turn, and 0.2.5 §3.2 must say so.

`Observed.` <fill in>

### 3.6 The hook can rewrite tool input (the fallback's precondition)

Only if 3.3 failed. Register a throwaway `PreToolUse` hook on `Bash` that
returns `hookSpecificOutput.updatedInput` with the command replaced by
`echo rewritten`, run `echo original`, and record which word the tool
printed. `MEASURED` only that the binary contains the field name; whether
the hook can change a `Bash` command is `REASONED`.

`Observed.` <fill in>

## 4. The decision rule

Write the outcome under `## Findings` at the foot of this file, then:

- **3.1 exits 0, 3.2 as expected, 3.3 works in variant A or B, 3.4 survives**
  → proceed to 0.2.1 as written.
- **3.3 fails for a reason one session can fix** (a missing bind, an
  environment variable) → fix, re-run, record, proceed.
- **3.3 fails structurally** (the CLI cannot authenticate from a cleared
  environment at all; the plugin cannot load headless) → switch the cycle to
  the fallback branch below and supersede L-2 in `../README.md` §6 with the
  reason measured.
- **3.4 fails** → 0.2.3 §3.4 uses a detached process with a pidfile and its
  own reaper; nothing else changes.

## 5. Done when

- [ ] `sandbox_probe.py` and its control exist; `python3 plugins/devteam/scripts/test_sandbox_probe.py` → its last line names the case count and `green`
- [ ] `check_plugin.py` is clean (it will report `uncontrolled-check` until the control exists — that is the mechanism working)
- [ ] every `Observed.` line above is filled from output, not from memory
- [ ] the decision under §4 is written and names the next subcycle

## 6. The fallback branch, specified so it is not designed under pressure

If L-2 is superseded: the worker stays an `Agent`-tool subagent; the guard's
`PreToolUse` on `Bash` returns `updatedInput` wrapping the command in
`bwrap --ro-bind / / --bind <each live scope path> <same path> --tmpfs /tmp
--new-session --die-with-parent --unshare-pid -- bash -c <cmd>` for any
session that is the board's writer or a worker of it; `Write`/`Edit` stay
judged by the guard as today. This closes F-80 and F-121 (the heredoc writes
to a read-only bind and gets `EROFS`), leaves the shared-index class open
(P-12b stands), and needs no credentials, no headless CLI and no promotion
gate. Its controls are the spec §7b table run through the hook. Subcycles
0.2.2 and 0.2.3 are then skipped and 0.2.4 wires this instead; 0.2.5–0.2.9
are unchanged.

## Findings

<recorded as they arrive; one per line, `- **what** — evidence — what it changes`>
