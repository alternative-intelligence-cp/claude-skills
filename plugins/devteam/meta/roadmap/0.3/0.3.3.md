# 0.3.3 — the disposition ledger: every item raised has one home, an id, and a decision or a date — PLANNED

> **Cost, `REASONED`.** About `$120` to `$210` of Opus 5.5 list price across
> six to nine sessions. At the cycle README's §8 rate that is about 4.5 to 9.7
> points of the all-model gauge, *converted, not read* (L-18). The basis is
> 0.3.2, which spent `$138.25` over 1,078 requests and seven sessions against
> an estimate of `$90` to `$160`, and 0.3.1, which spent `$101.85` against
> `$50` to `$90` ([0.3.2's measured cost block](../done/0.3.2.md)). This
> subcycle adds an artifact, a module and two commands, changes the REPORT,
> audit, board and question grammars, moves two classes onto the ledger and
> adds six, rewrites the audit skill's output, and replays the corpus with planted
> ledgers. So it is sized at 0.3.2's measured cost and above it, not at 0.3.2's
> estimate. Nothing runs on Fable (L-17). **Record the closing cost as L-18
> says**: the owner's opening and closing readings, split by his arithmetic
> (non-Fable = all-model − ½ Fable), with this subcycle's own list-price dollars
> from the machine meter beside them, converted at `$21.70` to `$26.90` per
> point and labelled *converted*. Two closes have now found the band's low end
> too low for their spans: their readings met the meter only at `$22.25` (0.3.1)
> and `$22.68` (0.3.2) a point or more.
>
> **The opening reading is 0.3.2's closing reading:** 22:31:29 EDT on
> 2026-09-24, all-model 42 %, Fable 58 % (0.3.2's cost block). This subcycle's
> own spend is `claude-skills-devTeam_s41`'s requests after that reading, and
> every later session's. `claude-skills-devTeam_s40`'s requests after it are
> 0.3.2's close, not this subcycle's. The meter is
> `python3 .internal/scratch/0.3.0/meter/machine_meter.py --from "2026-09-24 22:31:29"`.
>
> **Spend at planning**, by that meter from the opening reading to 23:52 EDT:
> `claude-skills-devTeam_s41`, 133 requests, `$18.21` at list price, which is
> 0.7 to 0.8 points of the all-model gauge, *converted, not read* (L-18). In
> the same window `_s40` made 16 requests (`$2.57`), which are 0.3.2's close.

> **What this subcycle is for.** The run's first theme: items raised by
> adversaries, workers, auditors, outgoing managers and the client reached no
> owner, to the end (the [register](../../v3-findings-register-2026-09-19.md)'s
> theme 1). An auditor's open item needing a client decision was read past by a
> supervisor and a manager (F-139). An audit finding sat four days with no owner
> (F-99). A verbatim audit landing and the declaration grammar could not both be
> followed (F-104, F-112). A question's status lived in two files that disagreed
> (F-63, F-64). And what the client said after the outgoing manager's last write
> reached no file, at any of fifteen rotations (F-21). The checks say nothing
> about most of it. `undispositioned-finding` cannot see the four gate audits,
> and `open-finding-at-close` cannot tie T-18's step audit to its task. Both
> read any disposition whose first word is not `open` as decided, so
> `check_refs` passes T-18's three findings *"carried to the checkpoint … to be
> given an owner there"*. This subcycle gives every item one home,
> `LEDGER.md`, with an id and a decision or a date by which one is due. It
> makes a landing that leaves an item without one refusable, puts a question's
> state in one file, and reads the client's words from where they already are.

## 1. Where you are

- **The tree.** `claude-skills` at or after the commit that adds this file.
  Plugin `0.2.0-rc`. CLI `2.1.282`, which moved from `2.1.281` since 0.3.2 was
  planned. Both measured 2026-09-24 at `0003e3d`, 0.3.2's close:
  - `python3 plugins/devteam/scripts/check_plugin.py` →
    `devteam plugin: clean  [15 skills, 9 agents, 64 rules, 6 root entries]`.
  - `python3 plugins/devteam/scripts/run_controls.py` → `all 20 controls
    green`, in 2 min 14 s. The controls this subcycle changes hold 193 cases
    (`check_trace`), 106 (`check_refs`), 101 (`check_report`), 77
    (`check_plugin`), 70 (`gate`), 75 (`result`) and 18 (`setup`).

  Re-run both before you start, and record what they print.
- **The live corpus** is `pricelog` at `a88990b`. 0.3.1's clone,
  `.internal/scratch/0.3.1/pricelog`, is at that commit and untouched. Read
  it, and copy it for any case that writes. Never read or run anything in
  `pricelog` itself. On the clone at `0003e3d` (`MEASURED` 2026-09-24; the
  outputs are `.internal/scratch/0.3.3/corpus-before-*.out`):

  | Check | Exit | Prints |
  |---|---|---|
  | `check_trace` | 1 | `5 finding(s), 11 part(s) not evaluated  [3 goals, 16 requirements, 19 tasks]`. The findings are T-16's and T-19's `bad-board-state` (0.3.6's `STOPPED`), F-85's pair (T-1's `missing-field`, T-10's `one-sided-link`) and T-17's `unmotivated-task`. The parts are ten identifier fields holding the corpus's own prose, and the T-18 step audit's name, which is this subcycle's |
  | `check_refs` | 3 | `not evaluated (4 part(s))  [47 files, 415 declared, 321 cited]`: the four gate audits' finding headings (F-99), this subcycle's |
  | `check_scope` | 0 | `scopes: clean  [19 tasks, 0 live]` |
  | `check_report`, T-1 to T-19 | 3, and 1 for T-12 | each `not evaluated (2 part(s))`, the harness parts a clone cannot supply (0.3.7's). T-10 and T-18 name a third, *the REPORT blocks*: T-10's `REPORT auditor T-10.S-3 (…`, this subcycle's (F-104), and T-18's `S-4-pre`, 0.3.7's (F-111). T-12 reports its `status-mismatch`, a corpus fact |

- **A fresh project**, `setup.py` run into an empty repository and the
  scaffold committed as its client would commit it: eight artifacts;
  `check_trace` clean with `--pre-plan` and without, `check_refs` clean
  `[11 files, 0 declared, 0 cited]`, `check_scope` clean `[0 tasks, 0 live]`
  (`MEASURED` 2026-09-24, in `.internal/scratch/0.3.3/fresh`).
- **The commit guard** (0.3.1 §3.6) runs in every session started after it
  entered `hooks/hooks.json`. It refuses a commit in a devteam project made any
  way but `gate.py commit`. The clone and every fixture with a `devteam/` are
  devteam projects. Make fixture commits from Python, as `test_gate.py` does,
  because the hook reads Bash commands only; or make them through `gate.py`. To
  learn whether it runs in your session, use the `setup` skill's probe,
  `git -C <fixture> commit -m probe -- devteam/no-such-file`: a refusal means
  it runs, and git's own pathspec error means it does not. `claude-skills` is
  not a devteam project, so commits here are unaffected.
- **Read first**, in this order:
  - the cycle README's §2, the 0.3.3 row of §5 whole, §6 (L-5, L-6, L-9 and
    L-18) and §9;
  - [0.3.2](../done/0.3.2.md) §2 and its Findings' *Found at the joints* under
    §3.2 (the disposition vocabulary), §3.5 (T-10's landing) and §3.7, and its
    *On the corpus at HEAD*. Its L-2.3 (every field read whole), L-2.6
    (HEAD's history), L-2.7 (the latest block, and a previous claim's) and
    L-2.14 (changed identities) bind here;
  - [0.3.1](../done/0.3.1.md)'s L-1.1 to L-1.9 — the four results, parts not
    evaluated, the gate and its one allowance, and acceptances — which bind
    here too;
  - from the [register](../../v3-findings-register-2026-09-19.md): theme 1
    whole, and its rows for F-21 (A2, A10), F-63 and F-64 (A10), F-99, F-104
    and F-139 (A5), and F-112 and entry 5 (A6);
  - the `pricelog` entries each decision below names, read from the clone:
    RECORD.md:239–242 (F-21), 503–505 and 527–528 (the client's words),
    826–827 (F-63), 841–844 (F-64), 1157–1158 (F-99), 1171 (F-104), 1307 and
    1315 (F-112), 1987 (F-139); `tasks/T-19.md`:1690–1889 (S-3's audit and its
    open items); `tasks/T-10.md`:423–620; `tasks/T-18.md`'s S-4 landing and
    its closing report at 1487; and `audits/pricelog-T-18-S-4-2026-09-17.md`;
  - [`FORMATS.md`](../../../templates/FORMATS.md) §"The namespace, and how not
    to collide with it", §"Status vocabularies", §"The REPORT block", §"What
    each check reads" and §"Accepted findings"; [`CHECKS.md`](../../../docs/CHECKS.md);
    [`DESIGN.md`](../../../DESIGN.md) §6's REPORT block; PROTOCOL P-13, P-17,
    P-23, P-24, P-26, P-27, P-29, P-31, P-34, P-50 and P-51;
  - the [`audit` skill](../../../skills/audit/SKILL.md) whole; the `supervise`
    skill's dispatch table and landing; the `run` skill's §6, §7b and §8; the
    `resume` skill's §0; the `status` and `checkpoint` skills;
  - `check_refs.py` (`DECLARATIONS`, `AUDIT`, `KNOWN`, the disposition scan
    and `gaps_from`), `check_trace.py` (`open-finding-at-close`),
    `check_report.py` (`HEADER`, `parse_block`, `judged`), `claim.py` and
    `tally.py` (the one-home module and the read-only command this subcycle
    copies), and the meter's transcript parsing in `session_cost.py`.
- **Where the work runs.** Tracked changes go under `plugins/devteam/`.
  Scratch goes in `.internal/scratch/0.3.3/`, which is gitignored. 0.3.2's
  scratch tools serve this subcycle too, in `.internal/scratch/0.3.2/`:
  `mutants.py` and `mutant_sets.py` (each mutation on its own copy of the whole
  plugin), `replay.py` (the corpus replay: `REPLAY_OUT`, `REPLAY_GATE` and
  `REPLAY_ONLY`; about 6 min run alone), `compare_3_7.py` (two replays
  compared by identity), `plant_3_7.py` and `plant_board_3_7.py` (a case
  planted on copies of corpus commits), and `examples_3_n.py`. `replay-3.7` is
  the replay at 0.3.2's close. Copy any you change into `0.3.3/`. This plan's
  measurements are there already: `questions_3_0.py`, `items_3_0.py` and
  `typed_prompts.py`, each with its `.out`. Run nothing heavy beside
  `mutate.py` or the mutation runner, which write each mutant into the real
  `scripts/` while they run.
- **The owner is needed once more:** at the close, for one whole `/usage`
  paste from the workstation. Ask him to paste it whenever he is there, and
  take its time from `~/.claude/history.jsonl`. Every decision this plan names
  as his was put to him on 2026-09-24, before this file was written, each with
  its corpus measurement beside it, and is recorded in §2.

## 2. Decisions (settled — see [`README.md`](../0.3/README.md) §6)

L-5, L-6, L-9 and L-18 bind; so do 0.3.1's L-1.1 to L-1.9 and 0.3.2's L-2.1 to
L-2.15. Do not re-litigate them here. Local to this subcycle:

- **L-3.1 — one ledger, `devteam/LEDGER.md`, is the home of every item's
  disposition.** (theme 1; F-99, F-139) *Settled by:* owner, 2026-09-24,
  choosing *"LEDGER.md: audits, questions, open (Recommended)"* over
  *"LEDGER.md: every item, protocol too"* and *"In place, no new file"*.
  - **An item is** every finding an audit reports, wherever its text lands;
    every item under a judged REPORT block's `questions:`, and under its new
    `open:` key (L-3.4); and any item the manager raises itself, from the
    client's words (L-3.7) or at a handoff.
  - **An item is not** a `findings-for-protocol:` line. That is addressed to
    the pipeline, and `run` §6 goes on recording it in `RECORD.md`. Nor is a
    verifier's verdict an item source, because 0.3.8 owns the verdict's grammar.
    Nor is a review's comment: the `review` skill writes *blocking*, *worth
    fixing* and *take it or leave it*, numbers nothing and files nothing in
    `audits/` (`MEASURED`, `skills/review/SKILL.md`), and the owner's answer
    names audits.
  - *Why protocol findings are left out:* the register's theme 1 names no
    protocol finding lost. Its measured losses are questions spoken and never
    filed (F-21's four escalations), audit items (F-99, F-139) and a
    question's status (F-63, F-64). Protocol findings are 119 of the 147 items
    in the corpus's judged blocks (`MEASURED`, `items-3.0.out`), so counting
    them would nearly triple the ledger for a loss nobody measured.
  - **The manager writes it, and nobody else** (P-13). An item's text stays
    where it was raised, as evidence, verbatim (P-17): a REPORT block, an
    audit's landing in a task file, a file in `audits/`. The entry gives the
    item an id, names where its text is, and holds its disposition. This is
    the shape `pricelog`'s manager improvised for T-18's audit,
    `audits/pricelog-T-18-S-4-2026-09-17.md`: *"This file declares them and
    carries each one's disposition; the evidence stays where it was landed."*
    It is also the register's suggestion for F-112 (A6), made the one home.
  - **Its size on the corpus** (`MEASURED` 2026-09-24): 41 audit items — 16 in
    the four gate audits, and 2, 6, 10 and 7 in the auditor passes of
    T-10.S-3, T-17.S-3, T-18.S-4 and T-19.S-3 — and 28 question items in the
    88 judged REPORT blocks. That is about 70 entries, under four per task.

  *Declined:*
  - *every item, protocol findings too* — about 190 entries, ten per task, each
    protocol finding needing `recorded F-n`;
  - *in place, no new file* — audit files keeping a `Disposition.` line per
    finding and report items dispositioned in `RECORD.md`, so dispositions in
    three homes: the shape F-63 and F-64 measured disagreeing (L-3.6).
- **L-3.2 — the disposition vocabulary is closed, five values, and an open
  item names the date it is due by.** *Settled by:* owner, 2026-09-24,
  choosing *"Five; open names its expiry (Recommended)"* over *"The audit
  skill's four, as written"* and *"Four plus fixed"*.
  - `open (until T-n)` or `open (until C-n)` — undecided. The manager owns
    it, as the ledger's one writer, and it must be decided by T-n's close — its
    title `DONE` or `ACCEPTED` — or by the filing of checkpoint C-n. Past that
    it is `expired-item`.
  - `routed T-n` — T-n owns it. The entry's `Needs.` names the paths the item
    needs changed, and T-n's `Scope.` covers every one. The audit skill says so
    in prose — *"a finding routed to a task that cannot touch what it needs is
    filed, not routed"* — and nothing checks it. It becomes
    `routed-out-of-scope`, which a routed entry with no `Needs.` also is.
  - `raised Q-n` — the client decides. Q-n's status in `QUESTIONS.md` is the
    one home of the answer (L-3.6). A question `withdrawn` leaves the item
    undecided again, and that is `expired-item` too.

  *Amended by the owner at §3.2 (L-3.12):* an `until C-n` is a date, not a
  citation; an item due by T-n falls due when T-n's row leaves `CLAIMED`; and
  `routed T-n` falls due at T-n's close.
  - `declined (D-n)` — decided not to act, and D-n says why.
  - `fixed (<commit>)`, or several commits comma-separated — fixed by the
    commits named, each an ancestor of HEAD, as L-2.6 reads a cited hash. One
    that is not is `fix-not-in-history`.

  T-18's ten, in this vocabulary (`MEASURED` from its declarations file):
  COR-1 and COR-4 `raised Q-28`, COR-2 `raised Q-29`, COR-3 `raised Q-30`,
  COR-5 `fixed (a4f3774)`, COR-7 `fixed (653a60a)`, COR-6, COR-9 and COR-10
  `open (until C-9)` and later `fixed (55c0eb7)` (COR-9 `fixed (55c0eb7,
  e586c74)`), and COR-8 `fixed (a4f3774, 55c0eb7)`. Today `check_refs` reads
  all ten as decided, the three with no owner included, and `check_trace`
  cannot tie the file to T-18 at all (`MEASURED` on the clone).

  **Its one home** is FORMATS §"Status vocabularies". **Its one reader** is
  `scripts/ledger.py`, which `check_refs` and `check_trace` import in place of
  their two copies of `OPEN_DISPOSITION` (P-34; 0.3.2 §3.2's joint).

  **P-31 still binds an audit's findings.** A finding an audit of T-n or of one
  of its steps reports is decided — any value but `open` — by T-n's close,
  whatever its `until` says. That is `open-finding-at-close`, read from the
  ledger now (L-3.5).

  *Declined:*
  - *the audit skill's four as written* — T-18's two fixes would read `routed
    T-18`, and the three deferrals `open` with no date, the state F-99 and
    F-139 measured going unowned;
  - *four plus fixed* — an open item still carries no date by which someone
    must decide it.
- **L-3.3 — every item gets one ledger id, `ITM-n`; an audit's own labels
  stay local to its text.** *Settled by:* owner, 2026-09-24, choosing
  *"Ledger ids; audit labels local (Recommended)"* over *"Dimension prefixes,
  reserved"* and *"Dimension prefixes, renumbered"*.
  - `ITM-` joins the namespace, declared only in `LEDGER.md` as `### ITM-n —
    <one line>`. `COR-`, `SEC-`, `HYG-` and `REV-` leave the checked set: an
    auditor's `COR-3` is a label inside its own text, which `check_refs` no
    longer resolves, as it does not resolve `UTF-8`. `CNV-` is unchanged.
    *Amended by the owner at §3.2 (L-3.12): `CNV-` becomes external, as `P-`
    is.*
  - This reverses 0.2.6's widening (FORMATS §"The namespace", *"Safe from
    collision also meant unseen"*). The reason for that widening was findings
    cited from other files and declared nowhere, and the ledger answers it by
    counting every audit's findings directly (L-3.5). FORMATS keeps the
    history, and marks the widening superseded by date.
  - *Why:* the correctness gate audit numbered its findings outside the
    namespace, `## Finding 1 (defect, load-bearing) —` (`MEASURED` on the
    clone). Had it used the namespace, T-18's COR-1 to COR-4 would have been
    declared twice (`REASONED` from the two files). T-18's audit, landed
    without headings as F-104 required of a task file, cited its own COR-1 to
    COR-10 with nothing declaring them: F-112's seven `cited-undefined`
    (`tasks/T-18.md`:1526, `MEASURED`). With one ledger id nothing is reserved
    at dispatch, and nothing collides at width 2.
  - **The safety dimension has no label today.** The audit skill numbers
    `COR-n`, `SEC-n` or `HYG-n`, FORMATS' table has no safety row, and the
    corpus's safety audit wrote `## Finding 1 (HIGH)`. The audit skill gives it
    `SAF-n`, as a local label like the others.

  *Declined:*
  - *dimension prefixes, reserved at dispatch* — a forgotten or concurrent
    reservation reuses a number, and a report citing the auditor's label then
    resolves to the older finding without a word;
  - *dimension prefixes, renumbered in the ledger* — citations written before
    the entry, the supervisor's closing report among them, resolve to
    whatever already holds those numbers.
- **L-3.4 — items arrive as countable lines: a report's `open:` key, and an
  audit's answer between two lines the grammar reads.** (F-139, F-104, F-112;
  0.3.2 §3.5) *Settled by:* planning, open to the owner's overruling.
  - **The REPORT block gains `open: none | - <item>`**, required, after
    `questions:`. It holds anything the report leaves open that is neither a
    question for the client nor a finding for the protocol: a defect found and
    not fixed, or work the step could not reach. *Why:* T-18's restart
    supervisor filed COR-6, COR-9 and COR-10, found and deliberately not
    fixed, under `findings-for-protocol:` (`tasks/T-18.md`:1534–1540,
    `MEASURED`), where the pipeline's findings go and no owner looks. It is
    required so that a reporter cannot leave it out by omission, and `none` is
    an answer, as for `questions:`.
  - **An audit's answer opens `AUDIT <scope> (<dimension>)` and closes `END
    AUDIT <scope>`**, each on a line of its own. The scope is `T-n.S-m` for a
    step's audit, `T-n` for a task's, and a word for a milestone's. The
    dimension is `safety`, `correctness`, `security` or `hygiene`. Every
    finding in it is a heading, `## <LABEL>-n — <title>`, with its dimension's
    label (`SAF`, `COR`, `SEC`, `HYG`), numbered from 1 in each audit, and a
    `- **Needs.**` line under it. It carries no `Disposition.` line, because
    that is the ledger's (L-3.1). *Precedent:* T-19.S-3's auditor already
    opened its message `AUDIT T-19.S-3 (correctness):` (`tasks/T-19.md`:1704,
    `MEASURED`).
  - **Where it lands.** A supervisor lands a step's audit in the task file,
    verbatim, its `AUDIT` and `END` lines included, as it lands a worker's
    block. The manager files a task's or a milestone's audit as
    `audits/<scope>-<dimension>-<date>.md`, the whole message verbatim. The
    scope an audit is tied to is read from its `AUDIT` line, not from a file
    name, so `open-finding-at-close` no longer depends on a name (0.3.1 §3.2).
    The name repeats it for a reader. A file in `audits/` with no `AUDIT` line
    is a part not evaluated — its findings are still counted, and tied to no
    task — as the T-18 step audit's name is today.
  - **An `AUDIT` landing is an audit, not a report.** A `REPORT` line whose
    role is `auditor` is named as not a report, and its message shows the
    `AUDIT` form. That is T-10's `REPORT auditor T-10.S-3 (…` (0.3.2 §3.5).

  *Declined:*
  - *`open:` optional* — a reporter in a hurry omits it, and T-18's supervisor
    measured where an unfiled item goes instead;
  - *the audit's answer inside a code fence*, as T-18's and T-19's landings
    were — a fence hides the text's own citations from `check_refs`, and
    T-18's landing, which holds a nested `python` fence, shows a fence nested
    inside another fence ends it early;
  - *the landing's scope read from the file name*, today's contract — T-18's
    step audit was named `pricelog-T-18-S-4-2026-09-17.md`, and nothing tied it
    to its task (0.3.1 §3.2).
- **L-3.5 — a landing that leaves an item without an entry is refused, once
  the task's claim is released.** (F-139, F-99) *Settled by:* planning, open to
  the owner's overruling.
  - **`unledgered-item`** (`check_trace`): every item raised in a task — each
    of its judged blocks' `questions:` and `open:` items, and each finding in
    an `AUDIT` landing in its file — has a ledger entry whose `Raised.` names
    it, at every commit where the task's board row is not `CLAIMED`. Every
    finding in an `audits/` file has one from the commit that adds the file.
  - **While the row reads `CLAIMED`, a task's items are pending, and nothing
    fires.** The supervisor lands them and may not write the ledger (P-13).
    The manager writes the entries in the commit that moves the row, which is
    its advance, `DONE`, or its stop, `BLOCKED on Q-n`. That is F-19's window
    again, and L-2.2's reason: the manager moves the board after the verifier
    returns (P-18). **A block landed before the task's current claim** began
    (`claim.py`, L-2.5) is a previous claim's, and its items are due whatever
    the row reads, as L-2.7 reads a previous claim's close.
  - **`Raised.` names an item in one of four forms:**
    - a report's item: the block's id, the key and the item's opening words,
      as in `T-19 questions "Does T-19's disclosed limit"`;
    - an audit landed in a task file: its scope and the finding's label, as in
      `T-18.S-4 COR-3`;
    - a filed audit: its path and the label, as in
      `audits/T-7-security-2026-09-30.md SEC-2`;
    - the manager's own: `client <date>` or `manager <date>`.

    The opening words are matched with whitespace and dashes normalised,
    against every block for that id, so an entry made at an earlier stop still
    resolves after a later attempt supersedes its block. An entry naming
    nothing that exists is `unknown-source`.
  - **`open-finding-at-close` reads the ledger.** An entry raised by an audit
    of T-n or of one of its steps, and still `open` when T-n closes, is the
    finding (P-31). The `T-n-<dimension>-<date>.md` name test and its part
    are retired.
  - **`ledger.py` prints, for every item without an entry, the `Raised.` line
    to write**, so the manager copies it rather than types it. Writing the
    entry is 0.3.4's `land` (L-5).
  - A project with items and no `LEDGER.md` reports each item as unledgered.
    The check looked at every item and found no entry, and that is a finding,
    not a part not evaluated.

  *Declined:*
  - *due at the supervisor's landing* — the supervisor cannot write the ledger
    (P-13), so every close would be refused;
  - *due at the task's close only* — a stop's items would wait for a close
    that may never come. T-19 stopped three times and never closed, and F-139's
    two items were lost at its second stop.
- **L-3.6 — a question's state lives in `QUESTIONS.md` alone.** (F-63, F-64)
  *Settled by:* owner, 2026-09-24, choosing *"QUESTIONS.md only
  (Recommended)"* over *"Board lists pending ids, checked"* and *"Keep both,
  compare them"*.
  - **The board loses `## Questions for the client` and `## Decided without
    the client`.** Each fact they held has one home. A question's window
    becomes its `Window.` field in `QUESTIONS.md`: a date and time for a
    `REVERSIBLE` question, `none` for a class that always blocks. A proceeded
    question is its status, `proceeded-unreviewed D-n`, and its decision's
    `Reviewed.` line, as `run` §8 already writes both. `status` and
    `checkpoint` list what is pending and what proceeded from `QUESTIONS.md`.
    The board's `Reviewed at` column has no home yet: where the client's later
    review of a proceeded decision is written is §3.5's to settle with the
    `checkpoint` skill, without editing a decision (P-23), and to record under
    Findings.
  - **A board table row whose first cell names a `Q-n` is
    `question-on-board`** (`check_trace`, P-34). A Tasks row's `BLOCKED on
    Q-n` is the task's state, not the question's, and is not one.
  - **Two rules name *"the table"*, which no longer exists.** P-26's
    `REVERSIBLE` row says the question *"goes on the table with its
    recommendation"*, and P-29 sends the batch *"when the table holds three"*.
    **Two rules supersede them**, one each, in that respect alone (P-23). Each
    is placed after the rule it supersedes and takes that rule's number with a
    letter, as P-10c follows P-10b, and is numbered when it is written, as
    0.3.1's L-1.9 numbered its rules. They say that a question waiting on the
    client is an `open` question in `QUESTIONS.md`, and that the batch goes
    when three are open. `run` §6 and §8's *"the table"*, and DESIGN.md's
    escalation diagram, change with them.
  - *Why* (`MEASURED` on the clone, `questions-3.0.out`): of 142 commits
    touching the board or `QUESTIONS.md`, 22 had the two disagreeing, for three
    questions. Q-2 read open on the board and answered in the log for 4
    commits, Q-10 answered on the board and open in the log for 17 (F-63), and
    Q-12 open on the board and answered in the log for 7 (F-64). The corpus's
    manager wrote the status into the board's `Window expires` column, which
    has no status.

  *Declined:*
  - *the board lists pending ids, checked against the log* — the same fact
    twice, kept equal by a check;
  - *keep both, compare them* — every question fact still written twice.
- **L-3.7 — what the client says during a handoff is read from the session's
  own transcript.** (F-21) *Settled by:* owner, 2026-09-24, choosing *"The
  transcript, read by a command (Recommended)"* over *"A file a hook writes"*
  and *"A tracked file exempt from the lock"*.
  - **The place outside the writer lock is the session's transcript**, which
    already holds every prompt the client typed, with its time.
    `scripts/client_words.py <session> [--since <commit>|<iso time>]` prints
    them. It prints typed prompts only, and skips compaction summaries
    (`isCompactSummary`), injected and peer messages (`isMeta`), tool results
    and local command output. It finds the transcript by session id across
    `~/.claude/projects/`, not by the directory mapping the cycle README's
    §4.8 found wrong. It keeps the result contract: exit 2 when no transcript
    exists, and 3 when a line cannot be read, naming it.
  - *Why* (`MEASURED` 2026-09-24 on this machine, `typed_prompts.py`): all
    three messages the record names as lost are typed prompts in the outgoing
    session's own transcript. They are *"great. lets continue."* at the C-3
    rotation (session `9053d333`, 2026-09-11 11:27:06 EDT; RECORD.md:504),
    *"sweet. I did it and kicked it off. …"* at C-4 (`1aeaa3c8`, 2026-09-13
    16:37:07 EDT; RECORD.md:841), and *"sounds good"* in s24's tenure
    (`609e9161`, 2026-09-18 20:11:19 EDT; RECORD.md:1985). Compaction
    summaries and peer messages sit beside them as user entries, told apart
    only by those two flags.
  - **`resume` §0 runs it** for the outgoing session that `handoff-ready`
    names, since that session's last write to `devteam/`, and minutes its lines
    verbatim in the `resumed` entry, or `none`, instead of asking. 0.3.6 makes
    it a field of the structured handoff record, which is not built here.
  - It depends on the transcript format, as the meter does (L-18). Its control
    plants every kind of line on synthetic transcripts, and never reads a real
    one.

  *Declined:*
  - *a file a hook writes* — a `UserPromptSubmit` hook appending each prompt
    under `devteam/.run/`. The pipeline would own the format, but the hook
    runs on every prompt in every session, and holds nothing typed before it
    was installed;
  - *a tracked file exempt from the lock* — a session the client spoke to
    appends his words, which is the remembered act the register measured
    missing at all fifteen rotations.
- **L-3.8 — acceptances stay P-51's, and 0.3.2's five unrouted items are
  routed.** *Settled by:* owner, 2026-09-24, choosing *"As listed
  (Recommended)"* over *"Acceptances into the ledger"* and *"Leave all five
  unrouted"*.
  - **0.3.1's L-1.8 left open whether the ledger absorbs L-1.6's
    acceptances. It does not.** An acceptance is already one countable line
    with a decision behind it (P-51), and the gate already refuses an
    unaccepted finding. The register's *What worked* lists that mechanism, and
    moving it would give an acceptance two homes while it moved.
  - Routed, each added to its row of the cycle README's §5 in this plan's
    commit:
    - to 0.3.4: 0.3.2 §3.7's stale acceptance, landed unjudged, that refuses
      a restarted task's first report, which its supervisor cannot supersede;
      §3.4's question, whether a finding a supervisor's own commit adds is ever
      acceptable, and by whom; §3.5's gate printing the file being committed
      as `dirty-tree`; and §3.4's two readings of a RUNNING title;
    - to 0.3.10: §3.6's C-8 row, whose tally lines the checkpoint skill's
      *Satisfied?* column does not read, and the CHECKPOINT template's
      missing §3b;
    - to 0.3.13: §3.3's `docs/PAIRS.md` sweep, which predates P-48b to P-51.
- **L-3.9 — the rules.** Each new class names a rule in `docs/CHECKS.md`, or
  `unruled-finding` fails the plugin. One rule is added, numbered when it is
  written into PROTOCOL.md at the next free number, as 0.3.1's L-1.9 did:
  ***an item raised is ledgered: it has one entry, with a decision or a date
  by which one is due, and a landing that leaves one without is refused*.**
  `unledgered-item`, `expired-item`, `routed-out-of-scope`,
  `fix-not-in-history` and `unknown-source` name it, `open-finding-at-close`
  keeps P-31, and `question-on-board` names P-34. The two rules of L-3.6
  supersede P-26 and P-29 in one respect each. No rule is edited (P-23), so
  `check_plugin`'s count goes from 64 rules to 67.
- **L-3.10 — changed identities are listed, and nothing else is renamed.**
  L-2.14's rule, restated. A finding's identity is its check, class, anchor
  file and message, and a part's is its name. So a message or anchor that
  changes makes an acceptance of the old one stale. Record each identity that
  changes under Findings, with its class. The two classes moving onto the
  ledger change their anchor file — `undispositioned-finding` from `audits/`
  to `LEDGER.md`, `open-finding-at-close` likewise — and that is listed, not
  avoided. No `pricelog` commit ever carried an `Accepts.` field (0.3.2 §3.7),
  so none goes stale on the corpus.
- **L-3.11 — what 0.3.3 does not do**, so that nothing falls between
  subcycles:
  - write a ledger entry by command — 0.3.4's `land`, which this subcycle's
    `ledger.py` prints the input to;
  - the structured handoff record, and rotation's open-items field — 0.3.6's,
    which reads the ledger and `client_words.py`;
  - iterate's carried audit findings dispositioned before the charter gate —
    0.3.10's (F-99's iterate half), which the ledger makes countable;
  - a verifier's verdict as an item source, and its coverage field — 0.3.8's;
  - acceptances, and the four joints L-3.8 routes to 0.3.4 — 0.3.4's;
  - the board's and the guard's stop states — 0.3.6's (F-141).
- **L-3.12 — when an item falls due, and where a convention is declared: the
  owner's four answers at §3.2.** *Settled by:* owner, 2026-09-25 01:06:57
  EDT, choosing the recommended option each time. They were put to him before
  §3.2 was built (§4). Each amends the decision it names, in that respect
  alone.
  - **`until C-n` is a date, not a citation** (amends L-3.2, and §3.2's
    *"`check_refs` already resolves every citation"*). `open (until C-n)`
    names a checkpoint not yet filed, and `check_refs` read its `C-n` as a
    citation. So the entry was `cited-undefined` until C-n was filed
    (`MEASURED`, `.internal/scratch/0.3.3/probe_until/`), and `expired-item`
    from the filing on: no commit could hold it clean. `check_refs` does not
    resolve the `C-n` of an `until`. `check_trace` keeps the entry pending
    while no checkpoint numbered n or higher is filed, and reports
    `expired-item` once one is, so a skipped number still falls due.
    *Declined:* *only the next checkpoint* — it would stop a distant date
    from postponing a decision, and would also forbid a deliberate deferral
    past the next checkpoint; *removing `until C-n`* — items no task owns, a
    milestone audit's among them (F-99), would have no open value left.
  - **An item due by T-n falls due when T-n's board row leaves `CLAIMED`**
    (amends L-3.2's *"its title DONE or ACCEPTED"*). The supervisor writes
    T-n's closing title in its own close commit, while the row still reads
    `CLAIMED`, and it may not write the ledger (P-13). Under the title trigger
    the gate would refuse that commit whenever an item due by T-n was open,
    and only the manager could clear it. So the item is due in the commit that
    moves the row, the manager's advance or stop, as L-3.5 makes an unledgered
    item due: `open (until T-n)` is `expired-item` once T-n's title reads
    `DONE` or `ACCEPTED` and its board row does not read `CLAIMED`.
    *Declined:* *the title, as written* — the manager would have to decide
    every item due by T-n before the supervisor closed, or the close would be
    refused and the supervisor would stop to escalate.
  - **`routed T-n` falls due at T-n's close, by the same trigger** (adds to
    L-3.2). An entry still reading `routed T-n` once T-n closed passed every
    check, whether or not T-n fixed it: an item whose owner has gone, the
    loss F-99 and F-139 measured. It is `expired-item`. The manager's advance
    rewrites it as `fixed (<commit>)` or `declined (D-n)`, or routes it on.
    *Declined:* *leaving it as L-3.2 wrote it* — whether T-n fixed the item
    would be left to the verifier and the checkpoint, and nothing mechanical
    would check it.
  - **`CNV-` becomes external, as `P-` is, at §3.4's switch** (amends L-3.3's
    *"`CNV-` is unchanged"*). A decision citing a convention was
    `cited-undefined` (`MEASURED`, §3.1's Findings), because `check_refs`
    resolved `CNV-` against `audits/`. Yet the `onboard` skill records each
    convention confirmed or declined as a decision citing its id, and FORMATS
    says a convention lives outside any project. `CNV-` joins `check_refs`'
    `EXTERNAL`, and a mistyped `CNV-` id is no longer caught. *Declined:*
    *resolving it against the conventions directory* — the result would
    depend on the machine, so a clone, or the gate's checkout, elsewhere
    would answer differently; *leaving it* — every decision citing a
    convention would stay refused, and FORMATS would stay untrue.

## 3. The work

Seven steps. Each is committed on its own, with `run_controls` green and
`check_plugin` clean, so that a rotation can fall between any two. In each:

- the planted case comes out as expected before the real result is believed
  (P-35b);
- every new assertion is shown to fail on a mutant of a copy of the whole
  plugin (`mutants.py`);
- the commit body names the register rows it closes (the cycle README's §9);
- the documents are brought level for what the step changes — CHECKS.md's
  rows, FORMATS, the templates, and the skill text that describes it. A class
  with no CHECKS.md row fails the plugin (`unruled-finding`), and a skill that
  describes the old parse is a second home, now wrong (P-34);
- every example the step adds or changes in a document is executed as written
  in a scratch project, in `.internal/scratch/0.3.3/examples_3_n.py`, with its
  output kept beside it — the owner's rule.

### 3.1 The ledger, its ids and its vocabulary (L-3.1, L-3.2's grammar, L-3.3's `ITM-`)

The audit prefixes stay checked until §3.4, where the join that replaces them
lands, so that no commit in between reads less than today does.

Build:
- `templates/LEDGER.md`: what the ledger is, who writes it (P-13), L-3.9's
  rule, written into PROTOCOL.md at this step, the vocabulary's pointer to
  FORMATS, and one example entry between the
  `<!-- example:begin -->` markers `setup.py` strips. `setup.py` scaffolds it,
  so a fresh project has nine artifacts.
- FORMATS:
  - a section, *The ledger*, giving an entry's grammar. It is
    `### ITM-n — <one line>`, then `- **Raised.**` (L-3.5's four forms),
    `- **Needs.**` (optional; the path list form, required for `routed`) and
    `- **Disposition.**`. Each field is read whole, across its continuation
    lines (L-2.3).
  - The namespace table gains `ITM-`, declared in `LEDGER.md`.
  - §"Status vocabularies" gains the ledger's `Disposition.` row, with
    L-3.2's five values.
- `scripts/ledger.py`, the one home of the ledger's grammar (P-34), as
  `claim.py` is of a claim, with its control, `test_ledger.py`. At this step
  it reads `LEDGER.md`'s entries whole and parses each disposition, and it
  holds the first-word `open` test that both checks still apply to an audit
  file's `Disposition.` lines until §3.4 retires them. Run as a command,
  `ledger.py <project>` prints each entry's disposition and the counts, and it
  follows the result contract as `tally.py` does: an entry it cannot read
  makes it exit 3, naming the line.
- `check_refs`:
  - `LEDGER.md` joins `ARTIFACTS`, and `ITM-n` is declared by its heading
    there and nowhere else. `ITM` joins `KNOWN`, and stays out of
    `MUST_BE_CITED`, for the reason `check_refs`' own comment gives for audit
    findings: one nobody cites is the ordinary state of one being decided;
  - `undispositioned-finding` also reads each ledger entry through
    `ledger.py`. It fires for an entry with no `Disposition.` line, or for one
    reading `open` with no `until`. A value outside the vocabulary is
    `bad-status`, as every other closed field's is.
- `check_trace` and `check_refs` import the first-word test from `ledger.py`
  in place of their two copies of `OPEN_DISPOSITION` (0.3.2 §3.2's joint).
- `check_plugin`'s `namespace-drift` diffs the table against the new set.

**Planted cases:**
- **The fresh project.** `setup.py` into an empty repository, committed: nine
  artifacts, and clean in all three plan checks. `template-ships-a-finding`
  reads the ledger template too.
- **An entry per value**, each clean, the example from FORMATS executed as
  written.
- **F-99 and F-139's state.** An entry reading `open`, and one reading `open —
  carried to the checkpoint, to be given an owner there` — T-18's wording —
  each fire `undispositioned-finding`. Today's first-word reading passes the
  second as decided, and it still does for an audit file until §3.4.
- **Outside the vocabulary.** `put to the client as Q-28`, T-18's COR-1
  wording, fires `bad-status` in the ledger, and the message shows
  `raised Q-n`.
- **The id.** `### ITM-3 —` in a task file declares nothing; `ITM-3` cited
  where no ledger declares it is `cited-undefined`; a second `### ITM-3` in
  the ledger is `duplicate-id`.

**False-positive cases:** an empty ledger; an entry continuing onto a second
line; `CNV-n` cited from a decision, unchanged; an audit file read exactly as
before.

**Measure on the corpus:** the four checks on the clone, before and after.
The corpus has no `LEDGER.md`, so nothing is expected to change. Record any
line that does.

### 3.2 The vocabulary against the tree (L-3.2)

Build, in `check_trace`, through `ledger.py`:
- `expired-item`: `open (until T-n)` once T-n's title reads `DONE` or
  `ACCEPTED`; `open (until C-n)` once C-n is declared in `checkpoints/`; and
  `raised Q-n` once Q-n reads `withdrawn`.
- `routed-out-of-scope`: `routed T-n` whose entry has no `Needs.`, or whose
  `Needs.` holds a path T-n's `Scope.` does not cover. Use `check_trace`'s
  `contains(scope, path)`, the containment `unreachable-acceptance` applies
  (P-34), not a second one.
- `fix-not-in-history`: `fixed (<commit>)` naming a commit that is not an
  ancestor of HEAD. The test is the one L-2.6 built for a hash a report cites,
  inline in `check_report` today (`git merge-base --is-ancestor <hash> HEAD`).
  Give it one home that both checks read (P-34), placed where §3.4's import
  cycle cannot form.

`raised Q-n`, `declined (D-n)`, `routed T-n` and `until T-n|C-n` cite
identifiers, and `check_refs` already resolves every citation in an artifact.
Nothing is added for them.

*As the owner amended it before this step was built (L-3.12):* an `until C-n`
is a date, so `check_refs` does not resolve it, and it falls due once any
checkpoint numbered n or higher is filed. `open (until T-n)` falls due once
T-n's title reads `DONE` or `ACCEPTED` and its board row does not read
`CLAIMED`, and `routed T-n` falls due at the same moment.

**Planted cases:** each class both ways — T-18's COR-6 as `open (until C-9)`
before C-9's file exists and after it; `routed T-18` whose `Needs.` names
`pricelog/store.py`, outside T-18's declared scope (`pricelog/cli.py`,
`tests/test_failures.py`; `MEASURED` on the clone), and one whose `Needs.`
names `pricelog/cli.py`, inside it; `fixed (a4f3774)` on a history that holds
it, and on one that does not; `raised Q-28` over an open Q-28 and over a
withdrawn one.

**False-positive cases:** `open (until T-n)` while T-n is `RUNNING`; a scope
entry that is a directory covering a `Needs.` path; several commits in one
`fixed`, all ancestors.

**Measure:** nothing on the corpus, which has no ledger. §3.7 plants one.

### 3.3 Where items come from: the report's `open:` key, and an audit's answer (L-3.4)

Build:
- DESIGN.md §6: the REPORT block gains `open: none | - <item>` after
  `questions:`. FORMATS §"The REPORT block" says which keys' items are the
  ledger's (L-3.1) and that `findings-for-protocol:` stays the record's.
  `check_report` requires `open:` as it requires `questions:`.
- FORMATS, a section on an audit's answer: the `AUDIT <scope> (<dimension>)`
  and `END AUDIT <scope>` lines, the finding heading with its dimension's
  label and `Needs.`, numbering from 1, no `Disposition.`, where each kind of
  audit lands, and the file name, `audits/<scope>-<dimension>-<date>.md`.
- `ledger.py` reads an audit's answer, in a task file or in `audits/`, into
  its scope and its findings, with cases in `test_ledger.py`. An `AUDIT`-ish
  line that does not parse, and a heading inside an answer that looks like a
  finding and does not parse, are each something it could not read, and it
  says so by line: its findings cannot be counted. `check_trace` reports these
  as parts not evaluated from §3.4 on, when it counts findings. The loose
  shapes it must name are the corpus's: `## Finding 1 (…)`, `### 1.` and
  `## F-1 — HIGH` in the gate audits, and T-19's `AUDIT T-19.S-3
  (correctness): …` with text after the scope.
- `check_report` reads an `AUDIT` landing as text between blocks, as it reads
  a supervisor's prose. A block's field scan, which runs from the line its
  parse stopped at to the next header or heading, also stops at an `AUDIT`
  line, so an answer landed after a block is never read as that block's
  fields. It names a `REPORT auditor …` line as not a report, with the `AUDIT`
  form in its message, in place of today's unparsed-header part for that
  line.
- The `audit` skill's §"The report": the `AUDIT` and `END` lines, the labels by
  dimension with `SAF-n` for safety (L-3.3), `Needs.` on every finding, and no
  `Disposition.` line, since the manager ledgers each finding. Its
  *"Number your findings"* and *"Every finding carries a `Disposition.`
  line"* paragraphs are rewritten to say why.
- The scaffolded `audits/README.md` text in `setup.py`: the name and the form.

**Planted cases:**
- **T-10's landing** (`tasks/T-10.md`:423): `REPORT auditor T-10.S-3 (…`
  over two unindented lines is named as not a report, and the message shows
  `AUDIT T-10.S-3 (correctness)`. The same audit landed in the new form is
  read by `ledger.py`, with its two findings.
- **T-19's landing**: `AUDIT T-19.S-3 (correctness): **one break found.** …`
  inside a fence is named by its line as not read. The same text with the
  scope alone on its line, and `END AUDIT T-19.S-3` after it, is read, with its
  seven items written as findings.
- **`open:`** — a block with no `open:` fires `missing-field`; `open: none`
  and a two-item `open:` are each clean.
- **The file.** A filed audit is tied to the scope its `AUDIT` line names,
  whatever the file is called. A file in `audits/` with no `AUDIT` line is
  named as not read.

**False-positive cases:** a REPORT block landed after an `END AUDIT` line is
read as before; a block followed directly by an `AUDIT` line reads clean, with
no field from the answer named as its own.

**Measure on the corpus:** `check_report` for T-1 to T-19 on the clone. Every
judged block now reports `missing-field` for `open:`. That is real per the new
grammar, and the corpus predates it (0.3.2's *On the corpus at HEAD* treats
its ten prose fields the same way). T-10's part *the REPORT blocks* keeps its
name, with the `AUDIT` form as its reason. Record both.

### 3.4 The join, and the switch (L-3.5, L-3.3)

Build:
- `ledger.py` collects every item. That is each judged block's `questions:`
  and `open:` items, read with the same parse `check_report` uses (`HEADER`,
  `parse_block` and `judged`; P-34), and each finding in an audit's answer
  (§3.3). It matches every entry's `Raised.` against them. Run as a command,
  `ledger.py <project> --pending` prints the `Raised.` line for every item
  without an entry.
- **Mind the import cycle.** The join needs `check_report`'s block parse and
  `claim.py`'s current claim, and `claim.py` imports `check_trace`
  (`MEASURED` at `0003e3d`). So `check_trace` importing a module that imports
  `check_report` at load time closes a cycle: `check_trace` → `ledger` →
  `check_report` → `claim` → `check_trace`. Put the block parse where no cycle
  forms — in a module that imports no check, as 0.3.2 gave the claim
  `claim.py` — and record the choice under Findings.
- `check_trace`:
  - `unledgered-item`, under L-3.5's window. A task's row reading `CLAIMED`
    leaves its current claim's items pending, and a block landed before the
    current claim is due whatever the row reads;
  - `unknown-source`;
  - `open-finding-at-close` read from the ledger. The audit file-name test,
    `AUDIT_FILE`, and its part retire;
  - each audit answer `ledger.py` could not read, as a part not evaluated.
- **The switch (L-3.3).** `COR`, `SEC`, `HYG` and `REV` leave `check_refs`'
  `KNOWN`, and the audit heading leaves its `DECLARATIONS`; `CNV` stays as it
  is. *Amended by the owner (L-3.12): `CNV` leaves `KNOWN` and joins
  `EXTERNAL`, as `P` is.* `check_refs` stops reading an audit file's `Disposition.` lines and
  finding headings, so its audit parts retire, and with their last reader the
  first-word `open` test leaves `ledger.py`. FORMATS' namespace table turns the
  four audit rows into one row for an audit's own labels, `SAF-` among them,
  which are not project identifiers, and marks 0.2.6's widening superseded on
  its date. `namespace-drift` agrees.

**Planted cases:**
- **F-139, the headline.** A task stopped `BLOCKED on Q-n`, whose step's
  answer holds seven findings, with a ledger holding entries for five:
  refused, naming the two. With all seven: clean.
- **The window, both ways.** The same items under a row reading `CLAIMED`:
  silent. The row moved to `DONE` without the entries: `unledgered-item`.
- **A previous claim's items.** A restart whose earlier stop left an item
  without an entry fires under the new `CLAIMED` row.
- **A superseded attempt.** An entry naming an item in attempt 1's block still
  resolves after attempt 2 lands; an item only attempt 2 raises is due.
- **Filed audits.** An `audits/` file whose findings have no entries fires at
  the commit that adds it, whatever any row reads.
- **P-31.** An audit finding of T-n's step, `open (until C-9)`, fires
  `open-finding-at-close` once T-n closes, although C-9 is not filed.
- **`unknown-source`.** An entry naming `T-19 questions "no such words"` fires.
- **The labels.** A task file citing `COR-3` with no audit anywhere is silent,
  where today it is `cited-undefined`. Two audits each holding `## COR-1` are
  two findings, neither `duplicate-id`.

**At the gate.** `test_gate` gains:
- a supervisor's stop landing a report with a question and an `open:` item
  while `CLAIMED`, which lands;
- the manager's stop commit moving the row to `BLOCKED on Q-n` without the
  entries, which is refused, and with them, which lands;
- the manager filing an audit without entries, which is refused, and with
  them, which lands.

The gate is not changed: the window is `check_trace`'s rule, as L-2.2's is.

**Documents.** The `run` skill's §6 says the advance and the stop write an
entry for every item `ledger.py --pending` prints, in the commit that moves the
row. Its `READY-TO-AUDIT` row says each audit is filed with its entries, in one
commit. Execute both as written.

**Measure on the corpus:** `check_refs` and `check_trace` on the clone, before
and after.
- `check_refs`' four gate-audit parts are expected to go, and with them the
  `COR-n` declarations in T-18's declarations file and any `cited-undefined` a
  `COR-n` citation raised. Record the line.
- `check_trace` has no ledger to read, so every item it counts is unledgered.
  Record the count by source against §2's 41 audit items and 28 questions.
  Expected: the 28 questions; the declarations file's ten `COR` headings,
  counted, with its missing `AUDIT` line a part; the gate audits' sixteen
  headings and T-19's fenced `AUDIT` line named as parts, their items
  uncounted; and T-10's and T-17's landings unseen by the join, because no
  line marks either as an audit's answer (`check_report` names T-10's
  `REPORT auditor` line; nothing names T-17's bold paragraph). Record that
  last as the grammar's limit on the corpus: a landing is found by its `AUDIT`
  line, which the audit skill now has the auditor write in its own message.
  The T-18 step audit's name part is expected to go.

### 3.5 A question's state in one home (L-3.6)

Build:
- The BOARD template loses its two question tables. The QUESTIONS template
  gains `Window.`. FORMATS says where each fact lives.
- `check_trace`'s `question-on-board`.
- PROTOCOL: L-3.6's two superseding rules.
- Every site that sends a question to the board's tables, or reads one from
  them, names `QUESTIONS.md` instead. At `0003e3d`, `grep -rn "Questions for
  the client\|Decided without the client\|to the table\|the table holds"
  plugins/devteam --exclude-dir=meta` finds them all (`MEASURED`):
  - `templates/BOARD.md`:72 and :78, the two tables, which go;
  - the `run` skill's §6 status table (:417–419, *"goes to the table"*) and
    §8 (:724, the board's *Decided without the client* row; :728, *"when the
    table holds three"*);
  - the `checkpoint` skill (:75), the `status` skill's §4 (:35), and
    `docs/CEREMONY.md`'s row 61 (:323);
  - P-29 (PROTOCOL.md:510), which its superseding rule leaves unedited;
  - `docs/CONSOLIDATION.md`:437, which quotes the old `run` text in a dated
    record, and stays as it is.

  `grep -rn "on the table"` finds three more (`MEASURED`): P-26's `REVERSIBLE`
  row (PROTOCOL.md:488), which its superseding rule leaves unedited; the `run`
  skill's §8 class table (:554); and DESIGN.md's escalation diagram (:571).
  Re-run both greps after the step, and record what they find.

**Planted cases:** F-63's shape, the board naming Q-10 `answered D-15`, fires
`question-on-board` on the board row, whatever `QUESTIONS.md` says; F-64's
mirror fires the same way; a board with no question table is clean; a Tasks
row reading `BLOCKED on Q-4` is clean; a question carrying `Window.` is read
by `check_refs` as before, its value not judged in this subcycle.

**Measure on the corpus:** `question-on-board` on the clone's board, one per
row naming a question. Record it as the corpus's own, as §3.3 records
`open:`.

### 3.6 The client's words (L-3.7)

Build `scripts/client_words.py` and its control, `test_client_words.py`, on
synthetic transcripts only. The command takes the transcripts' root as an
option, defaulting to `~/.claude/projects/`, so that its control and every
example write only under `.internal/scratch/0.3.3/`, never under `~/.claude`
(0.2's §9: do not write outside this repository). The control plants:
- a typed prompt as a string, and as a list holding a text block;
- a compaction summary, a peer message, a tool result, a slash command and
  its local output, each skipped;
- `--since` given a commit, read as its committer time, and given a time;
- a line that is not JSON, which makes it exit 3 naming the line;
- a session id with no transcript, which makes it exit 2;
- a transcript under a directory name holding a dot, found by its id (the
  cycle README's §4.8).

**Measure on this machine**, not in the control, because real transcripts are
not in the tree: the three messages L-3.7 names, each found in its session
since the outgoing manager's last write before it. Record what the command
prints for each, and anything else it prints that is not a typed prompt.

**Documents.** The `resume` skill's §0 runs the command for the outgoing
session that `handoff-ready` names, and its `resumed` entry minutes the output
verbatim, or `none`. FORMATS §"What each check reads" gains the command's row.
Execute it as written against a scratch project whose `handoff-ready` names a
synthetic session, under a synthetic transcripts root.

### 3.7 The replay, the documents and the examples

- **The replay.** Copy `replay.py` into `0.3.3/` and run it with this
  subcycle's checks and `REPLAY_OUT=replay-0.3.3`: every `pricelog` commit
  rebuilt as the gate's candidate on its parent.
  - **Three families are expected, each real per the new grammar**, because
    the corpus predates it: items without entries once their task leaves
    `CLAIMED`, and every filed audit's findings; question rows on the board;
    and REPORT blocks with no `open:`. Count each.
  - **With those three filtered out, compare the replay with 0.3.2's
    `.internal/scratch/0.3.2/replay-3.7` by identity** (`compare_3_7.py`).
    Every difference is an identity this subcycle changed or is classified
    here, and a false positive is answered before the step closes.
- **Planted on copies of corpus commits**, as `plant_3_7.py` plants. At each
  planted commit the three families' other causes are planted away: the
  board's question tables are removed, and `open: none` is added to each
  judged block. Every other item the corpus holds by then is given an entry
  generated from `ledger.py --pending`, so that only the case's own items
  decide the result:
  - **F-139**, at `b005ec7`, the batch that went without item 8. T-19.S-3's
    answer is landed in the `AUDIT` form at `79f2caf`, with its open items
    (`tasks/T-19.md`:1853–1869) written as findings. T-19's row reads
    `BLOCKED on Q-42, Q-43, Q-44`, the questions `b005ec7` filed. F-139
    counts six open items and two lost; the list holds seven bullets, the
    break among them. With entries for the other five, the commit is refused,
    naming item 8 (*"Needs a CHARTER decision on a user's interrupt"*) and the
    message in items 2 and 3. With all seven, it would commit.
  - **F-112**, at `c073ff4`, the manager's filing of Q-28 to Q-31, where
    T-18's row leaves `CLAIMED`. T-18.S-4's answer is landed in the `AUDIT`
    form at `34c4430`. With its ten findings ledgered as the record
    dispositioned them by then, in L-3.2's vocabulary, the commit would
    commit; without them, `unledgered-item` names all ten; and no `COR-n`
    citation is refused either way (L-3.3). COR-8 was six-ninths fixed by
    then, so record how its entry says so — `open (until C-9)`, or two
    entries — and whether the choice needs the owner.
  - **F-99**, at `7234e48`, the four gate audits filed, each opening with an
    `AUDIT` line whose scope is a word, and each finding written in the
    heading form. Sixteen have headings today; record how the hygiene audit's
    three unheaded cosmetic items are counted. With an entry for each, it
    would commit. With the correctness audit's Finding 3 `open (until C-6)`,
    `expired-item` fires at C-6's commit, `ec8c924` (2026-09-14 06:35), the
    first checkpoint after the filing. The record found the gap that evening
    (RECORD.md:1157), and Finding 3 got an owner on 2026-09-17 (Q-22, D-33;
    RECORD.md:1204): four days, as the register counts them.
- **The documents.** FORMATS §"What each check reads" names `LEDGER.md` and
  the transcript. CHECKS.md has a row for each new class. PROTOCOL has
  L-3.9's three rules. DESIGN.md §6 shows `open:`. The skills are level: `audit`,
  `supervise` (landing an audit's answer; relaying items), `work` (`open:`),
  `run` (§6, §7b, §8), `checkpoint`, `status`, `resume` and `check`, the last
  to read the new classes. The plugin README's *Known problems* row on open
  items stays until 0.3.13, which strikes a row only where the fix has been
  shown working.
- **Every documented example is executed as written**, the owner's rule. Each
  earlier step's `examples_3_n.py` is re-run at this step's tree, and each
  result that changed is recorded.

## 4. The decision rule

- The owner's answers settle L-3.1, L-3.2, L-3.3, L-3.6, L-3.7 and L-3.8.
  All were given on 2026-09-24, before this file was written, and are
  recorded in §2. His four answers of 2026-09-25, given before §3.2 was
  built, settle L-3.12.
- If two items in one block's key begin with the same words, so that
  `Raised.` cannot tell them apart, record the case and match on the item's
  position within its key as well. Put any change to `Raised.`'s grammar that
  this needs to the owner as options before building it.
- If `client_words.py` meets a line on this machine that it cannot place as a
  typed prompt or a kind it skips, record it and stop §3.6 until the owner
  decides. A command that prints a summary as the client's words puts words
  in his mouth.
- If a parse turns out to need a grammar change no decision here covers, put
  it to the owner as options before building it.
- If the filtered replay differs from `replay-3.7` in a way no changed identity
  explains, answer it before §3.7 closes.
- If a part the corpus reports is owned by no subcycle, add it to the owning
  subcycle's row in the cycle README's §5, with the reason.

## 5. Done when

- `LEDGER.md` ships in the templates and in every fresh scaffold, and a fresh
  project reads clean in all three plan checks.
- The vocabulary is closed in FORMATS and read by `ledger.py` alone, and each
  value's class is planted both ways.
- `ITM-` is a checked prefix and `COR-`, `SEC-`, `HYG-` and `REV-` are not;
  `namespace-drift` agrees.
- Every item source L-3.1 names is counted, and the F-139 case planted on the
  corpus is refused for its two lost items and lets all seven through.
- A question's state is written only in `QUESTIONS.md`: the board template has
  no question table, the rules superseding P-26 and P-29 are in PROTOCOL, and
  no skill sends a question to *"the table"*.
- `client_words.py` finds the three messages L-3.7 names on this machine, and
  its control plants every kind of line.
- Every new class has a CHECKS.md row, cases both ways and killed mutations.
  `python3 plugins/devteam/scripts/run_controls.py` is green, with the new
  cases counted, `check_plugin.py` is clean, and `mutate.py` exits 0.
- The replay's three expected families are counted, and with them filtered
  every refusal equals `replay-3.7`'s by identity or is classified under
  Findings.
- Each item of the cycle README's §5 row for 0.3.3 is built, or recorded here
  as moved, with the owner's decision.
- Each commit body names the register rows it closes.
- This file's title reads `DONE (<date>)`, and the file has moved to
  [`../done/`](../done/).
- The closing cost is recorded as L-18 says.

## 6. Do not

- Add a branch to the gate. The ledger's window is `check_trace`'s rule, as
  L-2.2's is.
- Tune a check until the corpus passes. A corpus refusal is classified and
  answered, never made to disappear (P-35b).
- Write into `pricelog`, or run anything in it. Use the clone, and copies of
  it.
- Copy a real transcript into the tree, or into a control. The client's words
  reach the record only through `resume` §0's entry.
- Absorb acceptances into the ledger (L-3.8), or build what L-3.11 leaves to a
  later subcycle.
- Edit a protocol rule in place (P-23).
- Change `guard.py` or `commit_guard.py`, whose limits are 0.3.11's, or add a
  hook (L-3.7).

## Findings

*Each step records here what it measured before the next step starts.*

### §3.1 as built — the ledger, its ids and its vocabulary

`MEASURED` 2026-09-25. `devteam/LEDGER.md` ships in the templates and in every
fresh scaffold. `scripts/ledger.py` is the one home of its grammar. `check_refs`
declares `ITM-n` from its heading, in `LEDGER.md` alone, and judges each entry's
disposition through it, and both checks take the first-word `open` test from
it.

**What was built.**
- `templates/LEDGER.md`: what the ledger holds and who writes it (P-13), P-52,
  the pointers to FORMATS, the command, and one example entry between the
  markers `setup.py` strips. `setup.py` is unchanged, because it installs every
  template `NOT_INSTALLED` does not name, so a fresh project has nine
  artifacts.
- FORMATS: *The ledger*, after §"Status vocabularies", with an entry's grammar
  and an example; an `item` row in §"Identifier declarations"; `ITM-` in the
  namespace table, which now counts six checked three-letter prefixes; the
  ledger's `Disposition.` row in §"Status vocabularies"; and `check_refs`' row
  of §"What each check reads", naming the dispositions it judges.
- `scripts/ledger.py` and its control, `test_ledger.py`. It holds the heading,
  the three fields read whole, the five-value vocabulary, and `is_open`, the
  first-word test. As a command it prints each entry's disposition and the
  counts by value, and names by line, exit 3, each entry whose disposition it
  could not count.
- `check_refs`: `LEDGER.md` in `ARTIFACTS`; `ITM` in `KNOWN` and out of
  `MUST_BE_CITED`, with a home in `HOMES`, so `cited-undefined` says where to
  declare one; the declaration read from `ledger.HEADING` in `LEDGER.md` only;
  and each entry judged. An entry with no `Disposition.`, or one reading `open`
  with no date, is `undispositioned-finding`, anchored at its heading. A value
  outside the vocabulary is `bad-status`, anchored at the field, and its
  message ends with the vocabulary. A `Disposition.` line the ledger's grammar
  did not read is the part *LEDGER.md's dispositions*.
- `check_trace` and `check_refs` call `ledger.is_open` for an audit file's
  `Disposition.`, and their two copies of the regex are gone.
- PROTOCOL: **P-52**, L-3.9's rule, in §5 after P-29. `check_plugin` counts 65
  rules.
- CHECKS.md's `bad-status` and `undispositioned-finding` rows; DESIGN.md §3's
  tree; and the `setup` skill's *nine artifacts*.

**Choices made inside L-3.1 to L-3.3 and L-3.9, each open to the owner's
overruling:**
- **P-52 sits in §5, *Decisions, questions and escalation*, after P-29.** The
  two rules L-3.6 supersedes live there too, so the three sit together. P-52
  names no REPORT key. It describes an item by what it is — a question for the
  client, a defect found and not fixed — and FORMATS §"The ledger", the
  grammar's one home, names the keys. So `open:`, which joins the REPORT block
  at §3.3, changes FORMATS and not a rule, which P-23 would forbid editing.
- **The vocabulary is matched whole. Only whitespace, and a commit's
  backticks, are layout.** `open  (until C-9)` and ``fixed (`a4f3774`)`` read
  as `open (until C-9)` and `fixed (a4f3774)`: pricelog's manager backticked
  every hash in T-18's declarations file (`MEASURED`). Bold around a value is
  `bad-status`, as it is around a requirement's `Status.`, which is the field
  the ledger's most resembles. The board cell and the record verdict read
  decoration because the owner settled it from what the corpus wrote (L-2.1,
  L-2.12), and no corpus has written a ledger.
- **A value whose first word is `open`, and that is not `open (until T-n)` or
  `open (until C-n)`, is `undispositioned-finding`**, whatever follows it:
  `open — carried to …` and `open (until C-9) — at the checkpoint` alike. §3.1
  says an entry *reading `open` with no `until`* fires it. Read narrowly, a note
  after a well-formed date would be neither class, so a value that says it is
  open, and whose date the vocabulary cannot read, is undispositioned. Its
  message says *open, with no date the vocabulary reads*.
- **A second field of one name in an entry is named as not read, and the first
  stands.** A manager updating `open (until C-9)` may add a line instead of
  editing one, and reading either would be a guess.
- **A heading in an entry's position that does not parse** — `## ITM-3 —`,
  `### ITM-3:`, `### ITM3 —`, `###ITM-3 —` — is named by `ledger.py`.
  `check_refs` names it through its existing *declarations* part, and the
  number it fails to declare is `cited-undefined` from its own line, as a
  decision declared `### D-2:` is.
- **`ledger.py` never exits 1**, as `tally.py` never does: `check_refs` judges
  the ledger, and the command prints it. An entry whose disposition it could
  not count — none, outside the vocabulary, or open with no date — is named in
  the part *LEDGER.md's entries*, exit 3. No `LEDGER.md` is not evaluated
  either, because nothing was read. An empty ledger is clean, with its zeros
  shown.

**The planted cases, as §3.1 lists them.**
- *The fresh project*: nine artifacts, `LEDGER.md` among them. `check_trace` is
  clean with `--pre-plan` and without, `check_refs` clean `[12 files, 0
  declared, 0 cited]`, `check_scope` clean `[0 tasks, 0 live]`, and `ledger.py`
  clean with every count zero (`examples-3.1.out`). `template-ships-a-finding`
  reads the ledger template: an entry shipped outside its markers is reported
  (`template-ships-a-finding-the-ledger-template`).
- *An entry per value*, each clean, in both controls. FORMATS' example,
  executed as written, is clean in `check_refs` and `ledger.py` once what it
  cites is declared: T-18 with its step S-4, C-9, and COR-6 (below).
- *F-99 and F-139's state*: `open`, and `open — carried to the checkpoint, to be
  given an owner there`, each fire `undispositioned-finding`. **A deviation
  from the plan's wording, declared.** §3.1 says *today's first-word reading
  passes the second as decided*. It does not (`MEASURED`): the second's first
  word is `open`, and the first-word test reads it open. The wording that test
  passes is T-18's own, *carried to the checkpoint D-38 requires before T-18's
  next dispatch, to be given an owner there*, whose first word is `carried`.
  Both are planted. In the ledger T-18's wording is `bad-status`, and in an
  audit file it still reads as decided until §3.4, as §3.1 intends
  (`fp-t18s-carried-wording-in-an-audit-file-still-reads-decided`).
- *Outside the vocabulary*: `put to the client as Q-28` is `bad-status`, and the
  message ends with the vocabulary, `raised Q-n` in it.
- *The id*: `### ITM-3 —` in a task file declares nothing and is
  `cited-undefined` from its own line. `ITM-3` cited with no ledger is
  `cited-undefined`, and its message says *Declare it in LEDGER.md as `### ITM-3
  — <one line>`*. A second `### ITM-1` is `duplicate-id`.
- *False positives*: an empty ledger; an entry continuing onto a second line; an
  entry nobody cites, which is not `defined-uncited`; `CNV-`, unchanged
  (below); and an audit file read exactly as before, every audit case in both
  controls passing unchanged.

**On the corpus**, the clone at `a88990b`: the four checks print exactly what
they printed at `0003e3d`, every line of every output (`corpus-3.1-*.out`
against `corpus-before-*.out`). The corpus has no `LEDGER.md`, and §3.1
expected no change.

**Found at the joints:**
- **`CNV-` cited from a decision is `cited-undefined`, before this step and
  after it.** §3.1 lists it among the false-positive cases, which expects it
  clean. FORMATS says a convention *lives outside any project and is cited from
  a decision that adopts or declines it*. `check_refs` resolves `CNV-` against
  the project's declarations, and `DECLARED_IN` puts those in `audits/`, as a
  finding with a `Disposition.` line. So every citation of a convention is
  reported unless the project declares it as an audit finding (`MEASURED`, a
  scratch project whose D-1 adopts `CNV-1`). No project has cited one: the
  clone holds none, and the plugin only FORMATS' prose. It is pinned
  unchanged, as L-3.3 says, in
  `cnv-unchanged-a-convention-declared-nowhere-is-still-cited-undefined`.
  **For the owner**, before §3.4's switch, which is where it could change: make
  `CNV-` external, as `P-` is, which is what FORMATS describes; or leave it,
  and FORMATS' description of it stays untrue.
- **`Raised.`'s form `T-n.S-m <label>` is a qualified step citation**, so
  `check_refs` already requires T-n to declare S-m (`MEASURED`: FORMATS'
  example was refused until the scratch T-18 declared S-4). The task, question
  and decision ids of the other forms resolve the same way. So at §3.4
  `unknown-source` has less to add than its wording suggests. `check_refs`
  already refuses a `Raised.` naming a task or a step that does not exist, and
  the join adds what no citation reaches: the label, and the quoted opening
  words.
- **FORMATS' example resolves `COR-6` only through an audit file's
  declaration**, at this commit, because the audit labels stay checked until
  §3.4. Without one it is `cited-undefined` (`examples-3.1.out`). §3.7 re-runs
  the example after the switch.
- **`test_setup.py` printed a constant as its false-positive count**, `fp = 6`,
  which matched its cases until one was added. It counts them by name now: 7
  of 21.
- **The mutation runner reads a case name up to its first space.**
  `test_ledger`'s first draft put the value in its unit cases' names, quotes
  and spaces included, so the runner read them cut and reported them unmoved,
  whether a mutation had moved them or not. They have kebab-case names now,
  as every other control's cases do. Five of them were then still unmoved, and
  a mutation was aimed at each. `mutate.py`'s `^FAIL\s+(\S+)` reads names the
  same way.

**Identities changed (L-3.10).** None. Every message and part name this step
does not mean to change is byte-identical, `undispositioned-finding`'s for an
audit file among them. The ledger's findings, and its part *LEDGER.md's
dispositions*, are new.

**The controls.**

| Control | Cases before → after | Mutations killed |
|---|---|---|
| `ledger` | new, 60 | 40 of 40 |
| `check_refs` | 106 → 124 | 19 of 19 |
| `check_trace` | 193 → 194 | 2 of 2, one only once a case was added for it |
| `check_plugin` | 77 → 80 | 2 of 2 |
| `setup` | 18 → 21 | 2 of 2 |

Against the pre-3.1 code, every `ledger` case fails, because its subject did
not exist (`REASONED`). Measured, so do 10 of `check_refs`' 18 new cases, 2 of
`setup`'s 3 and 1 of `check_plugin`'s 3 (`pre-3.1-new-cases.out`). The other twelve are twins, and pins of unchanged
behaviour, that the old code also passes. Each fails under a mutation aimed
at it:
- `item-must-be-cited` fails `clean-a-ledger-entry-per-value` and
  `fp-an-item-nobody-cites-is-not-defined-uncited`;
- `an-empty-ledger-is-not-evaluated` fails `fp-an-empty-ledger`;
- `ledger-field-first-line-only` fails
  `fp-a-ledger-disposition-continuing-onto-a-second-line-is-read-whole`;
- `audit-files-judged-by-the-vocabulary`, the switch made early, fails
  `fp-t18s-carried-wording-in-an-audit-file-still-reads-decided`;
- `ledger-teaching-judged` fails `fp-a-ledger-disposition-placeholder-is-teaching`;
- `ledger-not-an-artifact` fails `fp-an-item-the-ledger-declares-resolves`;
- `cnv-made-external` fails the `CNV-` pin;
- `examples-not-stripped` fails `the-ledger-ships-no-example-entry`;
- `known-read-to-two-letters` fails both of `check_plugin`'s namespace cases;
- `trace-first-word-a-prefix` fails `check_trace`'s new twin,
  `fp-open-finding-at-close-a-disposition-beginning-opened-is-decided`, which
  was added because that mutation survived the control without it.

No existing case was rewritten. `check_refs`' control gained an optional fifth
element, words its output must hold. All 21 controls are green in 2 min 17 s,
`check_plugin` reads `clean  [15 skills, 9 agents, 65 rules, 6 root entries]`,
and `mutate.py` exits 0. The sets are in `.internal/scratch/0.3.3/mutant_sets.py`,
run by `mutants.py`, copied from 0.3.2.

**Executed as written** (`examples_3_1.py`, its output in `examples-3.1.out`):
the `setup` skill's *nine artifacts*; the three plan checks and `ledger.py` on
the fresh project; the template's command line as written, with
`CLAUDE_PLUGIN_ROOT` set; FORMATS' entry, with and without the audit's
declaration; and the vocabulary row's three refusals and one clean value.

**Spend so far**, by 0.3.0's meter from the opening reading, 22:31:29, to 00:39
EDT: 267 requests, `$32.77` at list price, across this subcycle's two
sessions:
- `claude-skills-devTeam_s41`: 144 requests, `$19.98`;
- `claude-skills-devTeam_s42`, to this point: 123 requests, `$12.79`.

That is 1.2 to 1.5 points of the all-model gauge, *converted, not read*
(L-18). In the same window `_s40` made 18 requests (`$2.81`), which are 0.3.2's
close, the compiler's sessions spent `$27.71` on Fable, and a `nitpick-libs`
session spent `$4.57` on Opus.

### §3.2 as built — the vocabulary against the tree

`MEASURED` 2026-09-25. `check_trace` reads each ledger entry's disposition
against what it names, as three classes: `expired-item`, `routed-out-of-scope`
and `fix-not-in-history`. The ancestry test they share with `check_report` has
one home, `result.in_history`. Before any of it was built, the owner answered
four questions the step's design raised (L-3.12, 01:06:57 EDT). The build
follows his answers.

**What was built.**
- `scripts/result.py`: `in_history(root, ref)`, which returns `IN_HISTORY`,
  `ELSEWHERE` or `ABSENT`. `check_report`'s cited-hash test now reads it,
  and its two `unknown-commit` messages are byte-identical.
- `scripts/check_trace.py`:
  - it lists `LEDGER.md`, `QUESTIONS.md` and `checkpoints/*.md`;
  - `question_statuses` and `filed_checkpoints` are new;
  - `path_lists` takes the field it reads, and `NEEDS_FIELD` is `Needs.`;
  - the ledger block judges each entry whose disposition parses:
    - `open (until T-n)` and `routed T-n` are `expired-item` once T-n's title
      reads `DONE` or `ACCEPTED` and its board row does not read `CLAIMED`;
    - `open (until C-n)` once any checkpoint numbered n or higher is filed;
    - `raised Q-n` once Q-n reads `withdrawn`;
    - a `routed T-n` entry whose `Needs.` T-n's `Scope.` does not cover, or
      which has none, is `routed-out-of-scope`;
    - each commit of a `fixed (…)` entry not in HEAD's history is
      `fix-not-in-history`.
- `scripts/check_refs.py`:
  - the title declaration is named `TITLE_DECLARATION`;
  - a ledger entry's `until C-n` is not resolved: the lines its disposition
    is read from are dropped from `C-n`'s citations.
- Documents: CHECKS.md gains the three rows and names what `check_trace` now
  reads. Its `cited-undefined` and `unknown-commit` rows say what changed,
  and its class count moves from 26 to 29, a count written by hand.
  FORMATS changes in three places:
  - its vocabulary row;
  - a paragraph in §"The ledger" on what each value must stay true to;
  - `check_trace`'s and `check_refs`' rows of §"What each check reads".
  The ledger template says what `check_trace` reports, and when the due items
  are decided. The `audit` skill gains one sentence: the rule it states in
  prose, *"filed, not routed"*, is checked in the ledger. §3.3 rewrites the
  rest of that section.

**Choices made inside L-3.2 and L-3.12, each open to the owner's
overruling:**
- **The ancestry test's home is `result.py`**, beside `listed()`, the other
  git read every check shares. `ledger.py` would also have kept §3.4's cycle
  from forming. But the test is not the ledger's grammar, and `check_report`
  would then import the ledger for a history test.
- **A checkpoint is filed as `check_refs` declares one**, so the two checks
  cannot disagree about which checkpoints exist (P-34):
  - by its `TITLE_DECLARATION`;
  - in a file its `ARTIFACTS` names;
  - with fenced lines skipped as it skips them.

  So `check_trace` now imports `check_refs`, as it imports the guard's
  Protected-paths pattern. That makes a new dependency between two checks,
  recorded under the joints below.
- **A question's status is read by `check_trace`'s own field parse**, a
  `### Q-n —` block's `Status.`, read whole, as it reads a requirement's. Only
  `withdrawn`, matched whole, makes an item due. The vocabulary stays
  `check_refs`' to judge, so a status outside it is `bad-status` there and
  expires nothing here.
- **`Needs.` is read by the parse `Scope.` is, and is not added to
  `LIST_FIELD`.** The corpus's task files hold audit landings whose findings
  carry `Needs.` lines (`tasks/T-17.md`:444–483 and `tasks/T-18.md`:1001–1028,
  `MEASURED`). Read as the task's own, their prose would be named as the
  task's unparsed entries (`REASONED`). The corpus outputs are unchanged
  (below).
- **For a routed entry, the first `Needs.` stands**, as `ledger.entries` reads
  it. A second `Needs.`, one named and not written as the field, or an entry
  that is not a bare path is named in the part `ITM-n's Needs.` Each is named
  by line, and none is guessed at. It is a finding only when nothing was
  offered unread: no `Needs.`, or one naming no path.
- **One fault, one report.** A routed item whose task has closed is
  `expired-item` alone, and is not also judged for scope. A value naming a
  task no file declares is left to `check_refs`' `cited-undefined`.
- **The claim is read from the Tasks table's `State`**, first word
  `CLAIMED`, as L-3.5 words the window (*"the task's board row"*), not from
  the in-flight table. A board this check cannot read shows no claim. So an
  item due at a close is due, not held, which fails closed as the gate's
  allowance does.
- **The listing is the directory's, as `audits/*.md`'s is.** An untracked
  `LEDGER.md`, `QUESTIONS.md` or checkpoint file is `untracked-file` in
  `check_trace` now, as it already was in `check_refs`. That holds even when
  no entry needs the file.
- **Each finding is anchored at the entry's `Disposition.` line**, the value
  that went untrue. `check_refs` anchors `undispositioned-finding` at the
  heading, because there may be no such line. The messages cite P-52, the
  rule, and not L-3.12, the roadmap's decision.
- **In `check_refs`, only the date is left unresolved.** That is the lines the
  `Disposition.` is read from: its own line, and each line continuing it
  (`result.continuation`, as `ledger.entries` reads that field). So the same
  checkpoint cited in a note of the entry still has to be declared.

**The planted cases, as §3.2 lists them.**
- *T-18's COR-6, `open (until C-9)`, before C-9's file and after it*: silent
  with C-8 filed, and `expired-item` once C-9 is filed. Also `expired-item`
  when C-10 is filed and C-9 is not, because a skipped number still falls
  due.
- *`routed T-18`*, with T-18's declared scope as the clone has it:
  `pricelog/cli.py` and `tests/test_failures.py` (`tasks/T-18.md`:40–42,
  `MEASURED`). `Needs.` naming `pricelog/store.py` is `routed-out-of-scope`,
  and naming `pricelog/cli.py` is clean.
- *`fixed (a4f3774)` on a history that holds it, and on one that does not*.
  **A deviation, declared.** A synthetic history cannot hold `a4f3774`, so
  the clean case names a commit its own history makes. `a4f3774` itself is
  refused on a history that does not hold it, as *not a commit in this
  repository*. That it reads clean on the corpus's own history is §3.7's to
  plant. A commit on no branch HEAD holds is refused as *a commit, and not
  one in HEAD's history*. Of several commits, only the one outside is named.
- *`raised Q-28` over an open Q-28 and a withdrawn one*: clean, then
  `expired-item`. An answered Q-28 is clean too.
- *False positives*: `open (until T-1)` while T-1 is `RUNNING`; a scope
  directory covering a `Needs.` path; and several commits in one `fixed`, all
  ancestors, one abbreviated and one backticked.
- *L-3.12's cases*:
  - the window both ways, in `check_trace` and at the gate;
  - `routed T-n` at T-n's close;
  - an item due by another task, which T-1's close leaves pending;
  - in `check_refs`, a date not yet filed, and one wrapped onto its next
    line, both clean;
  - the same checkpoint cited in a note, and `until T-9` with no T-9, each
    still `cited-undefined`.
- *At the gate* (`test_gate`), measured on the gate itself:
  - the supervisor's close lands over an item due by its task;
  - the manager's advance with the item still open is refused, naming it;
  - the advance that decides it lands.

**On the corpus**, the clone at `a88990b`, working tree clean: the four checks
print exactly what they printed at `0003e3d`, every line of all 22 outputs
(`corpus-3.2-*.out` against `corpus-before-*.out`). The corpus has no
`LEDGER.md`, and §3.2 expected no change.

**Found at the joints:**
- **Four questions, answered before the build (L-3.12).**
  - *`until C-n` could never be clean.* A checkpoint named as a date is not
    filed yet, so `check_refs` reported it `cited-undefined` until it was
    filed (`MEASURED`, `probe_until/`), and from the filing on it is
    `expired-item`. §3.1's example could only be shown clean by declaring
    the checkpoint it waits for.
  - *The title trigger put its one refusal on the agent who cannot act.*
    Measured by mutation at the gate
    (`s32-window-ignored-the-title-alone`): under the literal trigger the
    supervisor's close is refused, though it may not write the ledger. And
    once that close is past, the manager's advance over the item still open
    is **not** refused, because the finding is already at HEAD (F-12's
    ratchet).
  - *`routed T-n` had no date*, so a routed item passed every check after
    its task closed.
  - *`CNV-`* (§3.1's joint): the `onboard` skill's own instruction produces
    `cited-undefined`. It is built at §3.4, with the switch.
- **FORMATS' entry, executed as written**, is now clean in `check_refs` with
  no checkpoint filed. §3.1 had to declare C-9 for it. It is clean in
  `check_trace` while C-9 is pending, and `expired-item` once C-9 is filed
  (`examples-3.2.out`).
- **`check_trace` imports `check_refs` now.** `claim.py` and `gate.py` import
  `check_trace`, so they load `check_refs` too. No cycle forms today:
  `check_refs` imports `result` and `ledger` only. **For §3.4:** `check_refs`
  must stay free of any import of `check_trace`, `claim` or `check_report`,
  and so must whatever `ledger.py` comes to import. Otherwise the cycle §3.4
  names forms through this edge.
- **A mutation survived the `check_trace` control until a case was added.**
  `s32-every-close-expires-every-entry` makes an entry due when any task
  closes. It passed all 227 cases, because no case had one task closed beside
  an item due by another. The gate's `fp` case caught it first, and
  `fp-an-item-due-by-another-task-is-not-due-at-this-ones-close` now does.
- **One kill is a crash.** `s32-due-without-closing` fails
  `fp-open-until-a-task-no-file-declares-is-left-to-check-refs` with a
  `KeyError`: the mutant reads the title of a task that does not exist. The
  shipped code cannot reach that line for an undeclared task, because a task
  is closed only if it is declared (`REASONED` from `closed`).

**Identities changed (L-3.10).** None:
- `check_report`'s `unknown-commit` messages are byte-identical. Its control
  passes unchanged, and its corpus outputs are byte-identical.
- `check_refs` no longer reports a ledger entry's `until C-n` date. That
  removes a finding for a ledger. It does not change an identity, and the
  corpus has no ledger.

New:
- the three classes;
- the part `ITM-n's Needs.`;
- `check_trace`'s `untracked-file` for `LEDGER.md`, `QUESTIONS.md` and a
  checkpoint file.

**The controls.**

| Control | Cases before → after | Mutations killed |
|---|---|---|
| `check_trace` | 194 → 228 | 28 of 28, one only once a case was added for it |
| `check_refs` | 124 → 128 | 4 of 4 |
| `result` | 75 → 79 | 4 of 4 |
| `gate` | 70 → 73 | 2 of 2 |
| `check_report` | 101 → 101 | 2 of 2, the shared test's own, killed by its L-2.6 cases |

Against the plugin at `cb01093` (`pre-3.2-new-cases.out`):
- 17 of `check_trace`'s 34 new cases fail;
- 2 of `check_refs`' 4 fail;
- 1 of the gate's 3 fails;
- all 4 of `result`'s fail. The control crashes, because `in_history` did
  not exist.

The other 21 are pins of what the old code also did. Each fails under a
mutation aimed at it:
- `s32-window-ignored-the-title-alone` fails the two `…-while-its-row-reads-claimed`
  cases, and the gate's supervisor's close;
- `s32-every-close-expires-every-entry` fails
  `fp-an-item-due-by-another-task-is-not-due-at-this-ones-close`, and the
  gate's advance that decides the item;
- `s32-every-task-due` fails `fp-open-until-a-task-still-running`;
- `s32-checkpoint-any-number`, `…-fence-read` and `…-any-file` fail the three
  checkpoint pins;
- `s32-raised-due-whatever-its-status` and `…-withdrawn-any-settled-status`
  fail the open and answered questions;
- `s32-routed-exact-path-match` fails the scope directory and the list written
  beside the field;
- `s32-due-without-closing` fails the need inside T-18's scope, and the open
  item due by a task no file declares (by a crash, above);
  `s32-routed-undeclared-task-judged` fails the routed one;
- `s32-an-empty-ledger-is-a-part` fails the empty ledger;
- `s32-in-history-never` fails the two histories that hold their fix;
- `s32-until-exempts-every-site` and `s32-until-task-exempted-too` fail
  `check_refs`' two `cited-undefined` pins.

No existing case was rewritten. The scratch runner, `mutants.py`, now takes a
glob pattern (`'s32-*'`). All 21 controls are green in 2 min 27 s.
`check_plugin` reads `clean  [15 skills, 9 agents, 65 rules, 6 root
entries]`, and `mutate.py` exits 0, in 2 min 33 s. The sets are in
`mutant_sets.py`, and the results are in `mut-3.2-*.out`.

**Executed as written** (`examples_3_2.py`, its output in `examples-3.2.out`):
- the fresh project, clean in the three plan checks and `ledger.py`;
- the ledger template's command line;
- FORMATS' entry, with C-9 unfiled and then filed.

The behaviour the new FORMATS paragraph states is planted both ways in
`test_check_trace` and `test_gate`, and is not repeated there.

**Spend so far**, by 0.3.0's meter from the opening reading, 22:31:29, to 01:41
EDT: 393 requests, `$48.02` at list price, across this subcycle's three
sessions:
- `claude-skills-devTeam_s41` (`e274c7bf`): 144 requests, `$19.98`;
- `claude-skills-devTeam_s42` (`c7b11bae`): 138 requests, `$15.01`;
- `claude-skills-devTeam_s43` (`4f857e14`), to this point: 111 requests,
  `$13.03`.

That is 1.8 to 2.2 points of the all-model gauge, *converted, not read*
(L-18).
