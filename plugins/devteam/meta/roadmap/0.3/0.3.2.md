# 0.3.2 — what the checks read: every gap 0.3.1 named, read, and a restart judged by its own claim — PLANNED

> **Cost, `REASONED`.** About `$90` to `$160` of Opus 5.5 list price across
> four to six sessions. At the cycle README's §8 rate that is about 3.3 to 7.4
> points of the all-model gauge, *converted, not read* (L-18). The basis is
> 0.3.1, which spent `$101.85` over 683 requests and six sessions against an
> estimate of `$50` to `$90` ([its measured cost block](../done/0.3.1.md)).
> This subcycle rewrites the parses of three checks, changes two grammars, adds
> three classes and a command, and replays the corpus again, so it is sized at
> 0.3.1's measured cost and above it, not at 0.3.1's estimate. Nothing runs on
> Fable (L-17). **Record the closing cost as L-18 says**: the owner's opening
> and closing readings, split by his arithmetic (non-Fable = all-model − ½
> Fable), with this subcycle's own list-price dollars from the machine meter
> beside them, converted at `$21.70` to `$26.90` per point and labelled
> *converted*. 0.3.1's close found the band's low end a little low for its
> span: its readings met the meter only at `$22.25` a point or more.
>
> **The opening reading is 0.3.1's closing reading:** 14:38:24 EDT on
> 2026-09-24, all-model 31 %, Fable 46 % (0.3.1's cost block). This
> subcycle's own spend is `claude-skills-devTeam_s34`'s requests after that
> reading. Its two requests before it (`$0.34`) were counted in 0.3.1's close,
> and `_s33`'s requests after it are 0.3.1's close, not this subcycle's.

> **What this subcycle is for.** 0.3.1 made every check say what it did not
> read. On the corpus that is five parts in `check_trace`, four in
> `check_refs` and two in every task's `check_report`. Its corpus replay found
> three families of correct commits that the gate refuses because a check reads
> the wrong thing: restarts over edits the manager made while a task was
> stopped (14 commits), requirements honestly left `open` over a closed task
> (3), and every planning commit on a board written as the template or as
> `pricelog` writes it. This subcycle reads what those parts name, fixes the
> windows the register measured wrong, and closes the three joints 0.3.1's
> gate met. It changes what the checks parse and which history they judge.
> The contract, the gate and the hook stay as 0.3.1 built them.

## 1. Where you are

- **The tree.** `claude-skills` at or after the commit that adds this file.
  Plugin `0.2.0-rc`. CLI `2.1.281`. Both measured 2026-09-24 at `c408e4c`,
  0.3.1's close:
  - `python3 plugins/devteam/scripts/check_plugin.py` →
    `devteam plugin: clean  [15 skills, 9 agents, 63 rules, 6 root entries]`.
  - `python3 plugins/devteam/scripts/run_controls.py` → `all 19 controls
    green`, in 1 min 44 s. The controls this subcycle changes hold 114 cases
    (`check_trace`), 95 (`check_refs`), 65 (`check_report`), 55
    (`check_scope`), 62 (`check_plugin`), 59 (`gate`) and 68 (`result`).

  Re-run both before you start, and record what they print.
- **The live corpus** is `pricelog` at `a88990b`. 0.3.1's clone,
  `.internal/scratch/0.3.1/pricelog`, is at that commit and untouched. Read
  it, and copy it for any case that writes. Never read or run anything in
  `pricelog` itself. On the clone today (`MEASURED` 2026-09-24):

  | Check | Exit | Prints |
  |---|---|---|
  | `check_trace` | 1 | `2 finding(s), 5 part(s) not evaluated  [3 goals, 16 requirements, 19 tasks]`. The findings are F-85's pair, T-1's `missing-field` and T-10's `one-sided-link`. The parts are *BOARD.md's task rows* (19 of 20 rows), the T-18 step audit's name (0.3.3's), T-18's `Because.` and `Discharges.`, and T-19's `Depends on.` |
  | `check_refs` | 3 | `not evaluated (4 part(s))  [47 files, 415 declared, 321 cited]`: the four gate audits' finding headings, 0.3.3's (F-99) |
  | `check_scope` | 0 | `scopes: clean  [19 tasks, 0 live]` |
  | `check_report`, T-1 to T-19 | 3, and 1 for T-12 | each `not evaluated (2 part(s))`: `budget-mismatch` and `model-mismatch`, because a clone carries no `devteam/.run/`. T-12 also reports its `status-mismatch`, a corpus fact (0.3.1 §3.1) |

- **A fresh project**, `setup.py` run into an empty repository and the
  scaffold committed as its client would commit it: `check_trace` exits 1 with
  four `unparseable-protected-path` findings, with `--pre-plan` or without it;
  `check_refs` reads `clean  [11 files, 0 declared, 0 cited]`; `check_scope`
  reads `scopes: clean  [0 tasks, 0 live]` (`MEASURED` 2026-09-24, in
  `.internal/scratch/0.3.2/fresh`). `check_plugin`'s `template-ships-a-finding`
  runs `check_refs` alone on its scaffold, which is why nothing has reported
  the four.
- **The commit guard** (0.3.1 §3.6) runs in every session started after it
  entered `hooks/hooks.json`. It refuses a commit in a devteam project made any
  way but `gate.py commit`. The clone and every fixture with a `devteam/` are
  devteam projects. Make fixture commits from Python, as `test_gate.py` does,
  because the hook reads Bash commands only; or make them through `gate.py`. To
  learn whether it runs in your session, use the `setup` skill's probe,
  `git -C <fixture> commit -m probe -- devteam/no-such-file`: a refusal means
  it runs, and git's own pathspec error means it does not. `claude-skills` is
  not a devteam project, so commits here are unaffected.
- **Read first**, in this order:
  - the cycle README's §2, the 0.3.2 row of §5 whole, §6 (L-5, L-6, L-9 and
    L-18) and §9;
  - [0.3.1](../done/0.3.1.md) whole. Its L-1.1 to L-1.9 bind here. Its
    Findings are this subcycle's worklist: §3.2's inventory and corpus table,
    §3.4's identity, and §3.5's class table, replay and *Found at the joints*;
  - from the [register](../../v3-findings-register-2026-09-19.md), theme 2
    and area A6 whole, and three rows elsewhere: A8's *Estimates were not
    checked against their own step lists*, A9's *Version 18 landed without
    moving the charter's header* and A12's *The checkpoints' verification tally
    counts grep matches*;
  - the `pricelog` entries each step below names, read from the clone;
  - [`FORMATS.md`](../../../templates/FORMATS.md) §"Status vocabularies",
    §"The liveness files under `.run/locks/`", §"The REPORT block", §"What
    each check reads" and §"Accepted findings";
    [`CHECKS.md`](../../../docs/CHECKS.md); PROTOCOL P-4, P-11, P-13, P-18,
    P-41, P-48, P-50 and P-51;
  - the [`check` skill](../../../skills/check/SKILL.md), whose *Until 0.3.2*
    section this subcycle retires; the `plan` skill's paragraphs on committing
    the plan; the `checkpoint` skill's verification tally;
  - `check_trace.py`, `check_report.py`, `check_scope.py`, `check_refs.py`'s
    `VOCAB`, `result.py` and `gate.py`.
- **Where the work runs.** Tracked changes go under `plugins/devteam/`.
  Scratch goes in `.internal/scratch/0.3.2/`, which is gitignored. 0.3.1's
  scratch tools in `.internal/scratch/0.3.1/` serve this subcycle too:
  `mutants.py` and `mutant_sets.py` (each mutation on its own copy of the whole
  plugin), `replay.py` (the corpus replay), `accept_cases.py` and
  `examples_3_7.py`. Copy any you change into `0.3.2/`.
- **The owner is needed twice:**
  1. at the start, for §2's two open decisions, put to him as options before
     the steps they govern are built — **answered 2026-09-24** (L-2.4,
     L-2.5);
  2. at the close, for one whole `/usage` paste from the workstation. Ask him
     to paste it whenever he is there, and take its time from
     `~/.claude/history.jsonl`.

## 2. Decisions (settled — see [`README.md`](../0.3/README.md) §6)

L-5, L-6, L-9 and L-18 bind, and so do 0.3.1's L-1.1 to L-1.9: the four
results, excluded against not evaluated, zero rows and partial reads,
untracked files, the gate and its one allowance, accepted findings, and the
hook. Do not re-litigate them here. Local to this subcycle:

- **L-2.1 — the board is read as its grammar writes it.** (F-70, and 0.3.1
  §3.2's in-flight row)
  - `board-drift` reads rows from the `## Tasks` table only, and finds the
    `State` column by the table's header row. `BOARD_ROW` reads every table on
    the board and takes each row's last cell as its state, so `pricelog`'s
    in-flight row for T-19 was compared with its `Note` as though that were a
    state (0.3.1 §3.2).
  - A row's first cell may name its task bare, as a link, in bold or in
    backticks: the four forms `BOARD_ROW_ISH` already offers. `pricelog` writes
    the link form, a bracketed `T-n` followed by a parenthesised path into
    `tasks/` (F-70, RECORD.md:942), and the template writes backticks,
    `` | `T-1` | … | ``. A state's bold and
    backticks are stripped, because `pricelog` writes `**DONE**`.
  - **A state outside FORMATS' board vocabulary is a finding,
    `bad-board-state`.** Today `board-drift` compares only states it knows
    (`if allowed and …`), so an unknown state compares nothing and says
    nothing (L-6). FORMATS' vocabulary gains `ACCEPTED (<date>, D-n)`, which
    the board template's legend and `BOARD_PHASES` already have and FORMATS
    lacks.
  - A board with no `## Tasks` section, or a Tasks table with no `State`
    column, is a part not evaluated (L-1.3).

  *Declined:* adding `STOPPED` to the vocabulary now. `pricelog`'s T-16 and
  T-19 read `**STOPPED (D-67)** — …`, a state its manager wrote because the
  pipeline had none for a stopped run (F-141). 0.3.6 designs the `cancelled`
  and `stopped` states the guard reads. Until then both rows are findings,
  real per the vocabulary.
- **L-2.2 — while a claim is held, `CLAIMED` allows every title its
  supervisor writes.** (0.3.1 §3.5, the first joint) Under `CLAIMED`, with the
  task's row in the in-flight table, the title may read RUNNING or PLANNED, as
  today, or any state a supervisor writes when it closes or stops: DONE,
  READY-TO-AUDIT, NEEDS-DECISION or BLOCKED. Without the in-flight row, only
  RUNNING or PLANNED.
  - *Why.* The manager moves the board only after the verifier returns (P-13,
    P-18), and the title is the supervisor's line (`BOARD_PHASES`' own
    comment). So a supervisor's close lands with `CLAIMED` on the board every
    time. 0.3.1 §3.5 wrote this joint for DONE, which is F-19's window. A
    supervisor that stops NEEDS-DECISION meets the same joint: its own commit
    would be refused over a board it may not edit (P-13).
  - Planted both ways, with the row present, and with the row removed and
    nothing else changed. That is the run's own mutation (RECORD.md:487).
  - This is `check_trace`'s relation between two vocabularies, which already
    allows PLANNED under `CLAIMED`. It is not a gate branch: the gate's one
    allowance stays as L-1.5 built it.
  - *Settled by:* planning, open to the owner's overruling.

  *Declined:* DONE only, which refuses a supervisor's own stop; and a second
  allowance in the gate, which would put a board rule in the gate.
- **L-2.3 — a field a check reads as identifiers holds identifiers only, and
  every field is read whole.** (F-132, F-95, and L-1.3's wrapped fields)
  - `Discharges.`, `Depends on.` and `Informs.` hold `none` or bare
    identifiers separated by commas, and may continue onto indented lines. One
    function reads them, and every class that reads one uses it (P-34). Today
    `Discharges.` is read by regex in `unmotivated-task` and by cutting at
    commas in the `one-sided-link` checks and the owner lists (F-132,
    RECORD.md:1675; `check_trace.py` line 875 against lines 976, 1119, 1133
    and 1140 at `c408e4c`).
  - A piece that is not a bare identifier is not read as one. The field is
    then a part not evaluated, and the part names the piece (L-1.3).
    Explanation goes on a bullet of its own, as T-8's file carries it and as
    the record's repair moved T-19's (RECORD.md:1675).
  - Every field a check reads is read across its continuation lines, in all
    four project checks. 0.3.1 made each wrapped read visible and left this
    here (L-1.3: *"Reading continuation lines is 0.3.2's work, with F-132"*).
    The sites are rows 8, 9, 15, 18, 19, 23 and 29 of 0.3.1's inventory; row
    23, `check_report`'s `status:`, is built with the report grammar in §3.5.

  *Declined:* identifiers by regex over the whole field, the record's first
  remedy for F-132 (RECORD.md:1675). It carries F-95's defect to
  `Discharges.`: every requirement a sentence mentions becomes one the task
  claims. T-17's `Discharges. none — **this task takes no discharge.** It
  re-establishes R-4's acceptance …` would discharge R-4, which the sentence
  says it does not.
- **L-2.4 — two requirement statuses and a task field, for the states the
  run had no words for.** *Settled by:* owner, 2026-09-24, choosing *"Two
  statuses + a field (Recommended)"* over *"Register's three statuses"* and
  *"Keep the vocabulary"*.

  0.3.1 §3.5's second false-positive family is three states the vocabulary
  cannot say (`MEASURED` on the clone). At `63674de`, R-4 went `open` over
  T-9's close because three named residuals remained (RECORD.md:712–713). At
  `ef8ee45`, R-10 went `open` because *"the judgement is the client's"*
  (RECORD.md:820). At `3bcf1f2`, R-4 went `open` because T-10's fix created a
  new violation. Separately, T-18's file counts five tasks that re-established
  R-4's acceptance, itself included, and T-8 re-established R-2's
  (RECORD.md:495). T-17 and T-18 wrote that as prose inside `Discharges.`, and
  T-8 on a bullet of its own.

  - `partly-discharged (T-n…; D-n)` — the tasks that discharged part of it,
    and the decision that records what remains;
  - `awaiting-judgement (T-n…; Q-n)` — built and evidenced, and the discharge
    waits on the question that asks the client;
  - on a task, `Re-establishes.` — bare requirement identifiers whose
    acceptance the task re-establishes without taking the discharge. The
    requirement keeps its `discharged (T-n)`, as T-17's and T-18's files say
    in prose, and the field is the task's motivation for `unmotivated-task`.

  `one-sided-link` accepts a closed task's requirement in either new status
  when the status names the task, and `check_refs`' vocabulary accepts both. A
  parenthetical holds identifiers only, as every status's does, so what
  remains is recorded in the decision or the question.

  *Declined:*
  - *the register's three statuses*, `re-established (T-n)` among them, which
    replaces the requirement's discharging task with the one that
    re-established it;
  - *keep the vocabulary*, with an acceptance by decision as the standing
    remedy, which is 0.3.1's interim remedy made permanent.
- **L-2.5 — a restarted task is judged by its current claim.** (F-135)
  *Settled by:* owner, 2026-09-24, choosing *"Current claim only
  (Recommended)"* over *"Current claim, list stops"* and *"Keep the first
  claim"*.

  `misattributed-write` opens a live task's window at its first claim. Its
  code chose that deliberately: a newest-claim window let a write made while a
  task was stopped escape every run, because it was never live and in window
  at any one moment (`check_scope.py`, the comment above `candidates`). F-135
  measured the cost. The manager's edits to a stopped task's files, made
  before its re-dispatch as `run` §4.3 asks, read as the task's
  `misattributed-write` once it restarts: three at T-19's restart
  (RECORD.md:1916), and 14 refusals in 0.3.1's replay.

  **What changed since that choice.** The blind spot's mechanism was a sweep:
  `git add -A` taking a file the task had left. 0.3.1's gate refuses a path
  naming the whole repository, and its hook refuses every commit made around
  the gate. A commit into a stopped task's scope now names its paths.

  **So the window is the current claim's.** It opens at the first commit in
  HEAD's history whose board carries the task's current claim label, read
  from its title's `RUNNING (since <date>, <label>)`. A commit made while the
  task was stopped is judged by nothing here, and CHECKS.md says so, as it
  says of a stranger's commit outside every live scope.

  *Declined:*
  - *the current claim, and the stop's commits named*: the same window, and
    the restarted task's run also lists every commit into its scope made while
    it was stopped, printed and never refusing;
  - *keep the first claim*, with an acceptance by decision at every restart
    until 0.3.4's `re-claim` records the manager's pre-dispatch commits.

  **The anchor, settled by planning:** the claim label, never a commit
  subject. `pricelog`'s claims are written `board: claim
  T-3 and T-2`, `plan T-17 and claim T-15`, `board: claim T-9 (re-dispatch)`,
  `board: re-claim T-18 as T18-refute-0928` and `board: T-5 restarted with Q-1
  answered`, so any subject pattern misses some (`MEASURED` on the clone). The
  manager writes the label on the board at the claim, and the supervisor
  writes it into the title (the claim protocol in the `BOARD.md` template;
  FORMATS' task title).
- **L-2.6 — history is HEAD's history.** (0.3.1 §3.5, the second joint)
  `undeclared-write`, `unknown-commit` and `head-subject` read `git log --all`,
  so a commit on another branch counts for a task. A promotion that stops on a
  conflict also leaves its fetched ref under `refs/devteam/sandbox/` for
  inspection (`sandbox.py`), so an unpromoted worker's subject resolves under
  `--all`. All three read the history of HEAD instead, which at the gate is
  the commit being judged. A commit a report cites by hash must be an ancestor
  of HEAD, not merely an object, because a refused gate candidate is an object
  in the repository. Workers cite their commits by subject, because promotion
  rewrites every hash (`sandbox.py`, the comment above `SUBJECT`), so the
  ancestry test reaches only commits a supervisor or the manager cites. That
  is `REASONED`, and §3.4 measures it on the replay.
- **L-2.7 — `check_report` judges every step's latest block, each against its
  own run.** (F-34, F-37, F-88, F-103, F-109, F-136)
  - `check_report . T-n` judges the task's latest task-level block and, for
    each step the file reports, that step's latest block. `check_report . T-9`
    read clean while `check_report . T-9.S-3` read `unknown-commit` on the same
    tree, because the unscoped form reads one block (F-37, RECORD.md:579). An
    earlier block for the same step is a superseded attempt, and is not
    judged.
  - A REPORT header may carry an annotation after its identifier —
    `REPORT implementer T-6.S-4 (ATTEMPT 2, correcting attempt 1's FAILED
    verification)` — and is read as that step's block (F-34, RECORD.md:543). A
    key may carry one before its colon — `checks (all run by the
    supervisor…):` — and is read as the key (F-88, RECORD.md:1106).
  - Each finding's message names the block it is about, so two blocks'
    findings are two identities.
  - **The harness comparison is made for a step's block, against the meter
    the `.sandbox` line names for that step.** A task-level block is a
    supervisor's, and the harness meters no supervisor, so the comparison is
    excluded for it by that declaration, and the exclusion is named (L-1.2).
    F-109 was a correct Sonnet supervisor's report marked for re-dispatch
    against its last worker's Opus meter (RECORD.md:1236). A step whose block
    the recorded meter does not belong to is a part not evaluated, naming the
    step: F-103 compared S-2's block with S-4's meter (RECORD.md:1169), and
    F-136's stale block against the current meter is the same case
    (RECORD.md:1917). Keeping every step's meter past its sandbox's close is
    0.3.7's inventory (F-9), and the part names what is missing until then.

  *Declined:*
  - *judging every block in the file.* A superseded attempt's block would
    stand as a finding for ever, because a failed attempt's commits are never
    promoted and its report cites commits no history holds. A permanent exit 1
    is what taught managers to commit over red (F-78, F-85, F-95);
  - *the per-step form in `run` §6 only*, the register's remedy, which leaves
    the command the gate runs blind to the steps (L-5).
- **L-2.8 — a report says it was reconstructed, and a hedged figure is
  compared.** (F-86, F-32)
  - `status:` may carry one qualifier, `(reconstructed: <by whom, and why>)`,
    as in `status: DONE (reconstructed: by the supervisor from the worker's
    commits; the worker died)`. It is read, and the line names the block as
    reconstructed. *"A check that refuses an honest label and accepts a less
    informative one is selecting against disclosure"* (RECORD.md:1095): `53326f6`
    moved the run's `DONE -- RECONSTRUCTED` into `notes:` so that the parser
    would accept the report. The parenthetical is the idiom the grammar already
    uses for a title status.
  - `tokens=~N` and `minutes=~N` are read as `N`, marked approximate, and
    compared at the same tolerance, and the finding says the figure was marked
    approximate. Four workers wrote `~N` and were out by 36, 24, 15.6 and 16.6
    times, and none was flagged (F-32, RECORD.md:475).
- **L-2.9 — the amendment check reads the latest entry, and only it.** (F-36,
  F-68, N-4, and A9's header)
  - The latest entry runs to the next entry heading or the section's end.
    `last = tail[pick:]` runs to the end of the file, so the check scored an
    older entry's list as the latest's (F-36, RECORD.md:563), and on a charter
    with two or more amendments it could not fire at all (F-68,
    RECORD.md:922).
  - An entry whose list opens with anything but `- **Re-affirmed.**` is a part
    not evaluated, naming the entry. F-36's entry opened with `**Every other
    row, checked rather than assumed:**`.
  - `Re-affirmed.` accepts `added (this entry)` for a condition the entry
    adds. The vocabulary had no word for an addition. Four authors invented
    one before the record separated that cause from the three authors who
    wrote a sentence where a token belongs (N-4, RECORD.md:938, 970–971).
  - **The header's `Version.` must equal the newest entry's number:
    `stale-version-header`.** Version 18 landed without moving the header,
    which was *"bookkeeping no check reads"* (RECORD.md:1350).
- **L-2.10 — an estimate's step count agrees with the task's steps:
  `estimate-step-mismatch`.** (A8) The template's `Estimate.` states its
  model, `model=<steps>x440000x1.78+150000` (P-41). The check compares that
  count with the task's `S-n` lines under `## Steps`, struck ones included,
  because a struck step was estimated. T-12 and T-16 each had four steps and
  were estimated for three, understating by 783,200 tokens apiece, and only an
  arithmetic check caught it (RECORD.md:946). A task with no `## Steps` is
  compared with nothing and is clean, because the `plan` skill lets a
  supervisor write the steps. An estimate that states no model, on a task that
  has steps, is a part not evaluated.
- **L-2.11 — a fresh project reads clean in every plan check, and the control
  checks all three.** (0.3.1 §3.2, *Found, pre-existing*)
  - The Protected paths cell is tested whole for a placeholder before it is
    split. The template's unfilled cell is one `<…>` with commas inside it,
    and it is split first, so its prose reads as four paths.
  - `template-ships-a-finding` scaffolds a project, commits it, and runs
    `check_trace --pre-plan`, `check_refs` and `check_scope`. Each must be
    clean. Today it runs `check_refs` alone. A control that did not look is
    the same defect as a check that did not look (0.3.1 §3.1).
  - `guard.py` is unchanged. It splits the unfilled cell and drops only the
    pieces holding `<` or `>`, so it reads a few entries that match nothing
    and protect nothing, as an unfilled cell should (`REASONED`, from
    `load_state`).
- **L-2.12 — checkpoint tallies are a command.** (A12, L-5)
  `scripts/tally.py <project> [--at <commit>]` counts the verdict entries in
  `RECORD.md`: the list items beginning `` `verify <label> PASS|FAIL` ``, the
  entry form the RECORD template defines. It prints the counts per task, split
  into task verdicts and step verdicts, and it uses the result contract: a
  verify-shaped line it cannot read makes it exit 3, naming the line. The
  `checkpoint` skill calls it and quotes its line, in place of its two
  `grep -coE` commands. Those counted a hypothetical at RECORD.md:422 and a
  step verifier at :684 as manager verdicts, so C-5's `11 PASS` was 9 by the
  verdict entries (0.2.9's Findings, *The checkpoints' verification tally
  counts grep matches*).
- **L-2.13 — `check_plugin` reads the paths the plugin's Python files cite.**
  (the cycle README's §4.8) `broken-link` reads the relative links in the
  plugin's documents and nothing in its scripts. Two scripts cite files that
  moved under `done/`: `test_sandbox_probe.py:17` cites
  `meta/roadmap/0.2/0.2.0.md`, and `test_bg_session_probe.py:19` cites
  `meta/roadmap/0.3/0.3.0.md` (`MEASURED` 2026-09-24; §4.8 found the first).
  A backticked path under the plugin's own directories — `meta/`, `docs/`,
  `templates/`, `skills/`, `agents/`, `scripts/`, `hooks/` — in a script's
  docstring or comment must resolve.
- **L-2.14 — changed identities are listed, and nothing else is renamed.** A
  finding's identity is its check, class, anchor file and message
  (`result.identity`, 0.3.1 §3.4), and a part's is its name. So a changed
  message or part name makes an acceptance of the old one stale, and the gate
  prints the change as *fixed here* plus an addition. Keep byte-identical every
  message this subcycle does not mean to change. Record each identity that
  changes under Findings, with its class. No project but the corpus exists, and
  no run happens before 0.3.14 (L-8), so the stale acceptances this makes are
  measured on the corpus and nowhere else.
- **L-2.15 — what 0.3.2 does not do**, so that nothing falls between
  subcycles:
  - the audit namespace, and the audit file's name that
    `open-finding-at-close` reads — 0.3.3's (F-99, F-104, F-112, and
    `pricelog`'s T-18 step audit);
  - keeping a step's meter past its sandbox's close — 0.3.5's and 0.3.7's
    (F-9);
  - the board's and the guard's stop states — 0.3.6's (F-141);
  - `re-claim` as a command — 0.3.4's (F-39);
  - a `Re-affirmed.` verdict that can say *in force, currently broken* —
    0.3.9's;
  - the hook's and the guard's limits — 0.3.11's.

## 3. The work

Seven steps. Each is committed on its own, with `run_controls` green and
`check_plugin` clean, so that a rotation can fall between any two. In each:

- the planted case comes out as expected before the real result is believed
  (P-35b);
- every new assertion is shown to fail on a mutant of a copy of the whole
  plugin (`mutants.py`);
- the commit body names the register rows it closes (the cycle README's §9);
- the documents are brought level for what the step changes — CHECKS.md's
  rows, FORMATS, the templates, and the skill text that describes it. A class
  with no CHECKS.md row fails the plugin (`unruled-finding`), and a skill that
  describes the old parse is a second home, now wrong (P-34).

### 3.1 The board (L-2.1, L-2.2)

Build L-2.1 and L-2.2 in `check_trace`. `in_flight()` stays the one reader
of the in-flight table, for the gate's allowance and for L-2.2.

**Planted cases:**
- **F-70.** `pricelog`'s link rows are read, and a link row whose state
  disagrees with its task's title fires `board-drift`.
- **F-107.** A board reading DONE over a title reading NEEDS-DECISION fires
  `board-drift`: T-12's three days (RECORD.md:1181).
- **The in-flight row.** A board whose in-flight table names T-19, with a
  `Note` in its last cell, reports nothing about that row.
- **L-2.2, both ways.** `CLAIMED` over a DONE title, with the in-flight row
  present: silent. The row removed, nothing else changed: `board-drift`. The
  same pair for NEEDS-DECISION.
- **An unknown state.** `STOPPED (D-67)` fires `bad-board-state`.
- **L-1.3.** A non-empty Tasks table that parses to no row, one row the
  grammar rejects among rows it reads, and a board with no `## Tasks` section
  are each named as not evaluated.

**False-positive cases:** the template's backticked form and the bare form
read as the link form does; a bold or backticked state reads as a bare one; a
fresh project's empty Tasks table is clean; `CLAIMED` over PLANNED stays legal
(the `BOARD_PHASES` comment's case).

**At the gate.** `test_gate`'s fixtures use the template's backticked rows
partly because `board-drift` is silent on them today (0.3.1 §3.5). Give them
link rows too. Plant a supervisor's close and a supervisor's stop through the
gate in F-19's window: both land, and both are refused with the in-flight row
removed. The plan commit on a template-shaped board lands with no acceptance.

**Documents.** Retire the `plan` skill's acceptance procedure for *BOARD.md's
task rows*, and the third bullet of the `check` skill's *Until 0.3.2*. Execute
the plan commit's example as written: `examples_3_7.py`'s plan commit, without
the acceptance.

**Measure on the corpus:** `check_trace` on the clone, before and after. The
*BOARD.md's task rows* part is expected to go, and T-16's and T-19's `STOPPED`
to report `bad-board-state`. Record any other disagreement between the board
and a title as a corpus fact.

### 3.2 Identifier fields, and the requirement states (L-2.3, L-2.4)

Build L-2.3's one reading, and the continuation reads in `check_trace`,
`check_refs`' vocabulary fields and dispositions, and `check_scope`'s inline
`Scope.`. Then build L-2.4: its two statuses in `one-sided-link` and in
`check_refs`' `VOCAB`, and `Re-establishes.` in `check_trace`, and all of it
in FORMATS and the REQUIREMENTS and TASK templates. The TASK
template shows where explanation goes, because it invited prose in exactly the
field the parser treats as a list (F-95, RECORD.md:1137).

**Planted cases:**
- **F-132.** T-19's first line, `R-6, R-2 — **completed here, advanced by
  T-3, and both are`: the part names the piece that is not an identifier, and
  no class reads `R-2 — **completed here` as a requirement. With the prose on
  its own bullet, as the record repaired it, both links are two-sided.
- **F-95.** D-31's repair, a sentence naming T-17 inside T-17's own `Depends
  on.` (RECORD.md:1137): no edge, and the piece is named. A prose mention of
  an unrelated task adds no edge. The record read that case from the parser
  and never ran it, so run it here. A real cycle between two bare lists still
  fires `dependency-cycle`.
- **Continuation.** A `Discharges.` of bare identifiers over two lines is read
  whole, and is clean. So is a requirement `Status.` continuing onto a second
  line, in `check_trace` and in `check_refs`.
- **L-2.4's three.** Each of the requirement states at `63674de`, `ef8ee45`
  and `3bcf1f2`, written in L-2.4's vocabulary over the closed task, raises
  no `one-sided-link`. Written as plain `open`, each is still refused. A status
  in the new vocabulary naming a task that does not list the requirement fires
  `one-sided-link`'s requirement-side message, unchanged.
- **Re-establishment.** T-17's shape, `Re-establishes. R-4` over
  R-4 `discharged (T-6)`, raises nothing, and `unmotivated-task` is silent for
  that task. A `Re-establishes.` naming no requirement fires
  `unknown-reference`.

**False-positive cases:** `none`; a genuinely empty field; the fresh project.

**Measure on the corpus:** `check_trace` and `check_refs` on the clone, before
and after. T-18's and T-19's three wrapped parts are expected to go. Read
whole, T-18's `Discharges.` and T-19's `Depends on.` hold prose, so each
becomes a part naming its prose instead. The corpus's own requirements use the
old vocabulary, so its honest states stay refused on the clone; §3.7 plants
them in the new vocabulary.

### 3.3 The amendment, the estimate and the fresh project (L-2.9, L-2.10, L-2.11)

**Planted cases:**
- **F-68.** A charter with three amendment entries, newest first, whose latest
  omits a condition the older ones re-affirm: `amendment-omits-condition` names
  the omission, and no older list is read. The hygiene audit's measurement is
  the model: Version 11's `Licence`, `Repository`, `Public?` and `Budget
  ceiling` were recorded as sourced from Version 2 (RECORD.md:922).
- **F-36.** The latest entry's list opens `**Every other row, checked rather
  than assumed:**`. The entry is named as not evaluated, and nothing is
  reported from the previous entry's list, whose count of 29 was what gave
  F-36 away (RECORD.md:563).
- **N-4.** Version 13's `DM-14` and `DM-15` as `added (this entry)` are read.
  An `added` item naming a condition the charter does not have fires
  `amendment-names-unknown`.
- **Version 18.** A header reading `Version. 17` over a newest entry numbered
  18 fires `stale-version-header`. Equal numbers are clean, and so is a
  Version 1 charter with no entry.
- **T-12 and T-16.** Four `S-n` lines under an estimate of `model=3x…` fire
  `estimate-step-mismatch`. Equal counts are clean, and so is a task with no
  `## Steps`. An estimate with no `model=`, on a task with steps, is a part not
  evaluated.
- **The fresh project.** `check_trace`, with `--pre-plan` and without, is
  clean on a committed scaffold. `template-ships-a-finding` fires on a copy of
  the plugin whose charter template carries a filled Protected paths cell
  written as prose, and does not fire on the real template.

**Measure on the corpus:** `check_trace` on the clone. `pricelog`'s charter
reads `Version. 26` over 25 entries (`MEASURED`), so F-68's bound changes which
list is read. Record what the latest entry's list yields, whether the header
agrees (its Version 18 correction is at RECORD.md:1355), and every
`estimate-step-mismatch`, each against RECORD.md:946 and 1015.

### 3.4 The claim window, and HEAD's history (L-2.5, L-2.6)

Build one home for a task's current claim: its label, read from the title's
`RUNNING (since <date>, <label>)`, and the first commit in HEAD's history at
which the board's in-flight table carries that label in its label column. It
goes in `result.py` or a small module beside it. `check_scope` and `gate.py`
read it, and nothing else computes it (P-34). Then build L-2.5's window, and
L-2.6.

**Planted cases:**
- **F-135.** T-19's restart. `95bcb33`, `4f56a7a` and `8ba93ad` precede its
  re-claim, `7a87cf8` (`MEASURED` on the clone: 15:21, 19:28, 19:31 and 19:34
  on 2026-09-18), and none of the three is judged. A commit naming no task, into T-19's scope, after `7a87cf8` and
  before its worker's, is still `misattributed-write`: that is the class's
  purpose.
- **The anchors `pricelog` wrote.** A claim written `board: claim T-3 and T-2`,
  and one written `plan T-17 and claim T-15`, each anchor at their commit.
- **No anchor.** A RUNNING title with no label, and a label no board commit
  carries, are each a part not evaluated, as today's no-anchor case is.
- **L-2.6.** A commit on another branch whose subject begins `T-n:` counts for
  neither `undeclared-write` nor `head-subject`. A report citing a subject
  found only on another branch, or only under `refs/devteam/sandbox/`, fires
  `unknown-commit`, and so does a report citing a refused gate candidate by
  hash.

**False-positive cases:** a task claimed once behaves as today; another task's
prefixed commit is skipped, as today; a report citing its own commit by
subject still lands through the gate
(`fp-a-report-naming-its-own-commit-by-subject-lands`).

**Measure on the replay:** 0.3.1 §3.5's 14 restart commits would commit. Every
RUNNING state in the replay finds its anchor, or the step records each that
does not. Every hash a report in the corpus cites is an ancestor of the commit
that cites it, or the step records each that is not, and why.

**Documents.** Retire the first bullet of the `check` skill's *Until 0.3.2*.
CHECKS.md's `misattributed-write` row states the window, and what the window
does not see.

### 3.5 What `check_report` reads (L-2.7, L-2.8)

**Planted cases:**
- **F-34.** An annotated header is read as that step's latest block, attempt
  1's block is not judged, and the part 0.3.1 names for this shape is gone.
- **F-37.** A malformed S-3 block followed by a well-formed task-level block:
  `check_report . T-n` reports S-3's finding, naming S-3.
- **F-88.** `checks (all run by the supervisor…):` is read as `checks:`, and
  no field after it is reported missing.
- **F-86.** `status: DONE (reconstructed: …)` is read, and the line names the
  block as reconstructed. The run's own `DONE -- RECONSTRUCTED` still fires
  `bad-report-status`, and the message shows the grammar's form.
- **F-32.** The four figures from RECORD.md:475 each fire `budget-mismatch`,
  advisory, saying the figure was marked approximate. A `~N` within tolerance
  is clean.
- **F-103.** With the `.sandbox` line naming S-4, S-2's block is not compared,
  and the part names S-2. S-4's block is compared with S-4's meter.
- **F-109.** A task-level block whose model differs from the last worker's
  meter raises no `model-mismatch`, and the line names the exclusion.
- **F-136.** A restarted task whose latest task-level block is the previous
  supervisor's: that block is never compared with the current meter.

**False-positive cases:** a step block and the task block with the same
defect are two findings, each naming its block; a superseded attempt citing a
commit never promoted is not judged; a task with no task-level block yet has
its steps judged, and nothing is compared with its title.

**Measure on the corpus:** `check_report` for T-1 to T-19 on the clone, before
and after. Record each finding newly visible in a step block, which F-37's
reading hid, as a corpus fact. In the replay, the F-34 headers (`07d67b9`,
`c6d1f82`, `13d51e4`) and `7aa90ac`'s reconstructed report (F-86, F-88) either
commit or are refused for a reason the record explains.

**Documents.** FORMATS §"The REPORT block" gains the annotation, the annotated
key, the qualifier, `~N`, and which blocks are judged. The `work` and
`supervise` skills each show one copyable example of every form, which is the
register's remedy for N-4's second cause (A6).

### 3.6 The tally, and the plugin's own citations (L-2.12, L-2.13)

**Planted cases:**
- **A12.** `RECORD.md` as it stood at C-5 (`2b8f0de`): `tally.py` counts the
  verdict entries, where the checkpoint's grep counted 11 PASS. 0.2.9's
  Findings measured 9 PASS and 3 FAIL there, and 8 PASS and 3 FAIL at C-4;
  re-measure both. The checkpoints attributed the three FAILs to T-9 twice and
  T-7 once while calling them three different tasks, and their own grep
  matched T-4, T-9 and T-7. Record what the per-task split says.
- A verify-shaped entry the grammar does not read makes it exit 3, naming the
  line. A record with no verdict yet is clean, with its zero shown.
- **§4.8.** On the plugin as it stands, `broken-link` fires for
  `test_sandbox_probe.py:17` and `test_bg_session_probe.py:19`. Then both
  citations are corrected. A citation in a script that resolves is silent, and
  a URL is not read.

**Documents.** The `checkpoint` skill calls `tally.py` and quotes its line in
the checkpoint.

### 3.7 The replay, the documents and the skills

- **The replay.** Run 0.3.1's `replay.py` with this subcycle's checks: every
  `pricelog` commit rebuilt as the gate's candidate on its parent, 591 of
  them, with no ref left but HEAD (0.3.1 §3.5's harness artifact). Expected:
  F-135's 14 would commit; the link-shaped board no longer adds a part at
  `8f67cc0`; and the three honest states, planted in L-2.4's vocabulary on
  copies of their commits, would commit. Classify every new refusal as real
  or a false positive, in a table like 0.3.1's, and answer every false
  positive before the step closes. List every identity that changed (L-2.14),
  with the pairs it made.
- **The documents.** FORMATS §"What each check reads" says what each check
  now reads. The `check` skill's *Until 0.3.2* section is gone, and no
  document in the plugin still says *until 0.3.2*, outside this cycle's
  roadmap. CHECKS.md has a row for every new class.
- **Every documented example is executed as written**, the owner's rule: each
  command and each grammar example this subcycle adds or changes is run in a
  scratch project and shown to do what the text says.

## 4. The decision rule

- The owner's answers settle L-2.4 and L-2.5 before §3.2 and §3.4 are built.
  Both were answered on 2026-09-24 and are recorded in §2.
- If the claim label cannot anchor every RUNNING state in the replay, record
  each miss. Then anchor on the first commit at which the board's Tasks table
  reads `CLAIMED` for the task after its last state that was not `CLAIMED`: a
  structural read, never a subject. Record why.
- If L-2.7's latest-block rule leaves a corpus refusal the record does not
  explain, record it and answer it before §3.5 closes.
- If L-2.6's ancestry test refuses a hash that a correct report cites, record
  the case, keep the object test for it, and name the gap.
- If a parse turns out to need a grammar change that no decision here covers,
  put it to the owner as options before building it.
- If a part the corpus reports is owned by no subcycle, add it to the owning
  subcycle's row in the cycle README's §5, with the reason.

## 5. Done when

- On the clone, no part is left not evaluated for a reason this subcycle owns.
  What remains is 0.3.3's (the audits) and the harness gaps that 0.3.7 closes,
  each named.
- The replay lets through the three families 0.3.1 §3.5 found: the restarts,
  the honest states planted in L-2.4's vocabulary, and the link-shaped board.
  Every refusal is classified under Findings.
- What L-2.4 and L-2.5 chose is built.
- Each item of the cycle README's §5 row for 0.3.2 is built, or recorded here
  as moved, with the owner's decision.
- Every new class has a CHECKS.md row, cases both ways and killed mutations.
  `python3 plugins/devteam/scripts/run_controls.py` is green, with the new
  cases counted, `check_plugin.py` is clean, and `mutate.py` exits 0.
- A fresh project reads clean in all three plan checks, and
  `template-ships-a-finding` checks all three.
- The `check` skill's *Until 0.3.2* is gone.
- Each commit body names the register rows it closes.
- This file's title reads `DONE (<date>)`, and the file has moved to
  [`../done/`](../done/).
- The closing cost is recorded as L-18 says.

## 6. Do not

- Add a branch to the gate. L-2.2 is `check_trace`'s relation between two
  vocabularies, and the gate's one allowance stays as L-1.5 built it.
- Tune a check until the corpus passes. A corpus refusal is classified and
  answered, never made to disappear (P-35b).
- Write into `pricelog`, or run anything in it. Use the clone, and copies of
  it.
- Build what L-2.15 leaves to a later subcycle.
- Edit a protocol rule in place (P-23).
- Change `guard.py` or `commit_guard.py`, whose limits are 0.3.11's.

## Findings

*Each step records here what it measured before the next step starts.*
