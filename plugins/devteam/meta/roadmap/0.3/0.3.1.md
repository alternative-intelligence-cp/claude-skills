# 0.3.1 — the check contract: clean means looked, and a commit is checked before it is made — PLANNED

> **Cost, `REASONED`.** About `$50` to `$90` of Opus 5.5 list price across two
> or three sessions. At the cycle README's §8 rate that is about 2 to 4 points
> of the all-model gauge, *converted, not read* (L-18). The basis is 0.3.0,
> which spent `$50.68` over 394 requests ([its measured cost
> block](../done/0.3.0.md)). This subcycle changes all five checks, adds a
> gate and touches most skills, and the cycle README's §8 already expected
> 0.3.1 to touch more files than any 0.2 subcycle. Nothing runs on Fable
> (L-17). **Record the closing cost as L-18 says**: the owner's opening and
> closing readings, split by his arithmetic (non-Fable = all-model − ½ Fable),
> with this subcycle's own list-price dollars from the machine meter beside
> them, converted at $21.70 to $26.90 per point and labelled *converted*.
>
> **The opening reading is 0.3.0's reading 4:** 07:59:19 EDT on 2026-09-24,
> all-model 17 %, Fable 26 % (`.internal/scratch/0.3.0/usage/reading-4.txt`).
> This subcycle's own spend starts at 08:16:26 EDT, in
> `claude-skills-devTeam_s29`, whose six earlier requests were counted in
> 0.3.0's close. The meter's by-session split separates what other sessions
> spent in between: the compiler on Fable, and `nitpick-libs` on Opus 5.5.

> **What this subcycle is for.** The run measured two failures of the checks
> (the register's themes 2 and 3). First, checks said *clean* without having
> looked. Second, commits went in over red, because nothing tied a check's
> answer to the commit. Every later subcycle builds its checks to the contract
> written here, and 0.3.4's `commit` primitive is the gate built here. So this
> is infrastructure: it changes what the checks *report* and *where they sit*.
> What each check parses is left to 0.3.2.

## 1. Where you are

- **The tree.** `claude-skills` at or after the commit that adds this file.
  Plugin `0.2.0-rc`. CLI `2.1.281`. Both measured 2026-09-24:
  - `python3 plugins/devteam/scripts/check_plugin.py` →
    `devteam plugin: clean  [15 skills, 9 agents, 48 rules, 6 root entries]`.
  - `python3 plugins/devteam/scripts/run_controls.py` → `all 16 controls
    green`. The check controls hold 77 cases (`check_trace`), 65
    (`check_refs`), 44 (`check_report`), 35 (`check_scope`) and 43
    (`check_plugin`).

  Re-run all three before you start, and record what they print.
- **The live corpus** (the cycle README's §9) is `pricelog` at `a88990b`.
  Clone it (`git clone --local ~/Workspace/REPOS/pricelog
  .internal/scratch/0.3.1/pricelog`) and run everything against the clone.
  The original is read-only, and even `git status` can rewrite an index. On
  the clone today (`MEASURED` 2026-09-24):

  | Check | Exit | Prints |
  |---|---|---|
  | `check_trace` | 1 | `2 finding(s)  [3 goals, 16 requirements, 19 tasks]`. They are T-1's `missing-field` and T-10's `one-sided-link`, the two the run accepted and committed over (F-85) |
  | `check_refs` | 0 | `clean` |
  | `check_scope` | 0 | `scopes clean`. No task title reads `RUNNING` (17 are `DONE`, and T-16 and T-19 are `NEEDS-DECISION`), so its live-scope classes compared nothing. This is RECORD.md:86's case: *silent because nothing is running* |

  `board-drift` also compares nothing on this board, because the first cell
  of every row is a link (F-70). So a `clean` that did not look exists in the
  corpus today.
- **Read first**, in this order:
  - the cycle README's §2, §5 (the rows for 0.3.1 to 0.3.4), §6 (L-5, L-6,
    L-9 and L-18) and §9;
  - from the register, themes 2 and 3; its *What worked* bullets for the gate
    and the lock commits (RECORD.md:1112–1113, 1139, 1379 and 1601); area A6
    whole; and area A1's rows for F-24/F-117 and F-26;
  - the 0.2.9 entries those rows cite — not all 3,400 lines — and each step
    below names its own;
  - CONSOLIDATION §8a and §8b; `docs/CHECKS.md`; `templates/FORMATS.md`
    §"What each check reads"; PROTOCOL P-18, P-19, P-20, P-35 and P-35b;
  - `skills/check/SKILL.md` and the five check scripts;
  - [`../done/0.2.6.md`](../done/0.2.6.md), the last subcycle that changed
    every check.
- **Where the work runs.** Tracked changes go under `plugins/devteam/`.
  Scratch goes in `.internal/scratch/0.3.1/`, which is gitignored. `pricelog`
  and its record are read-only inputs, cited by file and line as the register
  cites them.
- **The owner is needed twice:**
  1. at the start, for §2's two open decisions, put to him as options before
     any step is built — **answered 2026-09-24** (L-1.6, L-1.7);
  2. at the close, for one whole `/usage` paste from the workstation. Ask him
     to paste it whenever he is there, and take its time from
     `~/.claude/history.jsonl`.

## 2. Decisions (settled — see [`README.md`](README.md) §6)

L-5, L-6, L-9 and L-18 bind; do not re-litigate them here. Local to this
subcycle:

- **L-1.1 — four results, four exit codes, one line.** Every check ends in
  exactly one of:

  | Result | Exit | Means |
  |---|---|---|
  | `clean` | 0 | everything the check owns was evaluated against everything it exists to read, and nothing was found |
  | `findings` | 1 | at least one finding |
  | `could not run` | 2 | unchanged: the check could not start — its arguments, no repository, no `devteam/` |
  | `not evaluated` | 3 | no finding, and at least one part the check did not evaluate, each named with its reason |

  - A run with findings *and* parts not evaluated exits 1, and its line names
    both.
  - Every result line carries its denominators — what was read, and how many
    rows of each kind — and names every part not evaluated.
  - Every check takes `--json`, with one schema for all of them, and the gate
    reads that. The line is rendered from the same object, so the two cannot
    disagree.

  **Why 3 and not 2** (L-6 leaves this choice here). Exit 2 means the tool
  cannot answer, and the remedy is in the invocation. Exit 3 means the tool
  answered about the project and names what it could not read, and the remedy
  is in the project. One code for two remedies sends one of them to the wrong
  place. In 0.2.6, `unreadable` and `not-utf8` were moved to exit 2
  (`docs/CHECKS.md`, *Withdrawals*). Each is a project file the check could
  not read, which is the project's state, so both move to 3.

  **Why 1 over 3 when both hold.** A finding is definite, and the line also
  names the parts not looked at, so nobody reads a partial finding set as the
  whole.

  *Declined:* sharing exit 2 with *could not run*; and letting
  `not evaluated` outrank findings.
- **L-1.2 — excluded is not the same as not evaluated.** A class can be
  excluded by a declaration: a flag the caller passed (`--pre-plan`), or a
  project declaration the check reads itself (the charter's
  `Containment: guard-only`, under which there is no harness meter). An
  excluded class is named in the result line and does not change the exit
  code. Anything else the check did not evaluate is `not evaluated`.

  *Why:* onboarding's gate must still be able to exit 0 with `--pre-plan`.
  And FORMATS' *"an absent measurement is not a finding"* stays true without
  reading as clean. The difference is who said so. An exclusion has a
  declaration behind it that a reader can check; a gap has none.

  F-32's T-4 case is a gap, so it is `not evaluated`. The comparator was
  missing on a `structural` project because the sandboxes had been closed
  (RECORD.md:356), and no declaration says it should be.
- **L-1.3 — zero rows from a non-empty source is `not evaluated`, and so is a
  partial read.** Each parse site counts two things: the rows its source
  offers, by a loose shape (a table line, a list item, a field line), and the
  rows its grammar accepts.
  - Rows offered that do not parse make the site `not evaluated`, and each
    unparsed row is named by file and line.
  - A source that is genuinely empty is `clean`, with its zero shown in the
    denominators.
  - A field whose value continues past its first physical line, where the
    check reads only the first line, is `not evaluated`, naming the field.
    This is the cycle README's §9 wrapped-field case.

  Reading continuation lines is 0.3.2's work, with F-132. 0.3.1 makes each gap
  visible and fixes no individual parse.
- **L-1.4 — the checks read untracked files, and report each one.** Every
  check that enumerates `devteam/` reads untracked, non-ignored files as well
  as tracked ones. Each untracked file is reported as a finding of its own
  class. This is F-131's first remedy, which the register chose *"because it
  turns the miss into a finding rather than a step someone must remember"*.
  Ignored files, `devteam/.run/` among them, stay invisible.
- **L-1.5 — the gate checks the commit it is about to make, before it
  exists.** `gate.py commit` takes a message and named paths, then:
  1. builds the candidate commit, without moving any ref: HEAD's tree with
     those paths' working-tree content, HEAD as parent, and the message given;
  2. evaluates every tree- and history-derived class in a temporary worktree
     at the candidate, and the same at HEAD;
  3. commits **only if the candidate adds no finding and no part not
     evaluated** beyond HEAD's, net of accepted findings (L-1.6) and the one
     allowance below;
  4. commits the named paths, and checks that the commit it made has the
     candidate's tree and the old HEAD as its parent. It refuses if HEAD moved
     in between. This is the lock-commit guard that held from the ninth move
     on (register, *What worked*; RECORD.md:1379).

  Working-state classes — `foreign-write`, `dirty-tree` and the new
  untracked-file class — are read from the live checkout and printed. Of
  those, only an untracked file under `devteam/` that the commit does not
  name refuses the commit, as the run's gate did (RECORD.md:432).

  The gate refuses on its own failures too: a check that exits 2, or output
  the gate cannot parse, counts as red (RECORD.md:430, 1139).

  - **Why the candidate.** It makes structural the property CONSOLIDATION §8b
    names: *the tree being committed was not the tree that had been checked*.
    It also evaluates F-114's history-derived checks against the history the
    commit will create, so that failure is refused before it lands instead of
    reported after. **This deviates from the map's wording, and is declared
    here.** The map says *"a post-commit re-run of history-derived checks
    (F-114)"*, which is the record's remedy (RECORD.md:1359–1360). Refusing
    before the commit catches the same finding one step earlier. If §3.5's
    planted cases show the candidate cannot be evaluated reliably, build the
    map's post-commit re-run instead, and record why under Findings.
  - **Why a ratchet** (F-12's scoping). A finding already present at HEAD does
    not refuse an unrelated commit, so a concurrent task's red file cannot
    block it (RECORD.md:99, :164). A commit that adds any finding anywhere is
    refused, even one whose finding lands in a file it did not touch — such as
    a declaration removed while another file still cites it. Every gate run
    prints the standing findings.

    *Declined:* requiring every check to be green project-wide, which
    deadlocked twice in the run; and scoping to the commit's own files, which
    misses exactly that removed declaration.
  - **The one allowance.** A `one-sided-link` does not refuse when its
    requirement reads `in-progress (T-n)` and T-n is in the board's in-flight
    table. That is F-19's window, keyed the way the run's gate keyed it after
    it failed closed twice (RECORD.md:486–487). It is the only permissive
    branch. Its control shows it refusing with the row removed and allowing
    with the row present. It is removed when 0.3.4's `close` makes the window
    impossible.

    *Narrowed by the owner, 2026-09-24,* choosing *"F-19's window only
    (Recommended)"* over *"Both shapes, as written"*. The allowance covers
    `check_trace`'s task-side message for a task whose title reads DONE, and
    nothing else. A requirement naming a task that does not list it is a plan
    that disagrees with itself, never a window, and the run's gate refused one
    correctly (RECORD.md:430).

    *Two narrowings of where `check_report` is judged sit beside it.* §3.5's
    corpus replay found them, and the owner kept both on 2026-09-24, choosing
    *"Keep both (Recommended)"*. Each is planted both ways. See Findings,
    §3.5.
  - **Onboarding.** `gate.py commit --pre-plan` passes the flag to
    `check_trace`. The gate refuses the flag once the candidate holds any task
    file, so it cannot hide an uncovered requirement after planning.

    *Amended by the owner, 2026-09-24,* choosing *"Hold back new ones only
    (Recommended)"* over *"Accept by decision"* and *"One commit for both"*.
    The rule above made `iterate`'s charter-gate commit impossible. The client
    signs the amended charter before the plan, so its new requirements are
    committed uncovered while tasks already exist (pricelog `34f778d`).
    `--pre-plan` now holds back `uncovered-requirement` for a requirement the
    commit adds, and for none that HEAD already has. The gate does not pass the
    flag to `check_trace`, and a requirement that loses its task is still
    refused.
- **L-1.6 — an accepted finding is a decision, classed like any other.**
  *Settled by:* owner, 2026-09-24, choosing *"A decision, classed as usual
  (Recommended)"* over *"You, every time"* and *"The manager, with a
  reason"*.
  - A decision carries an **`Accepts.`** field that names the findings it
    accepts, by check, class, anchor file and message.
  - The check reports those findings as accepted, names them in its line, and
    exits 0 when nothing else is found.
  - The decision's P-26 class decides who makes it. The manager decides
    `REVERSIBLE` acceptances alone, recorded as unreviewed under P-27. The
    client decides `CHARTER` ones.
  - An acceptance whose finding no longer fires is itself a finding
    (`stale-acceptance`), so the count returns to zero and stays a signal
    (CONSOLIDATION §8a).
  - An `Accepts.` line outside the grammar accepts nothing, and is reported.
  - Superseding the decision withdraws the acceptance (P-23).
  - A part not evaluated can be accepted the same way — for example, a check
    that stays blind until 0.3.2 fixes its parse.

  *Declined:*
  - *the client accepts every finding* — the strongest guard against quiet
    suppression, at the price of an answer for every acceptance;
  - *a baseline line with a reason and no decision behind it*, which is the
    *suppress it when the tree is annoying* reading that §8a warns about.
- **L-1.7 — a plugin hook refuses a direct commit.** *Settled by:* owner,
  2026-09-24, choosing *"Plugin hook refuses it (Recommended)"* over *"Git
  hook in each project"* and *"Gate command only"*. A small `PreToolUse`
  script beside `guard.py` refuses `git commit` in a devteam project from an
  agent session outside a sandbox, unless the commit runs through `gate.py`.
  The script sits beside `guard.py`, not inside it, as 0.2.10's `root_guard.py`
  does. The owner's own terminal is untouched. Workers in a sandbox are
  exempt, because promotion gates them (P-44).

  *Declined:*
  - *A git hook that `setup` installs in each project.* This is the record's
    own remedy (RECORD.md:1003). It also gates the owner's commits, is skipped
    by `--no-verify`, has to coexist with the project's own hooks, and has to
    find the plugin again after every plugin update.
  - *The gate command only*, with nothing refusing a direct commit until
    0.3.4 or 0.3.11. This is the form the run measured failing: six commits
    used the `git add -A` form that `run` §4.5 forbids by name (A1).
- **L-1.8 — 0.3.1 builds 0.3.4's `commit` primitive, and 0.3.2 and 0.3.3 keep
  their rows.** `gate.py commit` is 0.3.4's *commit named paths on a green
  gate*, so 0.3.4 adopts it instead of building a second one.

  So that nothing falls between subcycles, 0.3.1 does **not**:
  - fix any single parse — that is 0.3.2's: `board-drift`'s link rows (F-70,
    with F-107), F-132, F-34, F-37, F-88, and F-32's `~n`;
  - key a check to the claim window — also 0.3.2's (F-135, F-136);
  - build the ledger — 0.3.3's, which may absorb L-1.6's acceptances into its
    lines;
  - rewrite `run` and `resume` beyond sending their commits through the gate —
    0.3.4's.
- **L-1.9 — the rules.** Each new class names a rule in `docs/CHECKS.md`, or
  `unruled-finding` fails the plugin (0.2.6). Three rules are added, each
  numbered when it is written into PROTOCOL.md at the next free number:
  1. *a check never reports clean when it did not look* (L-6, written as a
     rule);
  2. *a commit to `devteam/` is checked before it is made* (L-1.5);
  3. *an accepted finding is a decision* (L-1.6).

  A rule these change is superseded by a new number, never edited (P-23).

## 3. The work

Seven steps. Each is committed on its own, with `run_controls` green and
`check_plugin` clean, so that a rotation can fall between any two. Each step's
planted case must come out as expected before its real result is believed
(P-35b).

### 3.1 The result contract in every check (L-1.1, L-1.2)

Write one shared module for the result object, the exit codes, the line and
`--json` — one home for all four (P-34). `check_trace`, `check_refs`,
`check_report`, `check_scope` and `check_plugin` all use it. No check changes
what it parses yet.

- `check_trace --pre-plan` becomes a named exclusion.
- `check_report`'s harness classes become named exclusions on a `guard-only`
  project. On a `structural` project whose `.sandbox` root or
  `meta/budget.json` is missing, they are `not evaluated` (F-32's T-4 case,
  RECORD.md:356).
- `unreadable` and `not-utf8` move from exit 2 to exit 3.
- `--blocking-only` stays a filter on the verdict, and still prints every
  advisory finding (CHECKS.md).

**The joint to watch.** `check_plugin`'s `unruled-finding` finds each check's
classes by parsing its source for bare `add(...)` and `findings.append((...))`
calls (CHECKS.md, *Why the class list here is machine-derived*). If the shared
module changes how a class is emitted, that parser must still find every one.
Otherwise it goes blind and still reports clean, which is the failure this
subcycle exists to end. Keep the emit form, or change the parser in the same
commit.

**Planted cases:**
- a check with one part not evaluated and no finding exits 3, and names the
  part;
- with a finding as well, it exits 1 and names both;
- `--json` and the line agree on the same run;
- `--pre-plan` exits 0, with the exclusion named in the line;
- a class emitted through the new path with no CHECKS.md row makes
  `unruled-finding` fire.

**Measure on the corpus:** record each check's line and exit on the
`pricelog` clone, before and after, under Findings.

### 3.2 Zero rows, partial reads and wrapped fields (L-1.3)

First, make an inventory of every parse site: the check, the source, the loose
shape, the strict grammar, and whether the source can legitimately be empty.
The inventory goes under Findings, because 0.3.2 works from it. Then apply
L-1.3 at every site.

**Planted cases**, one of each per check that parses rows. Each must refuse
to report clean:
- a non-empty source that parses to zero rows;
- one unparsed row among parsed ones;
- a wrapped field.

**False-positive cases**, which must stay clean:
- a genuinely empty source. A fresh `setup` project must still report clean,
  so `template-ships-a-finding` stays green (CHECKS.md);
- a multi-line list field that the check does read.

**Measure on the corpus:** every part that the `pricelog` clone now reports
as `not evaluated`, with its reason. `board-drift` is expected to be one of
them (F-70). **Fix none of them here** (L-1.8). Record each with the
subcycle that owns it. If no subcycle owns one, add it to the cycle README's
§5 row of the subcycle it belongs to, with the reason.

### 3.3 Untracked files (L-1.4)

Enumerate with `git ls-files --cached --others --exclude-standard`, and add
the untracked-file class.

**Planted case: F-131's own** (RECORD.md:1641–1642). `tasks/T-19.md` is
untracked while another file cites T-19. Its declaration is read, so there is
no `cited-undefined`, and the untracked-file class names the file. An
untracked task file that nothing cites is now reported too. Today, such a
file *"is not checked at all, and the three checks report clean"*
(RECORD.md:1642).

**False-positive case:** a file under `devteam/.run/`, which is ignored,
produces nothing.

### 3.4 Accepted findings (L-1.6)

Build the `Accepts.` field on a decision in `DECISIONS.md`:
- its grammar, in FORMATS;
- its parse. An unparseable acceptance accepts nothing, because the run's
  parser failed closed (RECORD.md:430);
- matching, by the identity §3.5 also uses: check, class, anchor file and
  message, without line numbers;
- `stale-acceptance`.

**Planted cases**, on the `pricelog` clone, with its two findings accepted by
a decision:
- `check_trace` exits 0, and names both findings as accepted;
- a third finding makes it exit 1;
- an edit that shifts the anchors' line numbers does not un-accept them;
- an acceptance whose finding has been fixed reports `stale-acceptance`;
- an acceptance outside the grammar accepts nothing, and is reported.

### 3.5 The gate (L-1.5)

Build `scripts/gate.py` and its control. The gate reads the checks' `--json`,
never their text.

First, classify every class of the four project checks as tree-derived,
history-derived or working-state-derived, from what its code reads. The table
goes under Findings. It decides where each class is evaluated.

**Planted cases**, each one of the run's own failures where there is one:
- **F-114.** A commit that would give a requirement its third revision, which
  `re-litigated-requirement` counts over committed history
  (RECORD.md:1339–1340), is refused, and HEAD has not moved.
- **F-12.** A red finding at HEAD in a concurrent task's file lets an
  unrelated commit through. A commit that adds a finding in a file it did not
  touch, by removing a declaration another file cites, is refused.
- **§8b.** A defect in an uncommitted `devteam/` file that the commit does
  not name does not refuse the commit. The same defect in a named path does.
- **F-131 at the gate.** A commit of `BOARD.md` citing an untracked T-19 is
  refused, naming the file. With `tasks/T-19.md` named in the commit too, it
  goes through.
- **F-19's allowance, both ways.** With T-n in the in-flight table, the commit
  is allowed. With the row removed and nothing else changed, it is refused.
  This is the run's own mutation (RECORD.md:487).
- **F-24 and F-117.** A red result exits 1 with HEAD and the index untouched,
  and there is no second command left to chain.
- **Failing closed.** A check that exits 2 refuses, and so does `--json` the
  gate cannot parse.
- **HEAD moved** between the evaluation and the commit: refused.
- **The commit made is the commit checked.** Its tree equals the candidate's,
  and its parent is the old HEAD.

**The corpus, replayed.** In the clone, rebuild the candidates of real
`pricelog` commits — each commit's tree and message on its parent — and run
the gate on each. It must refuse the commits the record says went in over red:
the `check_refs` commit at RECORD.md:1001–1002, and the `one-sided-link`
window at RECORD.md:1112–1113. Record every refusal the record does not
explain as a false positive, and answer each before this step closes.

**If the candidate cannot be evaluated reliably**, build the map's post-commit
re-run instead (L-1.5), and record why.

### 3.6 Enforcement (L-1.7)

Build the hook script beside `guard.py`, register it in the plugin's
`hooks/hooks.json`, and give it a control. More than a third of the control's
cases must be false-positive controls, because *a guard that refuses
legitimate work gets disabled by whoever it obstructs* (P-35).

A direct `git commit` in a devteam project from outside a sandbox is refused.
All of these must pass:
- the same commit through `gate.py`;
- a commit in a repository with no `devteam/`;
- a worker inside a sandbox, with `DEVTEAM_SANDBOX` set;
- `git commit --help`;
- `git log`, `git status` and `git diff`.

**Then watch it act once, live.** A green control proves the script, never the
deployment (the check skill, *Before you trust a clean run*).

### 3.7 The documents and the skills

- **FORMATS** §"What each check reads" states the contract: the four results,
  the exit codes, `--json`, and untracked files.
- **CHECKS.md** gets a row for each new class, and a section for the gate's
  refusals, as it has one for `sandbox.py promote`'s classes.
- **PROTOCOL** gets L-1.9's rules.
- **The `check` skill** is rewritten to the contract. It points to CHECKS.md
  instead of keeping its own partial tables, which have drifted: they omit
  most of `check_trace`'s 21 classes, and still list `not-utf8`, which 0.2.6
  withdrew.
- **Every skill that states the exit codes** points to the check skill
  instead (P-34): onboard, plan, run, resume, supervise, verify, work,
  checkpoint, status and audit.
- **Every skill that commits into `devteam/`** commits through `gate.py`.
- **`setup`** scaffolds whatever §3.4 adds, and a fresh project still reports
  clean.

## 4. The decision rule

- The owner's answers settle L-1.6 and L-1.7 before §3.4 and §3.6 are built.
  Both were answered on 2026-09-24 and are recorded in §2.
- If §3.5 cannot evaluate the candidate reliably, build the map's post-commit
  re-run instead, and record why.
- If §3.2's corpus run leaves a check `not evaluated` for a reason no later
  subcycle owns, add it to the owning subcycle's row in the cycle README's §5,
  with the reason. Do not fix it here.

## 5. Done when

- Every check ends in one of L-1.1's four results, with its denominators, and
  takes `--json`.
- Each check's control plants every case below, and demands that none is
  reported clean (L-9, and the cycle README's §9):
  - a part not evaluated;
  - zero rows;
  - a partial read;
  - an untracked file;
  - a wrapped field, where the check reads fields.
- `gate.py` exists with its control, and §3.5's planted cases and corpus
  replay are recorded under Findings.
- What L-1.6 and L-1.7 chose is built.
- CHECKS.md, FORMATS.md, PROTOCOL.md and the skills state what the code does.
  `unruled-finding` and `stale-row` are clean, and every skill's commit form
  is the gate.
- `python3 plugins/devteam/scripts/run_controls.py` is green, with the new
  controls counted, and `check_plugin.py` is clean.
- Each commit body names the register rows it closes (the cycle README's §9).
- This file's title reads `DONE (<date>)`, and the file has moved to
  [`../done/`](../done/).
- The closing cost is recorded as L-18 says.

## 6. Do not

- Fix a parse that 0.3.2 owns, even where §3.2 makes it visible (L-1.8).
- Write into `pricelog`, or run anything in it. Use the clone.
- Add a permissive branch to the gate beyond L-1.5's one allowance without
  showing it refuse (RECORD.md:487) and recording it here.
- Edit a protocol rule in place (P-23).
- Change `guard.py` itself (L-1.7). The hook sits beside it and may import
  from it, as `root_guard.py` does.

## Findings

*Each step records here what it measured before the next step starts.*

### §3.1 as built — every check ends in one contract

`MEASURED` 2026-09-24. `scripts/result.py` is the contract's one home. All
five checks end in it, each takes `--json`, and `check_refs`' undecodable
file moved from exit 2 to 3. What each check parses is unchanged.

**On the corpus, before and after** (the `pricelog` clone at `a88990b`):

| Check | Before (the committed scripts) | After |
|---|---|---|
| `check_trace` | exit 1, `2 finding(s)  [3 goals, 16 requirements, 19 tasks]` | identical |
| `check_refs` | exit 0, `clean` | exit 0, `clean  [47 files, 415 declared, 321 cited]` |
| `check_scope` | exit 0, `scopes clean` | exit 0, `scopes: clean  [19 tasks, 0 live]` |
| `check_report`, T-1 to T-19 | 18 tasks exit 0 `clean`; T-12 exit 1 | 18 tasks exit 3, `not evaluated (2 part(s))`; T-12 exit 1, with the same finding and the two gaps |

The 18 `clean`s were F-32's T-4 case, 18 times over. A clone carries no
`devteam/.run/`, because it is ignored, so no report had a meter to compare
against, and the check said `clean`. It now names the gap: *"no
devteam/.run/locks/T-n.sandbox names a sandbox to compare against"*.

**A consequence to carry.** On a `structural` project, every task whose
sandbox has been closed will read `not evaluated` for the harness classes,
until 0.3.5 and 0.3.7 keep the meter outside the sandbox (F-9). No run
happens before then (L-8), so this blocks nothing now. It is pressure on
0.3.7 to keep the meter.

**The controls.**

| Control | Cases before → after |
|---|---|
| `check_trace` | 77 → 77. The `--pre-plan` cases now assert that the exclusion is named |
| `check_refs` | 65 → 67. A non-UTF-8 file is planted, checked as a line and as `--json` |
| `check_report` | 44 → 46. Three `fp-…-is-silent` cases became planted not-evaluated cases, and a `guard-only` exclusion and an undeclared containment were added. The false-positive share went from 50 % to 43 % |
| `check_scope` | 35 → 35 |
| `check_plugin` | 43 → 43, now asserting which parts each fixture cannot exercise |
| `test_result.py` | new, 20 cases |

All 17 controls are green. Each new assertion was shown to fail, on a mutant
of a *copy* of the plugin:
- `check_refs` put back to exit 2 fails both new cases;
- `result.py` reporting a gap as clean fails 4 cases;
- `result.py` with its precedence swapped fails 3 cases;
- `check_trace` not naming its exclusion fails the 2 `--pre-plan` cases.

**The joint held.** `unruled-finding` and `stale-row` are still clean, so every
CHECKS.md row still has an emitter and the class scanner still sees every
class. `Result.finding`, called with a computed class, is not matched by the
scanner, and did not trip its tripwire.

**Found: `check_plugin`'s own control never ran two parts of the check.** No
fixture carries a `setup.py`, so the template checks were skipped in all 43
cases. Every case that did not build the README's table also skipped the
root-table checks. Each skip printed as `SKIPPED` inside a `clean` line, exit
0, so the control proved *clean* for parts it never ran.
`template-ships-a-finding` and `template-scaffold-fails` are planted in no
control at all: a search of every `test_*.py` finds neither name. The control
now states and asserts what its fixture cannot exercise. Planting the two
classes needs a fixture that can scaffold a project, and it is done with
§3.2's control work, because a control that did not look is the same defect
as a check that did not look.

**Found: a missing template is a silent zero in `check_trace`.** Its template
rows are read with `CHARTER_ROWS = template_names("CHARTER.md", "row") or []`.
A mutant run from a copy of `scripts/` without `templates/` beside it made
`template-drift` and `unparseable-protected-path` evaluate nothing and report
clean. This goes in §3.2's inventory.

**Unchanged: T-12's `status-mismatch`.** Its last REPORT block says
`NEEDS-DECISION`, while its title reads `DONE (2026-09-14, after D-27
answered its CHARTER question)`. The finding is the same before and after.
It is a corpus fact, and not 0.3.1's to change.

**The documents that state the exit codes as law** now say 3: FORMATS
§"What each check reads", the `check` skill's opening, and CHECKS.md's
withdrawal note. The rest of the skills are §3.7's.

### §3.2 as built — zero rows, partial reads and wrapped fields

`MEASURED` 2026-09-24. Every parse site in the five checks now counts what its
source offers against what its grammar accepts (L-1.3). An offered row the
grammar rejects is named by file and line, and a field a check reads that
continues past its first line is named. Either makes the site `not
evaluated`. What each check parses is unchanged: every gap below is a parse
0.3.2 or 0.3.3 owns, and none is fixed here (L-1.8). `result.py` holds the
two shared rules, what continues a list item (`continuation`) and how a
missed row is named (`anchors`, `unparsed`, `wrapped`), and each check keeps
its own loose shapes, because each reads a different grammar.

**The inventory**, which 0.3.2 works from. *Offered* is the loose shape and
*accepted* the grammar that was already there. A site that can be
legitimately empty is clean when it offers nothing.

| # | Check | Source | Offered | Accepted | Empty is legitimate? | Not evaluated when |
|---|---|---|---|---|---|---|
| 1 | `check_trace` | the plugin's `templates/` | the template file | `template_names` | no | a template cannot be read: `template-drift`, `missing-field in requirements`, `missing-field in tasks`. This was _s29's carried item 2: `CHARTER_ROWS … or []` |
| 2 | `check_trace` | `CHARTER.md`, `## Goals` | a list item or table row in the section | `GOAL` | yes, before onboarding | a row is not `- **G-n** —`; or there is no section and no goal |
| 3 | `check_trace` | `CHARTER.md` | a table row whose first cell says Protected paths | `guard.PROTECTED_ROW` | no (`template-drift` covers a missing row) | `guard.py` cannot be imported; a row the guard cannot read |
| 4 | `check_trace` | `CHARTER.md`, `## Done means` | a list item | `DM_DECL` | yes | a row is not `- **DM-n** —`, and an amendment entry exists |
| 5 | `check_trace` | `CHARTER.md`, `## Constraints` | a table row, not a separator | `TPL_ROW` | no | a row is rejected, and an amendment entry exists |
| 6 | `check_trace` | the latest amendment onward (F-36's window, unchanged) | an indented item under `- **Re-affirmed.**` | `REAFFIRM_ITEM` | yes | an item is rejected, or comes after the parse stopped |
| 7 | `check_trace` | `REQUIREMENTS.md` | a heading beginning `R-n` that is not a possessive or dotted mention | `REQ` | yes | a heading is rejected; or the file is missing |
| 8 | `check_trace` | each requirement and task block | a list item naming a field the check reads, however decorated | `FIELD`, under that name | — | a field is named and not parsed, and not parsed anywhere else in the block |
| 9 | `check_trace` | each requirement and task block | a field line with continuation lines | its first line | — | a field continues past its first line and some class read it (F-132) |
| 10 | `check_trace` | `Scope.`, `Requires-write.` | an indented list item under the field | `LIST_ITEM` | yes, while PLANNED | an item is rejected |
| 11 | `check_trace` | `tasks/*.md`, tracked | a level-1 heading beginning `T-n` that is not a step or possessive | `TASK` | yes, before planning | a second title-shaped line. The whole-file case stays `unparseable-task` |
| 12 | `check_trace` | `BOARD.md` | a table row whose first cell names a task, bare, linked, bold or backticked | `BOARD_ROW` | yes | a row is rejected (F-70); or the file is missing |
| 13 | `check_trace` | `audits/` | a file naming a closed task and holding finding headings | `AUDIT_FILE` | yes | it is named outside `T-n-<dimension>-<date>.md` |
| 14 | `check_trace` | `audits/T-n-…md` of a closed task | a level-2 or 3 heading `Finding n`, `n.` or `ABC-n` | `AUDIT_HEADING` | yes | a heading is outside the five prefixes |
| 15 | `check_trace` | `git log` of `REQUIREMENTS.md` | every revision | `git show` | yes, if never committed | a shallow clone; a revision `git show` cannot produce; a wrapped `Statement.`, `Acceptance.` or `Shape reviewed.` in any revision |
| 16 | `check_refs` | every tracked artifact | a heading or bold list lead naming an identifier, in the file its kind is declared in | `DECLARATIONS` | yes | what it names is declared nowhere |
| 17 | `check_refs` | `audits/*.md` | as #14 | the five audit prefixes | yes | a heading is outside the namespace (F-99) |
| 18 | `check_refs` | the files `VOCAB` scopes | a list item naming `Status` or `Class`; a title-shaped line | the vocabulary's field pattern | yes | a field is named and not parsed; a `Status.` or `Class.` continues |
| 19 | `check_refs` | a finding block in an audit | a list item naming `Disposition` | `DISPOSITION`, one line | — | a disposition is named and not parsed, or continues |
| 20 | `check_report` | `tasks/T-n.md` | a level-1 heading beginning `T-n` | `TITLE` | no | none parses, where `status-mismatch` would compare |
| 21 | `check_report` | the same file | `REPORT <role> T-n…` naming this task | `HEADER` | no (no block is `no-report`) | one follows the block read (F-34) |
| 22 | `check_report` | the block read | a line beginning with one of the grammar's keys, annotated or not | `KEY`, or an indented continuation | — | a key not yet read appears at or after where the parse stopped (F-88) |
| 23 | `check_report` | `status:` | a status with continuation lines | its first element | — | it continues |
| 24 | `check_report` | the task's `Scope.` | an indented list item | `SCOPE_ITEM` | yes | an item is rejected, on a closing report |
| 25 | `check_report` | `budget:` | `tokens=` or `minutes=`, where the harness has a figure | a number | — | a figure is `~N` or absent (F-32) |
| 26 | `check_report` | `CHARTER.md` | a row whose first cell says Containment | `CONTAINMENT` | yes, and then nothing is excluded | a row is rejected |
| 27 | `check_scope` | `tasks/*.md` | a file named `T-*`, or holding a level-1 `T-n` heading | `TITLE` | yes | a task's title does not parse, which took it out of every comparison |
| 28 | `check_scope` | `Scope.` | an indented list item | `SCOPE_ITEM` | yes | unchanged: `unparseable-scope-entry`, a finding since 0.2 |
| 29 | `check_scope` | an inline `Scope.` value | a continuation line that is not a list item | the value, as one entry | — | it continues |
| 30 | `check_scope` | `git log` of the board and the task file | a live task | a claim-shaped subject, or `-S "RUNNING (since"` | no, for a live task | there is no anchor |
| 31 | `check_scope` | commit subjects | `T-n.S-m` without the colon | `^T-n(.S-m)?:` | yes | a step-shaped subject lacks its colon. A bare `T-n <words>` is the manager's, and is not offered |
| 32 | `check_plugin` | `PROTOCOL.md` | a line beginning `**P-n`, a letter allowed | `**P-n ` | no | its rule is declared nowhere |
| 33 | `check_plugin` | `docs/CHECKS.md` | a row whose first cell is a backticked name, under a heading naming a script | `CHECKS_ROW` under `CHECKS_HEADING` | no | a row is rejected, or sits under a source heading without backticks |
| 34 | `check_plugin` | `agents/*.md` frontmatter | `skills:` followed by indented or `- ` lines | `field()`, one line | yes | it is a block list |
| 35 | `check_plugin` | `templates/FORMATS.md`; `check_refs.py`'s sets | a row whose first cell is a backticked prefix; the three sets | `[A-Z]{1,3}-`; the set pattern | no | a row is rejected; either side is empty; either file is missing |
| 36 | `check_plugin` | `meta/roadmap/<cycle>/` | a file named `x.y.z…md` | `x.y.z.md` | yes | a file is named outside the grammar |
| 37 | `check_plugin` | `scripts/`, per `EMITTERS` | a script with rows in CHECKS.md | the file | — | the script is missing but its rows are not |
| 38 | `check_plugin` | `check_refs` on a fresh scaffold | its exit and its lines | a line it can read | no | it exits non-zero with nothing readable |
| 39 | `check_plugin` | the README's root table | a table row after the separator | `ROOT_ROW` | no | a row is rejected |

Links, leaks, citations and control characters in `check_refs`, rule
citations in `check_plugin`, and `commits:`, `checks:` and `model:` in
`check_report` are read on every line or joined whole, so they have no row
grammar to miss.

**On the corpus** (the `pricelog` clone at `a88990b`). These parts are newly
not evaluated. None is fixed here:

| Check | Part not evaluated | Why | Owner |
|---|---|---|---|
| `check_trace` | `BOARD.md`'s task rows | 19 of 20 rows name their task as a link to its file, not as a bare id. The 20th, T-19's in-flight row, is parsed from the wrong table | 0.3.2: F-70 with F-107. The in-flight row is now named in 0.3.2's §5 row |
| `check_trace` | `audits/pricelog-T-18-S-4-2026-09-17.md` | it names T-18 and holds 10 finding headings, but is not named `T-n-<dimension>-<date>.md` | 0.3.3: added to its §5 row |
| `check_trace` | T-18's `Because.` and `Discharges.`; T-19's `Depends on.` | each continues past its first line, which is all the check reads | 0.3.2: continuation lines, with F-132. T-19's continuation is F-95's prose edge |
| `check_refs` | the finding headings of the four gate audits (correctness 4, hygiene 5, safety 3, security 4) | `## Finding 1 (…)`, `### 1.` and `## F-1 — HIGH` are outside the namespace, so `undispositioned-finding` saw none of 16 findings | 0.3.3: F-99. The second half of the register's remedy, *report that instead of reporting clean*, is done here |

Lines on the clone, before this step and after:
- `check_trace` goes from exit 1, `2 finding(s)`, to exit 1, `2 finding(s), 5 part(s) not evaluated  [3 goals, 16 requirements, 19 tasks]`.
- `check_refs` goes from exit 0, `clean  [47 files, 415 declared, 321 cited]`, to exit 3, `not evaluated (4 part(s))`.
- `check_scope` is unchanged at `clean  [19 tasks, 0 live]`, and so is each task's run.
- `check_report` is unchanged beyond §3.1's harness gaps on T-1 to T-19.

A fresh `setup` project gains no part: `check_refs` still reads `clean  [11
files, 0 declared, 0 cited]`, so `template-ships-a-finding` stays green.

**Calibrated against the corpus, before believing it** (P-35b). Six loose
shapes first produced false gaps on real files. Each is now narrower, and
each has a false-positive twin shown to fail without the narrowing:
- **A step's heading is not a task title.** `# T-10.S-3 Adversarial Audit`
  sits in T-10's record.
- **A field mentioned in a record is not the field.** T-16.md:190 quotes
  `- **Scope:** check_scope … prints clean`. A mis-shaped field line is now a
  loss only where the field parsed nowhere in the block.
- **Prose after a finished REPORT block is not its fields.** T-6, T-11 and
  T-13 each have a `landing:` or an `anchor:` after the block. Only the
  grammar's own keys, not yet read, count.
- **A manager's topic commit is not a task's stray commit.** pricelog has five
  commits like `T-10 DONE, verified PASS -- …`, all the manager's, and the
  `T-n:` prefix exists to leave exactly those out. Only a step-shaped subject
  missing its colon is offered.
- **A narrative finding entry is not a declaration written wrong.** The record
  writes `- **F-70 — …**` beside the `- **F-70** —` line that declares it. A
  declaration shape is a loss only where what it names is declared nowhere.
- **My own pattern missed a shape.** `\d+\.\b` cannot match `### 1. …`,
  because a space is not a word character. So the hygiene audit's five
  findings were missing from the first measurement. Fixed before any figure
  above was taken.

**The controls.** Every new assertion was shown to fail, on a mutant of a
copy of the whole plugin. There were 74 mutations across the six controls
(the scratch runner, `.internal/scratch/0.3.1/mutants.py`, is not tracked).
All 74 are killed. Two survived at first, both in `check_scope`: my twin for
a step heading never reached the title shape, and no case had an inline scope
followed by list items. Each got a discriminating twin.

| Control | Cases before → after | Mutations killed |
|---|---|---|
| `check_trace` | 77 → 99 | 21 of 21 |
| `check_refs` | 67 → 79 | 10 of 10 |
| `check_report` | 46 → 59 | 11 of 11 |
| `check_scope` | 35 → 46 | 10 of 10 |
| `check_plugin` | 43 → 62 | 17 of 17 |
| `test_result.py` | 20 → 28 | 5 of 5 |

Each control plants a non-empty source parsing to zero rows, one unparsed row
among parsed ones, and a wrapped field where the check reads fields. Each
demands the part named and refuses clean. Each keeps a genuinely empty source
and a multi-line field the check does read clean. All 17 controls are green.

**Carried item 1, done: `check_plugin`'s control now runs the template
checks.** A fixture that can scaffold has to carry the real scaffolder, the
real templates, and every rule and script they cite. So it is the whole
plugin, copied under a throwaway repository root. It plants
`template-ships-a-finding` (`See R-99` in a template), `template-scaffold-fails`
(a `setup.py` that refuses), and a scaffold whose `check_refs` exits 2
saying nothing. It also gives the control its first clean case: every case
before it expected a part not evaluated.

**Found: two crashes, both introduced by step 3.1.** §3.1 made `check()`
return a tuple and `main()` unpack it. Two returns were left as bare lists:
- `check_report` on a project that is not a git repository;
- `check_scope` given a task id that names no task.

Each ended in a traceback, which is none of the four results. Both now exit 2,
as L-1.1's *could not run* says for no repository and a bad argument. Each
has a case. `check_scope`'s path returned a `no-file` class that has no row in
CHECKS.md. `unruled-finding` never saw it, because the class scanner reads
`add(...)` and `findings.append((…))`, not a literal returned in a list. No
such return remains, and the scanner's limit is recorded here.

**Found, and settled by the owner: `check_plugin` could not read a suffixed
rule.** `**P-35b —` declared nothing and `P-35b` cited nothing. So `unknown-rule`
never looked at P-10b, P-10c, P-12b, P-12c, P-14b, P-17b, P-17c, P-20b, P-20c,
P-27b, P-35b or P-38b, and a citation of a suffixed rule that does not exist
would have passed. L-1.3 required naming the gap, which would have put
`check_plugin` at exit 3 on the plugin for every later step. The owner,
2026-09-24, chose *"Read them now (Recommended)"* over *"Name the gap; 0.3.2
fixes it"*. The grammar now reads `P-<n><letter>` in declarations and
citations, and the plugin reads `clean  [15 skills, 9 agents, 60 rules, 6 root
entries]`. Before, it read 48. All 12 cited suffixed rules are declared. This
is a declared deviation from L-1.3's *fixes no individual parse*, which is
aimed at the project checks 0.3.2 owns; no subcycle owned this one.

**Found: a case that asserted the defect.**
`fp-audit-filename-outside-the-grammar-is-ignored` expected clean for an open
finding about a closed task, in a file the check could not tie to that task.
That is L-6's failure written as a test. It is now a planted case expecting
the part named. A new twin keeps a notes file with no finding heading clean.

**Found, pre-existing, and added to 0.3.2's §5 row:**
- A freshly scaffolded project's `check_trace` reports four
  `unparseable-protected-path` findings. The template's unfilled cell is split
  on commas before the placeholder test, so its prose reads as four paths.
  The script before step 3.1 does the same.
- `BOARD_ROW` reads rows from every table on the board, including the in-flight
  table's T-19 row.

**Also changed, each with a case:**
- `check_trace`'s fixtures had no `BOARD.md`, and a missing board is now a
  part not evaluated. The control writes one that agrees with the fixture's
  titles whenever a case gives none.
- A branch with no case hid a `TypeError`: `check_plugin`'s empty-prefix-side
  gap added a tuple to a string. It was rewritten when its case was written,
  which is why every branch here got one.
- `check_report` exempted an unfilled `<…>` Containment cell from being named.
  No fixture could reach that branch, and `setup` fills the cell, so the
  exemption is gone and such a row is named like any other.

**For §3.5 to decide.** Gap parts are named without line numbers: a file, a
field or a class. So a ratchet can match a part at HEAD with the same part at
the candidate. A new unparsed row inside a part already named, such as a
twentieth link row on the board, adds no new part. Whether the gate should
compare rows as well as parts is §3.5's question.

### §3.3 as built — untracked files

`MEASURED` 2026-09-24. `check_trace`, `check_refs` and `check_scope` now read
what git would show under `devteam/`: tracked files, and untracked ones that
no ignore rule covers. This is `git ls-files --cached --others
--exclude-standard`, in `result.listed`, the one home for the rule. Each
untracked file a check reads is reported as a finding of its own class,
`untracked-file`, with a CHECKS.md row under each of the three checks (22, 9
and 8 classes). Ignored files, `devteam/.run/` among them, stay invisible.
`check_trace`'s audits now come from the same listing, not from
`os.listdir`, so an ignored audit is invisible there too. `check_report` is
unchanged, because it reads the one task file it is given. FORMATS and the
`check` skill, which both said *git-tracked files only*, now state the rule.

**Two cases asserted the defect.** `check_trace`'s
`fp-untracked-task-is-not-scanned` and `check_refs`'
`fp-untracked-file-is-not-scanned` expected F-131's invisibility, and each is
now a planted case. `check_trace`'s shows the read half as well: R-3,
discharged only by an untracked T-3, is covered.

**F-131, replayed on real files.** In a throwaway copy of the `pricelog`
clone, `tasks/T-19.md` was taken out of the index and left on disk. Before
this step, measured with the scripts at `5a0e329`, compared with after:

| Check | Before | After |
|---|---|---|
| `check_trace` | 7 findings, `[… 18 tasks]`. A `board-drift` (*the board lists T-19, which has no task file*) and four `unknown-reference`s on T-16's dependency, all effects of the invisible file | 3 findings, `[… 19 tasks]`: the two permanent ones, and `untracked-file` naming the cause |
| `check_refs` | 4 `cited-undefined`: T-19, and three of its steps | 1 finding: `untracked-file` |
| `check_scope` | `clean  [18 tasks, 0 live]`, with T-19 silently absent | `untracked-file`, `[19 tasks, 0 live]` |

In the record's own moment nothing cited T-19 yet, so all three said clean
(RECORD.md:1642). The clone itself has no untracked or ignored file, so its
lines are unchanged from §3.2's.

**The controls.**
- `check_trace` goes from 99 to 100 cases, `check_refs` from 79 to 82, and
  `check_scope` from 46 to 48.
- Each plants F-131's shape and a file nothing cites. Each keeps an ignored
  file silent, and in `check_refs` that is the plan's own `devteam/.run/`
  case.
- 9 mutations, all killed: the listing reading the index alone, the listing
  honouring no ignore rule, and each check not naming the file.

**Found: promotion relied on the old reading.** `sandbox.py promote` reads a
task's scope through `check_scope.load_tasks`. It relied on that function
reading tracked files only, so that an uncommitted task file declared no
scope, because a scope nobody committed read as permission is F-118's shape.
Its comment said so. The control caught the break as a crash, from the new
return shape. Behind the crash was the dependency: fixing the unpacking alone
would have let an uncommitted task grant its scope. `declared_scope` now asks
the untracked set directly. `promote-task-file-untracked` fails without that
line (a mutation, killed). `mutate.py`'s declared mutation for `cited_rules`
named §3.2's old line, so it no longer applied; it follows the code again,
and `mutate.py` exits 0.

**For §3.5.** The gate refuses a commit that leaves an untracked file under
`devteam/` unnamed (L-1.5). It reads that from this class in the checks'
`--json`, with the file in the finding's `file`.

### §3.4 as built — accepted findings

`MEASURED` 2026-09-24. A decision accepts a finding, or a part not evaluated,
with an `Accepts.` field in `DECISIONS.md`. Each item names what it accepts as
the check printed it, without the line. The four project checks report what
was accepted, name the decision on their line, and exit 0 when nothing else is
found. An acceptance matching nothing is `stale-acceptance`, emitted by each
check at its own emit site. An acceptance the grammar cannot read accepts
nothing, and `check_refs` reports it as `unparseable-acceptance`, because it
owns `DECISIONS.md`'s grammar. The grammar's one home is FORMATS
§"Accepted findings", and the code's is the end of `result.py`, next to the
identity that §3.5's gate matches by. CHECKS.md now has 23, 11, 14 and 9
classes for the four checks.

**The identity**, which §3.5 uses too, is `result.identity`: check, class,
anchor file, and the message with its line numbers dropped and its whitespace
collapsed. Three messages carry line numbers of their own:
- `check_refs`' `duplicate-id`, *already declared at `<file>:<n>`*;
- a step's `cited-undefined`, *(also 14, 22)*;
- `check_report`'s `unfinished-scope`, *`<file>:<n>` carries …*.

`result.stable` drops all three, so an edit above the anchor moves nothing an
acceptance names. A part is matched by its name, which has no line number by
§3.2's design. An acceptance matches every finding with its identity.

**Choices made inside L-1.6, each open to the owner's overruling:**
- **A decision that accepts must say who reviewed it.** *Kept by the owner,
  2026-09-24, choosing "Keep it (Recommended)" over "Drop it".* L-1.6 records the
  manager's `REVERSIBLE` acceptance *as unreviewed under P-27*, and a
  decision's `Reviewed.` line is the only place that record lives. So an
  `Accepts.` field in a decision whose `Reviewed.` is missing, or is not
  `client`, `unreviewed` or `proceeded-unreviewed (Q-n)`, accepts nothing and
  is reported. `unreviewed` is a new value, for an acceptance the manager made
  without a question. **The P-26 class of an acceptance stays a written rule,
  not a check**, because nothing a check can read says what a finding means to
  the charter.
- **An acceptance cites nothing.** `check_refs` reads no identifier from an
  acceptance's lines. Without that, accepting a finding that names an
  undeclared `T-99` cites `T-99` again from `DECISIONS.md`. That file sorts
  first, so the finding's anchor moves there, and the acceptance naming its
  old anchor goes stale. This has the shape of the existing rule that quoted
  check output cites nothing. Two things watch the carve-out:
  `stale-acceptance` and `unparseable-acceptance`.
- **A run judges only what it evaluated.**
  - `check_report` applies an acceptance only in the run for the task whose
    file it names, never in another task's run or a step's.
  - `check_report` cannot accept a part at all. Its parts name no task, so no
    run could ever find such an acceptance stale.
  - `check_scope`'s `undeclared-write` is judged only by the run for the task
    it names.
  - A class that `--pre-plan` excludes is not judged.

  Without these rules, every targeted run would call every other target's
  acceptance stale.
- **A superseded decision's acceptances are withdrawn silently.** It is read
  from any `Supersedes.` line, and nothing is reported, because P-23 forbids
  editing the superseded text to fix it.
- **A line under the field that is not an item** is reported at its own line.
  The well-formed items beside it still accept exactly what they name.

**On the corpus**, in copies of the `pricelog` clone. D-68 accepts the two
standing findings, and D-69 accepts the five parts `check_trace` does not
evaluate and the four `check_refs` does not. Both use the template's bullet
form with `Reviewed. unreviewed`, and RECORD.md cites them. The scratch runner
is `.internal/scratch/0.3.1/accept_cases.py`.

| Planted case | `check_trace` | `check_refs` |
|---|---|---|
| Both findings accepted | exit 0, `clean, 7 accepted by D-68, D-69`, naming each | exit 0, `clean, 4 accepted by D-69` |
| A third finding: T-3's `Verify.` removed | exit 1, `1 finding(s), 7 accepted by D-68, D-69` | exit 0 |
| The anchors shifted: two lines above T-1's and T-10's titles | exit 0. Anchors now read `:3`, and both are still accepted | exit 0 |
| The finding fixed: T-1 given `Discharges. none` | exit 1, `stale-acceptance DECISIONS.md:1932` | exit 0 |
| Outside the grammar: the item's backticks dropped | exit 1. T-1's finding is back, unaccepted | exit 1, `unparseable-acceptance DECISIONS.md:1932` |

The plan's first case, *exits 0 and names both*, needed D-69's parts as well,
as `_s30` predicted. That is L-1.6's last bullet, used as designed: the parts
stay blind until 0.3.2 and 0.3.3. The unmodified clone reads as in §3.3.
`pricelog`'s own 67 decisions are written as prose paragraphs
(`**What was decided.**`), not the template's bullets. An acceptance added to
a decision in that style is refused, because it has no `Reviewed.` line.

**The controls.**

| Control | Cases before → after | Mutations killed |
|---|---|---|
| `test_result.py` | 28 → 65 | 18 of 18 |
| `check_trace` | 100 → 113 | 6 of 6 |
| `check_refs` | 82 → 94 | 7 of 7 |
| `check_report` | 59 → 64 | 3 of 4 |
| `check_scope` | 48 → 54 | 2 of 2 |

The fourth `check_report` mutation lets a `check_report` part through the
grammar. It is equivalent under that control, because `check_report`'s
coverage rule already skips an acceptance with no file. The refusal is what
makes the line visible, so a `check_refs` case plants it, and that control
and `test_result.py` both kill it. All 17 controls are green, and `mutate.py`
exits 0. `test_result.py`'s false-positive total was a hand count, and it now
counts case names. `check_refs`' total now counts what ran.

**Found and fixed before any figure above was taken:**
- The line said `traced end to end` while five accepted items were parts
  nobody read. An accepted part now keeps that note off the line.
- `check_report`'s `--blocking-only` trailer read the raw findings. So when
  every finding had been accepted, it would have called them all advisory. It
  reads what remains now.
- One control assertion indexed into `accepted` and crashed under the
  mutation that emptied it. A crash is not a named FAIL, and that case now
  reports one.

**For §3.5:**
- The gate matches by `result.identity`. In `--json`, an accepted finding is
  out of `findings` and in `accepted`, with `by`. An accepted part is in
  `accepted_not_evaluated`.
- **A commit that fixes an accepted finding adds a `stale-acceptance`.** The
  ratchet therefore refuses it unless the same commit supersedes the accepting
  decision. This keeps the zero, and it means the fix and the withdrawal land
  together. The finding's text already says what to do.
- `check_report` judges acceptances only in a task's own run. So if the gate
  runs `check_report` at all, it must run it per task.
- An acceptance matches every finding with its identity. So a second
  occurrence of the same message in the same file is accepted too. That is the
  same granularity as §3.2's parts.

**For §3.7:**
- The `DECISIONS.md` template shows `Accepts.` and the `Reviewed.` vocabulary.
- Its header carries the *restore the zero, not suppress* sentence. DESIGN.md
  records the reason: the header is read at the moment of temptation, and a
  skill is not.
- The `check` skill says how to accept a finding and what `stale-acceptance`
  asks.
- L-1.9's third rule is written into PROTOCOL, and the new rows then cite it.

**Also changed.** CONSOLIDATION §8a and DESIGN.md's accepted-findings passage
each gained a dated note saying what was built, and why it differs from the
shape they had proposed.

**Spend so far**, by 0.3.0's meter from 08:16:26 to 10:51 EDT, is 382 requests
and `$51.61` at list price:
- `_s29`: 97 requests, `$12.36`;
- `_s30`: 189 requests, `$27.70`;
- `_s31`, to this point: 96 requests, `$11.55`.

That is 1.9 to 2.4 points of the all-model gauge, *converted, not read*
(L-18). `_s28`, 0.3.0's session, made 3 more requests in the window, for
`$0.30`. They fall after 0.3.0's count closed at 08:08, and the close should
place them.

### §3.5 as built — the gate

`MEASURED` 2026-09-24. `scripts/gate.py commit (-m <msg> | -F <file>) [--pre-plan]
[--dry-run] [--json] [--wait <s>] [-C <dir>] -- <path> …` does what `git commit
-F <msg> -- <paths>` does, with the checks run on the commit itself before it
exists:

1. **The candidate** is built with no ref moved. A temporary index holds HEAD's
   tree and the named paths' working-tree content. The project's `pre-commit`,
   `prepare-commit-msg` and `commit-msg` hooks run against it, as `git commit`
   runs them. Then `commit-tree` makes a commit object whose parent is HEAD.
2. **It is evaluated** in one clean checkout of HEAD, then of the candidate.
   That covers the three plan checks, `check_scope` once per task, and
   `check_report` per task (below), all with `--at-commit` and `--json`.
3. **It commits only if the candidate adds nothing** HEAD lacks, counted by
   `result.identity`, net of what decisions accept. Every run prints what
   stands, what the commit fixes, what it let through and why, and the
   working state.
4. **The branch moves by compare-and-swap** — `update-ref <branch> <candidate>
   <old HEAD>` — so the commit made *is* the candidate object. The index is then
   brought level for the paths the commit changed, and `post-commit` runs.

It exits 0 committed, 1 refused, 2 could not run, and nothing is committed on 1
or 2. `P-49` is the rule, and `docs/CHECKS.md` has its five refusal classes.
`check_plugin` now reads `gate.py`'s emit sites, and fired on a copy of the
plugin with one of its rows removed.

**The class table** decides where each class is judged. It covers all 57
classes of the four project checks, read from what their code reads:

| Check | Tree-derived: judged at HEAD and at the candidate | History-derived: the same, so each sees the history its commit has | Working state: read from the live checkout and printed |
|---|---|---|---|
| `check_trace` (23) | `unparseable-protected-path`, `orphan-scope`, `uncovered-requirement`, `unmotivated-task`, `unverified-requirement`, `missing-field`, `unknown-reference`, `dependency-cycle`, `gate-omits-decision`, `board-drift`, `one-sided-link`, `template-drift`, `amendment-omits-condition`, `amendment-names-unknown`, `unreachable-acceptance`, `unparseable-task`, `bad-kind`, `open-finding-at-close`, `unjustified-task` | `re-litigated-requirement` (every committed revision of `REQUIREMENTS.md`); `unrecorded-amendment` (the first committed `Requires-write.` against the tree's) | `untracked-file` |
| `check_refs` (11) | `cited-undefined`, `defined-uncited`, `duplicate-id`, `broken-link`, `bad-status`, `leak`, `control-character`, `undispositioned-finding`, `unparseable-acceptance` | — | `untracked-file` |
| `check_report` (14) | `no-file`, `no-report`, `wrong-task`, `missing-field`, `bad-report-status`, `status-mismatch`, `no-evidence`, `unfinished-scope` | `unknown-commit`, `head-subject` (`git log --all`) | `dirty-tree` (`git status`); `budget-mismatch` and `model-mismatch` (the ignored `.run/locks/T-n.sandbox`, and a sandbox outside the repository) |
| `check_scope` (9) | `overlapping-scope`, `empty-scope`, `scope-escapes-tree`, `unparseable-scope-entry` | `undeclared-write` (a task's commits, by subject); `misattributed-write` (commits since the claim) | `foreign-write` (`git status -uall`), `untracked-file` |

That makes 40 tree-derived, 6 history-derived and 7 working-state classes. Each
check's `stale-acceptance` follows the class it accepts. L-1.5 named three
working-state classes. The two harness classes are the other two: they read
no commit's content, so they are printed and never refuse. `broken-link` looks
for its target on disk. So a link to an ignored or untracked file resolves in
the live checkout and not in a checkout of the commit, which is what a clone
sees.

**`--at-commit`, and why the gate does not filter.** Each check lists its
working-state classes in its own `WORKING_STATE`, beside the code that reads
them. `--at-commit` excludes those classes by the caller's declaration, and
names each one (L-1.2), through `Result.exclude_classes`. The gate cannot
simply drop them from a worktree run. An acceptance of a working-state finding
would then read as stale at every commit, and the gate would refuse the commit
that accepted it. `fp-accepting-a-working-state-finding-is-not-stale-at-a-commit`
is that case. `check_scope` also names `PER_TASK`, the classes only a run for a
task evaluates, which its `covers` and the gate both read.

**Settled by the owner, 2026-09-24,** each put as options with a marked
recommendation, and each recorded in §2 under L-1.5:
- **F-19's window only.** The allowance covers `check_trace`'s task-side
  message for a task whose title reads DONE (`check_trace.CLOSED_LINK`, kept
  beside the emit). The requirement-side shape — a requirement naming a task
  that does not list it — is refused. So is a RUNNING task's link.
- **`--pre-plan` holds back only requirements the commit adds.** It is not
  passed to `check_trace`. An added `uncovered-requirement` whose requirement
  HEAD lacks is let through and printed (`check_trace.UNCOVERED`). One HEAD
  has, which has lost its task, is refused as any added finding is. This
  replaced the refusal class `pre-plan-with-tasks`, which is gone.
- **Two narrowings of where `check_report` is judged, kept.** It runs for a
  task that has a REPORT line or a closed title (`reporting`). Every other task
  is named in the line as not run: a planned or claimed task reads `no-report`,
  which is true and not a defect, and would refuse every planning commit. And
  `unfinished-scope` counts only in a commit that changes the task's own file
  (`at_its_close`), because the rule is about the task at its close (below).

**Choices made inside L-1.5, each open to the owner's overruling:**
- **The commit is the candidate object,** moved to by compare-and-swap, never
  by `git commit`. So "the commit made is the commit checked" holds by
  construction, and a moved HEAD refuses with no window. The hooks run as
  `git commit` runs them, in its order: `pre-commit`, then `prepare-commit-msg`,
  then `commit-msg`, then cleanup. A project's secret scanner therefore still
  runs on every agent commit. `--dry-run` runs no hook, as `git commit
  --dry-run` runs none. `commit-tree` does not read `commit.gpgSign`, so the
  gate passes `-S` when a project signs its commits.
- **Exit 2 is the gate unable to answer:** its arguments, no project at HEAD
  (the client commits the scaffold, `setup` says), nothing to commit, a check
  that exits 2, output the gate cannot read, or JSON whose `exit` disagrees
  with the process's. `head-moved` is a refusal, exit 1. Its remedy is to run
  the gate again.
- **One gate at a time per repository,** by a lock under the git directory.
  `git log --all`, which three history classes read, includes every
  worktree's HEAD. With two gates running, each one's evaluation of HEAD would
  see the other's candidate. A killed run's checkout is swept at the start,
  under the lock.
- **A finding counts.** A second identical broken link in one file is added
  (`a-second-instance-of-a-standing-finding-is-refused`). Several runs of one
  check count an identity at its most in any one run, so a per-task run adds
  only its own.
- **Parts compare by name, not by row** — §3.2's question. A part's rows are
  named by position only, so there is no identity to ratchet on that survives
  an edit above them. The standing part is printed at every run.
  `check_report`'s parts name no task, so they are keyed by task.
- **A path naming the whole repository is refused.** That is the `git add -A`
  form `run` §4.5 forbids (A1). A named directory commits what is under it,
  its untracked files included.
- **P-49 is written now, and `gate.py` is in `check_plugin`'s `EMITTERS` with
  its `CHECKS.md` section now,** not at §3.7. The step must end with
  `check_plugin` clean, and the rows need a rule that exists. L-1.9's rules 1
  and 3 take the next free numbers when §3.7 writes them.

**Found in review, before any run: the sweep would have deleted a repository.**
Its first version removed any worktree whose path held `/devteam-gate-`, the
main one included, and then deleted the directory above it. A repository under
such a directory would have been deleted. It now touches only a detached
`checkout` directly inside a `devteam-gate-*` temporary directory, and never
the main worktree. `fp-a-repository-under-a-gate-named-directory-is-never-swept`
plants it, and `gate-sweep-matches-fragments` is killed.

**The planted cases,** each one of the run's own failures where there is one:

| §3.5's case | The control's cases |
|---|---|
| F-114 | `f114-a-third-revision-of-a-requirement-is-refused` also shows the working-tree `check_trace` missing it; `fp-f114-a-second-revision-goes-through` |
| F-12 | `fp-f12-a-red-finding-in-another-tasks-file-lets-an-unrelated-commit-through` (its standing leak printed); `f12-removing-a-declaration-another-file-cites-is-refused` (the finding lands in a file the commit did not touch) |
| §8b | `fp-8b-a-defect-in-an-unnamed-uncommitted-file-does-not-refuse`; `8b-the-same-defect-in-a-named-path-refuses` |
| F-131 at the gate | `f131-a-board-citing-an-untracked-task-is-refused-naming-it`; `fp-f131-with-the-task-file-named-too-it-goes-through`; `untracked-an-untracked-devteam-file-refuses-an-unrelated-commit`; `fp-an-ignored-file-under-devteam-does-not-refuse`; `fp-a-named-directory-names-what-is-under-it` |
| F-19, both ways | `fp-f19-a-closed-tasks-link-is-allowed-while-it-is-in-flight`; `f19-with-the-in-flight-row-removed-it-is-refused`; RECORD.md:486's shape, `fp-f19-a-long-closed-tasks-link-is-allowed-by-the-task-its-requirement-names`; the owner's narrowing, `f19-a-requirement-naming-a-task-that-does-not-list-it-is-refused` and `f19-a-running-tasks-link-is-refused-…` |
| F-24 and F-117 | `f24-a-red-result-exits-1-with-head-the-index-and-the-tree-untouched`, and every refusing case asserts the same of HEAD, the index, the working tree and the worktree list; `f117-a-report-citing-an-unknown-commit-is-refused`; `fp-a-report-naming-its-own-commit-by-subject-lands`, which resolves only in the history the commit creates |
| Failing closed | `closed-a-check-{exit2,garbage,exit-disagrees}-means-nothing-is-committed`, on a copy of `scripts/` whose `check_refs` is a stand-in; `closed-a-held-lock-means-nothing-is-committed` |
| HEAD moved | `head-moved-between-the-evaluation-and-the-commit-is-refused`: the stand-in commits to the live repository while the candidate is evaluated |
| The commit made is the commit checked | every committed case asserts HEAD is the candidate, its parent the old HEAD and its tree the candidate's; each named path is staged as committed, and each unnamed edit is still in the working tree |

Beside them are cases for `--pre-plan` (four), the report selection (three),
`unfinished-scope` both ways, identity (a line shift, a second instance, an
added part), accepted findings (three), hooks (two), working state (two),
invocation (nine), the sweep (two) and `--at-commit` (four, one per check).
`test_gate.py` has 59 cases, 22 of them false-positive controls (37 %). The
first run passed all 52 it then had, and one first-run pass proves nothing, so
the control was shown able to fail. All 37 mutations are killed, each on its
own copy of the whole plugin, set in `.internal/scratch/0.3.1/mutant_sets.py`.
Two first reported *survived* because their text no longer matched the code.
The runner printed a mutation that did not apply the same way as one that
survived, and it now says *not applied*. Both were re-aimed and are killed.

**The corpus replay.** Every `pricelog` commit was rebuilt as the gate's
candidate on its parent and gated with `--dry-run`, 591 of them. For each, the
parent was checked out and the commit's tree read into the index and working
tree, and its changed paths were named with its own message. `--pre-plan` was
passed before the first task file. The runner is
`.internal/scratch/0.3.1/replay.py`.

| Result | Commits |
|---|---|
| would commit | 524 |
| refused | 66 |
| could not run | 1: `9d6485f`, the scaffold, whose parent holds no `devteam/` |

A gate run took a median 2.6 s, 4.3 s at the 90th percentile and 5.7 s at most,
on 48 cores.

**A harness artifact, measured and removed before any figure above.** A clone's
remote-tracking refs name the corpus's last commit. So `git log --all` saw the
run's *future* at every candidate. The first replay reported an
`undeclared-write` for T-5 and T-14 at their plan commits, before the writes it
named existed. The runner now leaves no ref but HEAD. Any later replay must do
the same.

**The plan's two expected refusals:**
- **RECORD.md:1001–1002 is refused.** `400b799` cites C-6 before it exists.
- **RECORD.md:1112–1113 is not refused, by L-1.5's own design, and this is a
  declared deviation from §3.5's expectation.** The window there is F-19's
  window. `fbec402`, T-14's closing commit, is let through as F-19's window
  with T-14 in the in-flight table, which is the state RECORD.md:1112
  describes. The run's gate refused a manager commit at that moment, and that
  commit was held and never made. To show the allowance both ways on real data,
  T-14's in-flight row was removed in a commit beneath the same candidate, with
  nothing else changed. The candidate is then refused on exactly that
  `one-sided-link`: the run's own mutation (RECORD.md:487), on the corpus.

F-19's window was used at 17 commits, every task close in the run. Without it,
each close would be refused.

**Every refusal, classified.** Each of the 66 is in exactly one family, checked
by script. 48 are real, and 18 are false positives in three families:

| Family | Commits | Verdict |
|---|---|---|
| a citation of an identifier declared later | `400b799` (C-6), `29fae65` (C-1), `77d2d71` (C-5), `d53aea6`, `f25c413` (D-41 to D-43, before `e569eff` declared them) | real — the record's forward-citation defect (RECORD.md:1001) |
| an absolute home path in a tracked file | `30c4ead` (F-12's own), `cd1b50e`, `7b20ee4`, `36ef1c2`, `bf8ad6a`, `275a750`, `775468d`, `bc785f2`, `27f818c`, `29909d3` | real — F-12, F-89 |
| a requirement's third rewrite | `e569eff` (F-114's own, RECORD.md:1339–1340); `2b8f0de` (before D-23), with T-10's permanent `one-sided-link` | real |
| a closing report citing no commit | `ab29fc7`, the `- HEAD 62c25a0 …` line | real — F-117 (RECORD.md:1336) |
| a manager commit under a task's prefix | `92dc849` | real — F-123 |
| F-85's accepted pair, where it began | `8f67cc0` (T-1's `missing-field`) | real; its board rows are a part not evaluated, below |
| a digest cited before it was committed | `a29a18e` | real — `ba0160d`'s subject says so |
| the claim commit writing the supervisor's title | `2833b2b`, and `cbbad26`, where T-5's restart meets it again | real — T-5's own report calls it permanently misattributed |
| a report citing a commit by a subject no commit has | `30105a6` (S-3 was never promoted), `52484e1`, `166653d` (prose under `commits:`), `3347800` (a subject its own commit does not carry) | real |
| a requirement naming a task that does not list it | `f8d9d6e`, `928418f` (R-1 and R-4 read `in-progress (T-9)`, which never listed them) | real — the owner's narrowing refuses these, which L-1.5 read literally would have let through |
| a requirement resting on a decision no gate names | `fc24f47` | real — remedied later, as RECORD.md:1358 records |
| audit findings cited, with no declaration the grammar reads | `34c4430`, `c073ff4` | real per the grammar — F-104, F-112 (RECORD.md:1315); 0.3.3's |
| prose in a vocabulary cell | `a887d47` (a task title) | real |
| charter Version 2 | `786f3c0`: no `Re-affirmed.` block, and a Protected paths cell written as prose | real |
| a step report missing two keys | `0889ff1` | real |
| a step cited as the citing task's own | `b0c709c` (S-4 declared in a blockquote), `4c63513` (`T-8's S-2/S-3/S-5`), `58b402d` | real per the grammar: `T-n.S-m` is the form that names another task's step |
| a title moved before its report landed | `848e061`, `ec555bd` | real — the report and the title land together, as F-113's remedy has it |
| a task closed by its escalation's answer, with no closing report | `ffcca83` (T-12) | real — §3.1's corpus fact |
| a field or header the grammar cannot read, added | `4a7a081`, `cfee116` (wrapped fields, F-132); `07d67b9`, `c6d1f82`, `13d51e4` (REPORT headers, F-34); `7aa90ac` (a reconstructed report, F-86, F-88) | real parts not evaluated — the project can write each readably; 0.3.2 reads them |
| audits outside the namespace | `7234e48` | a real part not evaluated — F-99; 0.3.3's |
| **false positive:** a restart over edits made while the task was stopped | `f24b5f3`, `42425a8`, `b33e29b`, `79e9b7f`, `f0db47c`, `ff71ccc`, `29781ce`, `223c4d6`, `eaebe7d`, `90b8b42`, `b57c29e`, `7442808`, `1c89448`, `586717e` | Every `misattributed-write` here names a manager write made while its task was stopped or closed, measured from each write's parent. `check_scope` opens a live task's window at its *first* claim, and `check_report` reads a finished claim's report. That is F-135 and F-136, which are 0.3.2's; `42425a8` and `79e9b7f` are also re-claims (F-39, 0.3.4's `re-claim`). **Answer:** until 0.3.2 lands, the remedy is an acceptance by decision, which 0.3.2's fix then makes stale. No run happens before it (L-8). |
| **false positive:** a requirement honestly left `open` over a DONE task | `63674de`, `ef8ee45`, `3bcf1f2` | the check has no term for a partial or awaited discharge — 0.3.2's "partial and re-established requirement states". **Answer:** the same interim remedy. |
| **false positive:** `iterate`'s charter gate | `34f778d` | **Answered by the owner:** it goes through with `--pre-plan`, measured on the replay's own setup. |

Two further refusals the replay found were answered in the gate, and planted
both ways:
- **`1f88891`, T-8's tests-first stub,** in `store.py`, which T-3 closed over days
  before. It made T-3's report read unfinished. `unfinished-scope` now counts
  only in a commit that changes the task's own file. So the stub is judged at
  T-8's close, and the commit goes through.
- **The board's rows at `8f67cc0`.** Its seven link rows add the part
  *BOARD.md's task rows*. That is true under L-1.3, and it means every planning
  commit on a board shaped like the template or like pricelog's is refused
  until 0.3.2 reads the rows (F-70). The interim remedy is an acceptance of the
  part, which is L-1.6's last bullet.

**Found at the joints, and each added to its subcycle's row in the cycle
README's §5:**
- **For 0.3.2: `board-drift` will fire in F-19's window once it can read the
  board.** The board says `CLAIMED` while the title says DONE, and
  `BOARD_PHASES` allows only RUNNING or PLANNED under `CLAIMED`. Today it is
  silent only because template-shaped and link-shaped rows do not parse. When
  F-70 is fixed, the gate would refuse every close, unless `CLAIMED` allows
  DONE while the task is in the in-flight table. The fixture uses the
  template's backticked rows for this reason.
- **For 0.3.2: `git log --all` is the range of three history classes.** So
  another branch's commits count for a task. Both of the gate's evaluations see
  the same refs, so the comparison is fair. But the claim window 0.3.2 keys
  should decide that range too.
- **For 0.3.2: until it lands, the gate refuses** the three families above:
  restarts, honest requirement states and link-shaped boards.
- **For 0.3.11: `root_guard.py` reads `chmod +x <path>` as writing a root file
  named `+x`.** It refused one of this step's scratch commands, and the
  command ran without `chmod`.

**The controls.**

| Control | Cases before → after | Mutations killed |
|---|---|---|
| `test_gate.py` | new, 59 | 37 of 37 |
| `test_result.py` | 65 → 68 (`exclude_classes`) | 3 of 3 |
| `check_trace` | 113 → 114 | 1 of 1 |
| `check_refs` | 94 → 95 | 1 of 1 |
| `check_scope` | 54 → 55 | 1 of 1 |
| `check_report` | 64 → 65 | 1 of 1 |

Each check's new case is its `--at-commit` exclusion, named in the line, with
the rest deciding the exit. The four `*-ignores-at-commit` mutations are killed
by the check's own control and by the gate's.

**For §3.6:**
- The gate commits with `commit-tree` and `update-ref`, and never runs `git
  commit`. The hook sees the agent's command, `python3 …/gate.py commit …`.
  Whether the hook also refuses `commit-tree` or `update-ref` typed by hand —
  the same commit with the checks skipped — is §3.6's question.
- The gate runs the project's git hooks and knows nothing of Claude Code's.
  `gate.py` inside a sandbox is not needed, because promotion gates workers
  (P-44).

**For §3.7:**
- Every skill that commits into `devteam/` names `gate.py commit -F <msg> --
  <paths>` where it names `git commit -F <msg> -- <paths>` today. Each says what
  a refusal asks for: fix the commit; accept the finding by a decision;
  `head-moved`, run again; exit 2, the invocation or the gate.
- `onboard`'s *"`--pre-plan` matters here and only here"* is now untrue.
  `iterate`'s charter-gate commit uses it too.
- FORMATS now says every check takes `--at-commit`. §3.7 states the gate's
  own contract there beside the four results.
- CHECKS.md's `stale-acceptance` rows re-cite L-1.9's third rule once it is
  written, and `adds-not-evaluated` may cite its first.

**Spend so far**, by 0.3.0's meter from 08:16:26 to 12:17 EDT: 500 requests
and `$73.43` at list price across 0.3.1's four sessions.
- `_s29`: 97 requests, `$12.36`;
- `_s30`: 189 requests, `$27.70`;
- `_s31`: 109 requests, `$13.42`;
- `_s32`, to this point: 105 requests, `$19.95`.

That is 2.7 to 3.4 points of the all-model gauge, *converted, not read*
(L-18). `_s28`'s 3 requests (`$0.30`) are still unplaced, as §3.4 records.
